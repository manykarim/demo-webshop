## 1. Prerequisites and test harness

- [x] 1.1 Confirm that `reproducible-image` is merged into the working branch (it may still be an active OpenSpec change until its task 10.6): `uv.lock`, `[dependency-groups] dev` in `pyproject.toml`, `Settings.app_version` in `backend/app/core/config.py`, `tools/entrypoint.py`, `.github/workflows/image.yml` with the jobs `check`, `build`, `smoke` and `publish`, and the shared pytest harness of its design D11 exist, and `requirements.txt` is gone. The harness consists of `backend/tests/harness.py` (`isolated_app`, `SCRUBBED_ENV_VARS`, `SCRUBBED_ENV_PREFIXES`, `PINNED_ENV_VARS`, `pin_test_environment`) and the root `backend/tests/conftest.py` with the fixtures `temp_database`, `app_client`, `seeded_app_client`, `pdf_unavailable` and `fake_weasyprint`. Verify that `grep -cE "^def (temp_database|app_client|seeded_app_client|pdf_unavailable|fake_weasyprint)\(" backend/tests/conftest.py` prints `5`, that `grep -nE "^(SCRUBBED_ENV_VARS|PINNED_ENV_VARS)" backend/tests/harness.py` finds both declarations, and that `uv lock --check && uv sync --locked && uv run pytest backend/tests` passes before any code change.
- [x] 1.2 Set up this change's test suite on the shared harness owned by `reproducible-image` (design D12). In `backend/tests/harness.py` (modified), add `"WORKSHOP_ADMIN_TOKEN"` to `SCRUBBED_ENV_VARS` and `"WORKSHOP_SHARED_MODE": "false"` to `PINNED_ENV_VARS`; change nothing else in `harness.py` and nothing in `backend/tests/conftest.py`. Create the suite directory `backend/tests/spaces/` with `__init__.py` (new), `conftest.py` (new) and `helpers.py` (new). The suite conftest imports `backend.app` only inside fixture bodies, sets no environment variables, resets no engine globals and defines no fixture named `temp_database`, `app_client`, `seeded_app_client`, `pdf_unavailable` or `fake_weasyprint`; task 2.1 adds its only fixture, `shared_mode`. In `helpers.py`, add the plain functions:
  - `sqlite_rows(sql, params=())`, which reads the database file that `settings.database_url` points to at call time (the active harness database) with stdlib `sqlite3` and returns the rows as tuples;
  - `rendered_stage(html)` (`item-card` → v2, `product-tile` → v4, no `data-test="product-card"` → v3, otherwise v1). It relies only on the product-card names that `drift-coverage` fixes in its Decision 1 (stage-2 block `item-card`, stage-4 block `product-tile`, `data-test="product-card"` only in stages 1-2). `drift-coverage` runs this suite unchanged after its rebase and updates this helper only if its `StageSpec` tables rename those blocks (`drift-coverage` task 15.1).

  Suite tests use `seeded_app_client` wherever seeded users or orders are needed, `app_client` where products and flags are enough, and `temp_database` where no client is wanted. No workshop-spaces test reads `LOCATOR_*` or `BUG_*` keys from `response.context["feature_flags"]`, because `drift-coverage` narrows that context to non-workshop flags. Verify with `backend/tests/spaces/test_spaces_harness.py` (new):
  - `pin_test_environment` on a plain dict holding `WORKSHOP_SHARED_MODE=true` and `WORKSHOP_ADMIN_TOKEN=x` sets `WORKSHOP_SHARED_MODE` to `false` and removes the token;
  - inside a test, `os.environ["WORKSHOP_SHARED_MODE"]` is `false` and `WORKSHOP_ADMIN_TOKEN` is not in `os.environ`;
  - with `seeded_app_client`, `GET /health` returns 200, `sqlite_rows("select count(*) from products")` returns `[(12,)]`, and `sqlite_rows` finds the user `jamie@flowlinesupply.com`;
  - with `app_client`, parametrized over no flag, `LOCATOR_V2`, `LOCATOR_V3` and `LOCATOR_V4` (each enabled alone with `PUT /api/admin/flags/<key>` and body `{"enabled": true}`), `rendered_stage` of `/products` returns `v1`, `v2`, `v3` and `v4`.

  Also verify that `git diff --quiet "$(git merge-base HEAD main)" -- backend/tests/conftest.py` exits 0 and that `uv run pytest backend/tests` passes.

## 2. Settings: shared mode and cache deprecation

- [x] 2.1 In `backend/app/core/config.py`, add `shared_mode: bool = False` (`WORKSHOP_SHARED_MODE`) and `admin_token: SecretStr | None = None` (`WORKSHOP_ADMIN_TOKEN`), with a model validator that rejects shared mode without a non-empty token and names `WORKSHOP_ADMIN_TOKEN` in the error. Verify with `backend/tests/unit/test_settings_shared_mode.py` (new). It builds every settings object as `Settings(_env_file=None, ...)` and uses `monkeypatch.delenv(..., raising=False)` for both variables, so neither a local `.env` nor the process environment affects it. Checks: defaults are off and None; `Settings(_env_file=None, shared_mode=True)` and `Settings(_env_file=None, shared_mode=True, admin_token="")` raise a `ValidationError` whose text contains `WORKSHOP_ADMIN_TOKEN`; `Settings(_env_file=None, shared_mode=True, admin_token="x")` succeeds; the token value does not appear in `repr()`; with a `.env` in `tmp_path` containing `WORKSHOP_ADMIN_TOKEN=x` and `monkeypatch.chdir(tmp_path)`, `Settings(_env_file=None, shared_mode=True)` still raises. Then add the suite fixture `shared_mode` to `backend/tests/spaces/conftest.py`: it monkeypatches `settings.shared_mode` to true and `settings.admin_token` to `SecretStr("test-token")` and yields `"test-token"`. Verify in `backend/tests/spaces/test_spaces_harness.py`: a test without the fixture sees `settings.shared_mode` false (pinned by the harness environment); a test with it sees shared mode on and `settings.admin_token.get_secret_value()` equal to the yielded token; a later test in the same module sees shared mode off again.
- [x] 2.2 Cover the scenario *Misconfigured start* at process level. In `backend/tests/unit/test_settings_shared_mode.py`, add a subprocess test that runs `python -c "import backend.app.main"` with an explicit `env`: a copy of `os.environ` with `WORKSHOP_SHARED_MODE=true` and `WORKSHOP_ADMIN_TOKEN=` (empty, which the validator rejects and which wins over any token in `.env`). Verify that it exits non-zero with stderr containing `WORKSHOP_ADMIN_TOKEN`, and check by hand that `WORKSHOP_SHARED_MODE=true WORKSHOP_ADMIN_TOKEN= uv run uvicorn backend.app.main:app --port 9090` exits before port 9090 accepts connections.
- [x] 2.3 Deprecate `feature_flag_cache_seconds`. The field stays accepted but is ignored. A small function `warn_obsolete_settings(settings)`, called once from the application lifespan, logs exactly one deprecation warning when `WORKSHOP_FEATURE_FLAG_CACHE_SECONDS` is set. In `.env.example`, remove `WORKSHOP_FEATURE_FLAG_CACHE_SECONDS` and add commented `WORKSHOP_SHARED_MODE=false` and `WORKSHOP_ADMIN_TOKEN=` entries with a one-line explanation each. Import `warn_obsolete_settings` by name into `backend/app/main.py`. Verify with a `caplog` test in `backend/tests/spaces/test_settings_startup.py` (new): `warn_obsolete_settings` logs one warning for `Settings(_env_file=None)` built with the variable set through `monkeypatch.setenv`, and none for one built after `monkeypatch.delenv`, so a `.env` copied from the current `.env.example` (which sets the value to 10) cannot affect the result; with `backend.app.main.warn_obsolete_settings` monkeypatched to a recording stub, entering `isolated_app(tmp_path)` from `backend/tests/harness.py` records exactly one call, with the global `settings`. Also verify that `grep -n FEATURE_FLAG_CACHE .env.example` returns nothing.

## 3. Space resolution

- [x] 3.1 Create `backend/app/core/spaces.py` (new) with `DEFAULT_SPACE = "default"`, the pattern `^[a-z0-9](?:[a-z0-9]|-(?=[a-z0-9])){0,38}$`, a constant for the format message, and `normalize_space(raw) -> str | None`. The function trims the value, returns None if it is empty, lower-cases it, and raises `ValueError` with the format message if it is invalid. Verify with `backend/tests/unit/test_spaces.py` (new): `octocat` and `OctoCat` become `octocat`, and `default` is accepted; `""` and `"  "` return None; `-bad--name-`, `a--b`, `a-`, a 40-character string, `a:b`, `a_b` and `a%b` are rejected; a 39-character string is accepted.
- [x] 3.2 In `backend/app/core/spaces.py`, add `resolve_workshop_space(request)`: it reads the `X-Workshop-Space` header, then the `space` query parameter, then the `workshop_space` cookie directly from `request`, with no `Header`, `Query` or `Cookie` parameters; the first non-empty source wins, and an invalid value raises `HTTPException(400)` with the format message; it stores `request.state.workshop_space` and the D1 cookie markers. Also add a `current_space` accessor dependency, register `FastAPI(dependencies=[Depends(resolve_workshop_space)])` in `backend/app/main.py`, and add `space` next to `version` in `WorkshopStatus` (`backend/app/api/workshop.py`). Verify with `backend/tests/spaces/test_space_resolution.py` (new), using `app_client`:
  - *Header wins*: header `octocat` plus cookie `hubot` gives status `space` `octocat`;
  - *No space given*: the space is `default`;
  - *Mixed-case handle*: `OctoCat` gives `octocat`;
  - *Invalid identifier*: `-bad--name-` returns 400 with the format explanation on `/` and on `/api/workshop/status`;
  - an invalid header is not rescued by a valid cookie, and header `octocat` plus `?space=-bad-` returns 200 in `octocat` (lower sources are not evaluated);
  - an empty header falls through to the query parameter or cookie;
  - `/api/cart/`, a router added in `configure_routes()`, also answers 400 for an invalid header;
  - *Static asset with invalid identifier*: `/static/app.js` and the stylesheet URL linked from `/` (fetched without a space; `/static/styles.css` today, `/assets/styles.<digest>.css` after `drift-coverage`) both return 200 with header `-bad--name-`;
  - `/openapi.json` returns 200 with an invalid header and declares no parameter named `X-Workshop-Space`, `space` or `workshop_space`;
  - *Requests outside space resolution*: with header `-bad--name-`, `GET /no-such-path` returns 404 with the same body as without the header, and `GET /docs` returns 200 with the same body as without the header; neither response sends a `workshop_space` `Set-Cookie`.
- [x] 3.3 Add a pure ASGI middleware (not `BaseHTTPMiddleware`) in `backend/app/core/spaces.py` and register it in `backend/app/main.py`. On `http.response.start` it reads the marker from `scope["state"]` and appends `Set-Cookie: workshop_space=<id>; Path=/; Max-Age=2592000; SameSite=Lax; HttpOnly`, or an expiring cookie for an invalid cookie. Verify in `test_space_resolution.py`:
  - `/?space=octocat` sets exactly this cookie, and a follow-up request from the same `TestClient` without the parameter reports status `space` `octocat` (first half of *Human switches space via URL*);
  - `/products/99999?space=octocat` (404) still sets the cookie;
  - `?space=default` sets the cookie to `default`;
  - cookie `workshop_space=-bad-` returns 400 with an expiring `Set-Cookie`, and the next request resolves to `default`;
  - a space from the header or cookie alone sends no `Set-Cookie`;
  - *Header beats query parameter*: header `octocat` plus `?space=hubot` reports status `space` `octocat` and sends no `Set-Cookie`;
  - with `seeded_app_client` and `fake_weasyprint`, `/api/docs/orders/{id}/invoice.pdf` for a seeded order is requested first without a query parameter and then with `?space=octocat`; the second response has status 200 with the same body bytes as the first and carries the `workshop_space` `Set-Cookie`, which shows that the cookie hook appends a header to a `FileResponse` without touching its body (seeded orders stay visible in every space, also after task 7.4 scopes `api/docs.py`). Do not use `?space=`, whose empty value counts as "not provided" (D1), so neither request would take the cookie path; keep the plain request first, because the client stores the cookie the second one sets.

## 4. Schema: space flag table, order space column, start-up upgrade

- [x] 4.1 In `backend/app/models/feature_flag.py`, add the `SpaceFeatureFlag` model: table `space_feature_flags` with columns `space VARCHAR(39)`, `key VARCHAR(64)` and `enabled BOOLEAN NOT NULL`, and `PRIMARY KEY (space, key)`. In `backend/app/models/cart.py`, widen `CartItem.session_key` to `String(128)`; this is documentation only and needs no migration. Verify with `backend/tests/spaces/test_schema_upgrade.py` (new), using `temp_database`: after `init_db()`, `PRAGMA table_info(space_feature_flags)` on the yielded file lists the three columns with `space` and `key` as primary-key members, both on a fresh file and on a file first given only the pre-change tables with stdlib `sqlite3`.
- [x] 4.2 Add `Order.space` (`String(39)`, nullable, indexed) to `backend/app/models/order.py`. Move `ensure_product_columns` and `ensure_order_columns` from `backend/app/seeds/seed_data.py` into `backend/app/core/db.py`, keep them importable from `seed_data`, and call them in `init_db()` right after `create_all`, so both the lifespan and the seed entrypoint run them. Remove the now redundant explicit helper calls in `seed_products` and `seed_users`; `seed_data.main()` has no call of its own and picks the helpers up through `init_db()` (tidying; running the helpers twice would be harmless). Extend `ensure_order_columns`: it adds `space VARCHAR(39)` when the column is missing; on every call it runs `UPDATE orders SET space = 'default' WHERE space IS NULL AND user_id IS NULL` (idempotent; seeded orders always have a `user_id`) and `CREATE INDEX IF NOT EXISTS` on `orders(space)`. Verify in `test_schema_upgrade.py`, using `temp_database`, with a file whose `orders` table is created with the pre-change DDL through stdlib `sqlite3` and holds one order with a `user_id` and one without:
  - after `init_db()`, the column and the index exist (`PRAGMA index_list(orders)`), the order without a user has `space = 'default'`, and the user order has NULL;
  - insert another order with no `user_id` and no `space`, as an older image would write it; after a second `init_db()` it has `space = 'default'`, and the user order is still NULL;
  - a third `init_db()` changes nothing;
  - on a fresh `temp_database` file, `seed_data.main()` leaves every seeded order with `space IS NULL`.

## 5. Effective-flags seam

- [x] 5.1 Rewrite `backend/app/core/feature_flags.py`:
  - remove the `TTLCache`;
  - add `baseline_flags()`, which returns a new dict of the seeded `FEATURE_FLAGS` defaults from `backend/app/seeds/seed_data.py` with preset `clean` applied on top, importing both lazily inside the function to avoid import cycles (after `drift-coverage`, the preset comes from `build_presets()`);
  - add `resolve_effective_flags(space)`: it uses a short-lived session from `get_session_factory()`; for `default` it reads the global rows and applies environment overrides; for any other space it starts from `baseline_flags()`, applies that space's rows, then environment overrides, and never reads `feature_flags`; it returns a new dict;
  - add a `get_effective_flags` dependency that uses `current_space`;
  - add `set_flags(session, space, updates)`, which upper-cases keys and commits once. For `default` it upserts `feature_flags` and creates unknown keys; for other spaces it runs `INSERT … ON CONFLICT (space, key) DO UPDATE` on `space_feature_flags`;
  - add `clear_space_flags(session, space)`;
  - keep `list_feature_flags(session)`, `is_enabled(session, key)` and `set_feature_flag(session, key, enabled)` with unchanged signatures as temporary, uncached wrappers over the `default` space, each marked "removed in 6.4": `list_feature_flags` returns `await resolve_effective_flags(DEFAULT_SPACE)`; `is_enabled` returns `(await resolve_effective_flags(DEFAULT_SPACE)).get(key.upper(), False)`; `set_feature_flag` calls `await set_flags(session, DEFAULT_SPACE, {key: enabled})` and returns `await resolve_effective_flags(DEFAULT_SPACE)`. They keep `main.py`, `api/workshop.py`, `api/admin.py`, `api/search.py` and `services/ai_service.py` importable until their tasks move them.

  Verify with `backend/tests/spaces/test_effective_flags.py` (new), using `app_client`:
  - `default` returns the global rows;
  - a space value wins over the baseline;
  - *Default space does not leak*: with global rows `LOCATOR_V2`, `BUG_WRONG_PRICE` and `NEW_CART_UI` enabled, `resolve_effective_flags("octocat")` without space rows equals `baseline_flags()`;
  - an unknown key that exists only as a global row is absent for `octocat`;
  - `settings.feature_flag_overrides` wins over space, baseline and global values;
  - a space row does not change `default`;
  - mutating a returned dict does not affect the next call;
  - a `set_flags` change is visible to the very next resolve;
  - a multi-key `set_flags` makes exactly one commit (SQLAlchemy `commit` event counter on `get_engine().sync_engine`);
  - keys are upper-cased;
  - `clear_space_flags("octocat")` keeps the `hubot` rows.

  Also verify that `uv run python -c "import backend.app.main"` succeeds and that `reproducible-image`'s `backend/tests/integration/test_harness.py` still passes (its `isolated_app` clears the flag cache only while `_cache` exists).
- [x] 5.2 Switch the six page routes in `backend/app/main.py` (`/`, `/products`, `/products/{product_id}`, `/cart`, `GET /checkout`, `POST /checkout`) to `flags = Depends(get_effective_flags)`. Verify with `backend/tests/spaces/test_page_flags.py` (new), using `app_client`: with `NEW_CART_UI` enabled only for space `octocat` (a non-workshop flag that `drift-coverage` keeps in the template context), `response.context["feature_flags"]["NEW_CART_UI"]` is true for all six routes under header `octocat` and false without the header (for `POST /checkout`, use the empty-cart 400 render); with `LOCATOR_V2` enabled only in `octocat`, `rendered_stage()` of `/products` returns `v2` under header `octocat` and `v1` in `default`; `grep -n list_feature_flags backend/app/main.py` returns nothing.
- [x] 5.3 Move `backend/app/api/search.py` (`SEARCH_V2`) and `backend/app/services/ai_service.py` onto the seam: `AIService` receives the flags dict from `backend/app/api/ai.py` through `get_effective_flags` instead of calling `_load_flags`. Verify in `test_page_flags.py`: with `SEARCH_V2` enabled only in `octocat`, `/api/search/?query=desk` returns `mode` `semantic` under header `octocat` and `keyword` without it; with `AI_VARIED_RESPONSES` enabled only in `octocat`, `POST /api/ai/ask` returns `workshop_flags.varied` true in `octocat` and false in `default`; `grep -n "is_enabled\|_load_flags\|list_feature_flags" backend/app/api/search.py backend/app/services/ai_service.py backend/app/api/ai.py` returns nothing.
- [x] 5.4 Add the seam guard test `backend/tests/unit/test_flag_seam.py` (new). It parses every module under `backend/app` with `ast` and fails when `FeatureFlag` or `SpaceFeatureFlag` is imported outside `core/feature_flags.py`, `core/db.py`, `models/` and `seeds/seed_data.py`. Verify that the test passes on the branch and fails after temporarily adding `from ..models.feature_flag import FeatureFlag` to `backend/app/api/admin.py`.
- [x] 5.5 Remove `cachetools` from `[project.dependencies]` and `types-cachetools` from the `dev` group in `pyproject.toml`, then regenerate `uv.lock` with `uv lock`. Verify that `grep -rn cachetools backend pyproject.toml` returns nothing, that `uv lock --check && uv sync --locked && uv run pytest backend/tests` passes (the 5.1 wrappers keep every importer working), and that the lock diff removes only these direct entries. A transitive dependency may keep `cachetools` in the lock.

## 6. Space-scoped workshop and admin APIs

- [x] 6.1 Scope `GET /api/workshop/status`, `POST /api/workshop/preset` and `POST /api/workshop/flags` in `backend/app/api/workshop.py` to `current_space`. Writes use one `set_flags` call, `current_status` in the response is computed from a fresh `resolve_effective_flags(space)` rather than the per-request cached dependency value, and `GET /api/workshop/presets` stays unchanged. Verify with `backend/tests/spaces/test_space_flags_api.py` (new), using `app_client`:
  - *Preset does not leak*: `octocat` applies `stage2` and `hubot` applies `stage3`; status `locator_stage` is `v2` and `v3`, and `rendered_stage()` of `/products` matches for each header;
  - *Default space does not leak*: in local mode, after `stage2` and `buggy` are applied without a space, space `hubot`, which has set no flags, reports `v1` and no active bugs in status, and `rendered_stage()` of `/products` under `hubot` returns `v1`;
  - *Immediate effect*: `octocat` applies `stage3` and the next `/products` renders `v3`;
  - each preset request makes exactly one commit;
  - `POST /api/workshop/flags` under `octocat` writes only `octocat` rows in `space_feature_flags` and leaves `feature_flags` unchanged, checked with `sqlite_rows`;
  - the unknown-preset response body is unchanged;
  - `grep -n "list_feature_flags\|set_feature_flag" backend/app/api/workshop.py` returns nothing.
- [x] 6.2 Scope `GET /api/admin/flags` and `PUT /api/admin/flags/{flag_key}` in `backend/app/api/admin.py` to `current_space` through the seam. Verify in `test_space_flags_api.py`: a PUT under `octocat` creates a `space_feature_flags` row and leaves `feature_flags` untouched; a GET under `octocat` returns that space's effective flags (the baseline keys plus its own rows), and a GET without a space returns the global flags; *Environment override*: with `settings.feature_flag_overrides = {"LOCATOR_V2": False}` and `octocat` enabling `LOCATOR_V2` by PUT, `/products` under `octocat` renders `v1` and status reports `LOCATOR_V2` as false; `grep -n "list_feature_flags\|set_feature_flag" backend/app/api/admin.py` returns nothing.
- [x] 6.3 Add `require_workshop_write_access` to `backend/app/core/spaces.py` and attach it to the control operations `PUT /api/admin/flags/{flag_key}`, `POST /api/workshop/preset` and `POST /api/workshop/flags`. It allows the request when shared mode is off, when the space is not `default`, or when `Authorization: Bearer <token>` matches (compared with `secrets.compare_digest`). Otherwise it returns 401 with `WWW-Authenticate: Bearer` and the D6 message, which names `?space=<github-handle>` and `X-Workshop-Space`. Verify with `backend/tests/spaces/test_shared_mode.py` (new), using `app_client` and the `shared_mode` fixture:
  - *Participant forgets the space header*: a preset without space or token returns 401, the message contains `?space=` and `X-Workshop-Space`, and `feature_flags` rows are unchanged;
  - *Participant uses their space*: header `octocat` without a token returns 200, and status under `octocat` shows the preset's stage;
  - the bearer token yielded by `shared_mode` returns 200 in `default`, and a wrong token returns 401;
  - admin PUT and bulk flags in `default` without a token return 401;
  - *Shopping in the default space stays open*: `POST /api/cart/items`, `GET /api/cart/`, `DELETE /api/cart/`, `POST /api/checkout/` and form `POST /checkout` in `default` without a token are not rejected with 401, and neither are GET status, presets and admin flags.
- [x] 6.4 Delete the temporary wrappers `list_feature_flags`, `is_enabled` and `set_feature_flag` from `backend/app/core/feature_flags.py` (6.3 does not use them). Verify that `grep -rn "list_feature_flags\|is_enabled\|set_feature_flag\b\|_load_flags" backend/app` returns nothing, that `uv run python -c "import backend.app.main"` succeeds, and that `uv run pytest backend/tests` passes, including the 5.4 seam guard.

## 7. Space-scoped carts and orders

- [x] 7.1 Add `cart_session_id(session_id)` (`session_id or "workshop-demo"`) and `cart_storage_key(space, session_id)` to `backend/app/core/spaces.py`. In `default`, `cart_storage_key` returns `cart_session_id(session_id)`, otherwise `f"{space}:{cart_session_id(session_id)}"`. Verify with unit tests in `backend/tests/unit/test_spaces.py` for all four combinations: `("default", None)` → `workshop-demo`, `("default", "demo")` → `demo`, `("octocat", None)` → `octocat:workshop-demo`, `("octocat", "demo")` → `octocat:demo`; `cart_session_id(None)` is `workshop-demo` and `cart_session_id("demo")` is `demo`.
- [x] 7.2 Add an optional `session_label` keyword to `CartService` in `backend/app/services/cart_service.py` (`self.session_label = session_label or session_key`): `get_cart_state()` reports `"session": self.session_label`, and `DELETE /api/cart/` in `backend/app/api/cart.py` returns `cart_service.session_label`. Use `cart_storage_key` for the pages in `backend/app/main.py` (session id from the header, then the cookie) and in `backend/app/api/cart.py` and `backend/app/api/checkout.py` (header only). `api/cart.py` passes `cart_session_id(session_id)` as the label for GET, POST items and DELETE; pages and `api/checkout.py` pass only the storage key. Verify with `backend/tests/spaces/test_space_carts.py` (new), using `app_client`:
  - *Same session id in two spaces*: `octocat` and `hubot` add different products with `X-Session-ID: demo`, and each `GET /api/cart/` contains only its own items; `cart_items.session_key` values are `octocat:demo` and `hubot:demo`; under `octocat`, `POST /api/cart/items`, `GET /api/cart/` and `DELETE /api/cart/` all report `"session": "demo"`;
  - two session ids in `octocat` have separate carts;
  - *Cart API without session id*: under header `octocat` without `X-Session-ID`, `POST /api/cart/items` stores `cart_items.session_key` `octocat:workshop-demo`, the `default` cart `workshop-demo` stays empty, and `POST /api/cart/items`, `GET /api/cart/` and `DELETE /api/cart/` all report `"session": "workshop-demo"` (as `acceptance-conformance` API-005 AC-1, AC-8 and AC-10 expect); no reported `session` contains `:`;
  - `/cart` under header `octocat` without a `session_id` cookie shows the items added through the API without `X-Session-ID`;
  - `/cart` with cookie `session_id=demo` and header `octocat` shows the items added through the API with `X-Session-ID: demo`;
  - `default` keeps the legacy keys `demo` and `workshop-demo` and reports the same values as `session`.
- [x] 7.3 Add a `space` keyword to `OrderService.create_order` in `backend/app/services/order_service.py`. `POST /checkout` in `backend/app/main.py` and `POST /api/checkout/` in `backend/app/api/checkout.py` pass the resolved space, including `"default"`. Seeding passes no space. Verify with `backend/tests/spaces/test_space_orders.py` (new), using `seeded_app_client`: `orders.space` is `octocat` after an API checkout under `octocat`, `default` after an API checkout without a space, and `octocat` after a form `POST /checkout` under `octocat`; all seeded orders keep `space IS NULL`.
- [x] 7.4 Add `order_visible_in(space)`, meaning `space IS NULL OR space = :space`, to `backend/app/services/order_service.py`. Apply it in `backend/app/api/auth.py` as a filtered `selectinload(User.orders.and_(...))`, and in `_fetch_order` in `backend/app/api/docs.py`, which then returns the same 404 `Order not found` as for a missing id. Verify in `test_space_orders.py`, using `seeded_app_client` and `fake_weasyprint`: *Order history isolation*: after an API checkout under `octocat`, link the order to `jamie@flowlinesupply.com` with `UPDATE orders SET user_id` (checkout does not link users yet). `POST /api/auth/login` under `hubot` then contains all seeded order numbers but not the new order, and under `octocat` it contains both; under `hubot`, `GET /api/docs/orders/{id}/invoice.pdf` for that order returns the same status and body as for a non-existent id; under `octocat`, it returns 200 with `application/pdf`.

## 8. Space reset

- [x] 8.1 Add `POST /api/workshop/reset` to `backend/app/api/workshop.py`, guarded by `require_workshop_write_access`. For a non-default space, one transaction runs `clear_space_flags`, deletes `cart_items` with `session_key LIKE '<space>:%'`, then deletes the `order_items` of that space's orders and the orders themselves. After the commit, it removes `invoice_<order_number>.pdf` and `summary_<order_number>.pdf` from `settings.pdf_output_dir` and ignores missing files. The response contains `space`, counts of removed flags, cart items and orders, `locator_stage`, `active_bugs` and `ai_mode`. Verify with `backend/tests/spaces/test_space_reset.py` (new), using `seeded_app_client` and `fake_weasyprint`, so checkouts write their PDF files into the temporary `pdf_output_dir`:
  - *Reset one space*: in local mode, the `default` space has presets `stage3` and `buggy` applied without a space; `octocat` has `stage2`, cart items (with and without `X-Session-ID`) and one runtime order; `hubot` has `stage3`, cart items and one runtime order;
  - after resetting `octocat`, it renders `v1`, its status reports no active bugs and `NEW_CART_UI` false, its cart is empty (including `octocat:workshop-demo`), no orders with `space='octocat'` exist, no `order_items` remain for the deleted order ids, and its `invoice_<order_number>.pdf` and `summary_<order_number>.pdf` files, present before the reset, are gone;
  - `hubot` still reports `v3` and keeps its cart, its order and its PDF files, the `default` space still reports `v3`, and the number of seeded orders is unchanged;
  - resetting `octo` leaves `octocat:*` carts intact;
  - the counts in the response match the removed rows.
- [x] 8.2 Implement the `default` branch of reset: it sets global flags to `baseline_flags()` (the `FEATURE_FLAGS` defaults from `backend/app/seeds/seed_data.py` with preset `clean` applied); it deletes carts whose `session_key` contains no `:`; it deletes orders with `space = 'default'` and their PDFs; it never runs a seeder. Verify in `test_space_reset.py`, using `seeded_app_client` and `fake_weasyprint`:
  - in local mode, after `stage2`, `NEW_CART_UI=true`, a legacy cart and a runtime order in the default space, reset without credentials returns 200;
  - after that reset, all global flags equal `baseline_flags()`, legacy carts are gone, the default-space order and its PDF files are gone, `octocat:*` carts and `octocat` orders and their PDF files remain, and seeded orders are unchanged;
  - *Default-space reset without token*: with the `shared_mode` fixture, reset in `default` without a token returns 401 and deletes no rows; with the bearer token it returns 200; under `octocat` without a token it returns 200.

## 9. Shared-mode AI and space indicator

- [x] 9.1 In `backend/app/services/ai_service.py`, when `settings.shared_mode` is on, always use `MockLLM` and ignore both `settings.ai_provider` and the per-request `provider` override. Verify in `test_shared_mode.py` (*Real AI provider configured*). Set up shared mode on, `settings.ai_provider="openai"` and `settings.ai_api_key="test"`, replace `AIService._call_openai_chat` with a recording stub, and set `AI_DETERMINISTIC` to false in space `octocat`. Then check: `POST /api/ai/ask` under `octocat` with body `{"question": "desk", "provider": "openai"}` returns `provider` `mock`; the stub is never called; with shared mode off, the same request calls the stub (control).
- [x] 9.2 Make both values the indicator needs readable from the layout, then render it. `settings` is not reachable from a template today (`_mount_static_and_templates` in `backend/app/main.py` registers only the Jinja global `brand_name` and the `currency` filter, and no route passes `settings` in its context), so register the module-level `settings` object there as a second Jinja global, `templates.env.globals["settings"] = settings`, next to `brand_name`. It is the same object the `shared_mode` fixture (task 2.1) monkeypatches, and the attribute is read at render time, so the fixture takes effect. The space needs no new context entry: all six page routes already pass `"request": request` into the template context, and `resolve_workshop_space` (task 3.2) stores the identifier in `request.state.workshop_space`, which is backed by `scope["state"]` and therefore visible on the request object the template receives. In the site header of `backend/app/templates/base.html`, render `<p class="workshop-space" data-workshop-space="{{ request.state.workshop_space }}">Space: {{ request.state.workshop_space }}</p>` when `request.state.workshop_space` is not `default` or `settings.shared_mode` is on; do not use a bare `{{ space }}`, which no route defines. In shared mode in the `default` space, render the D8 sibling `<p class="workshop-space-hint">` whose text names `?space=<github-handle>` and `X-Workshop-Space`. Use no drift helper and no flag condition. Add minimal styling in `backend/app/static/styles.css`: one `.workshop-space` rule and one `.workshop-space-hint` rule, each a rule of its own (not a grouped selector) and both required, using no selector other than `.workshop-space`, `.workshop-space-hint` and `[data-workshop-space]`. Both rules must exist, because `drift-coverage` task 15.1 carries both forward into every per-stage stylesheet variant. These are not drift-covered names; when `drift-coverage` moves the stylesheet to `backend/app/assets/styles.css`, the rules move with it unchanged (`drift-coverage` task 15.1). Verify with `backend/tests/spaces/test_space_indicator.py` (new), using `app_client`:
  - *Participant checks their space*: `/` under header `octocat` contains `data-workshop-space="octocat"` and `Space: octocat`, and no `workshop-space-hint`;
  - *Default space in local mode*: `/` without a space returns 200 and contains no `data-workshop-space` and no `workshop-space-hint` (assert the status too, so a template error that renders neither element cannot make this pass);
  - *Default space on the shared instance*: with the `shared_mode` fixture, `/` without a space contains `data-workshop-space="default"`, `Space: default` and a `workshop-space-hint` containing `?space=`; under header `octocat` in shared mode there is no hint;
  - the indicator appears on `/products`, `/products/1`, `/cart` and `/checkout`;
  - the indicator markup is byte-identical under presets `stage1` to `stage4` and `buggy` (applied in `octocat`), and the shared-mode `default` indicator and hint are byte-identical under the same presets (applied with the bearer token);
  - *Human switches space via URL*, second half: `/?space=octocat` and then `/products` from the same client without the parameter both show `octocat`, and `?space=default` removes the indicator in local mode;
  - the stylesheet linked from `/` (read from its `<link rel="stylesheet">`, not hard-coded) contains both a `.workshop-space` rule and a `.workshop-space-hint` rule, matching each selector as a whole token (a `.workshop-space` followed by `{`, `,` or whitespace, so `.workshop-space-hint` does not satisfy the first check), so the check survives `drift-coverage`'s stylesheet move and matches what `drift-coverage` task 15.1 carries forward into every stage variant.

## 10. SQLite concurrency

- [x] 10.1 In `get_engine()` in `backend/app/core/db.py`, register a `connect` listener on `engine.sync_engine` for file-based SQLite URLs, skipping `:memory:`. The listener runs `PRAGMA journal_mode=WAL`, `busy_timeout=5000` and `synchronous=NORMAL`, and logs a warning when the returned journal mode is not `wal`. Set `pool_size` and `max_overflow` explicitly, starting at 20 and 20; task 14.2 tunes the values and task 14.4 confirms them. Verify with `backend/tests/spaces/test_sqlite_tuning.py` (new), using `temp_database`: a connection from `get_engine()` reports `journal_mode` `wal`, `busy_timeout` 5000 and `synchronous` 1, and `get_engine().pool.size()` equals the configured value.
- [x] 10.2 Add `backend/tests/spaces/test_space_concurrency.py` (new), using `temp_database`. One `asyncio.run` runs `init_db()`, `seed_products` and `seed_feature_flags`, then 40 concurrent `httpx.AsyncClient` instances over `ASGITransport(app)`, then `shutdown_db()`, so every pooled connection belongs to one event loop. Each client uses its own space `load-NNN` for a `stage2` or `stage3` preset, an API add-to-cart, an API checkout and a status call. Verify that every response is 2xx, that no `database is locked` error is logged, and that each client's final status and cart match its own actions.

## 11. Cross-cutting verification

- [x] 11.1 Add `backend/tests/spaces/test_local_mode.py` (new), using `app_client`, with shared mode off (the harness default). Verify: *Local participant applies a preset*: `POST /api/workshop/preset` `stage2` without space or token returns 200, and the next `/products` without a space renders `v2`; `POST /api/workshop/flags`, admin PUT and reset without credentials return 200; `/health` returns `{"status": "ok", "version": ...}` unchanged; pages in `default` carry no indicator, no hint and no `workshop_space` `Set-Cookie`; cart rows use the legacy unprefixed keys.
- [x] 11.2 Add a no-enumeration test to `test_shared_mode.py`, using `app_client`, the `shared_mode` fixture and `fake_weasyprint` (so the checkouts write into the temporary `pdf_output_dir`). Build the state an enumeration endpoint could read and leave it in place: `hubot` applies a preset, adds an item to its cart with `X-Session-ID: demo`, checks out and then adds another item, because `POST /api/checkout/` calls `CartService.clear_cart()` and without the second add there would be no live cart row; record its order number. A second space, `mona`, does the same and then calls `POST /api/workshop/reset`, so a space whose rows a reset removed is covered too (writes in a non-default space need no token in shared mode, task 6.3). Before calling any GET route, assert with `sqlite_rows` (task 1.2) that `hubot` is enumerable at call time — at least one `space_feature_flags` row with `space = 'hubot'`, one `cart_items` row with `session_key` `hubot:demo` and one `orders` row with `space = 'hubot'` — and that `mona` has none of the three, so the test fails loudly instead of going vacuous if checkout, reset or the storage layout changes later. Build the list of endpoints from `app.routes`, not from `/openapi.json`: every `fastapi.routing.APIRoute` whose `methods` include GET and whose path has no `{`. This includes routes registered with `include_in_schema=False` (as `drift-coverage` does for the workshop and admin routers and its hidden `/search/results`) and excludes mounts such as `/static` and `/assets` and FastAPI's own `/openapi.json`, `/docs`, `/docs/oauth2-redirect` and `/redoc` routes. Before calling anything, assert that the list contains `/api/workshop/status`, `/api/workshop/presets`, `/api/admin/flags`, `/`, `/products`, `/cart` and `/checkout`, so a later change that hides or renames routes fails the test instead of shrinking it. Call each route with header `X-Workshop-Space: octocat` and no token. Verify, over the raw response body bytes so non-HTML responses are covered too, that no response body contains `hubot`, `mona` or `hubot`'s order number (a 4xx such as a 422 for a missing query parameter is acceptable) and that no path in `app.routes` contains `spaces`.
- [x] 11.3 Run the full gate locally: `touch "$marker"`, then `uv lock --check && uv sync --locked && uv run pytest backend/tests`. Also verify that `uv run pytest backend/tests` passes with a temporary `.env` in the repository root containing `WORKSHOP_SHARED_MODE=true`, `WORKSHOP_ADMIN_TOKEN=x` and `WORKSHOP_FEATURE_FLAG_CACHE_SECONDS=10` (back up an existing `.env` first), then remove that file. After both runs, verify the stray-file check of `reproducible-image` D11: `find . \( -path ./.venv -o -path ./.git \) -prune -o \( -name '*.db' -o -name '*_ORD-*.pdf' \) -newer "$marker" -print` prints nothing. Push the branch and verify that the pull request's `.github/workflows/image.yml` run passes `check` and the `build` job's amd64 smoke phases (`fresh`, `restarted`, `recreated`), where `tools/smoke_image.py` runs against the default space.
- [x] 11.4 Build the image with `docker build -t demo-webshop:spaces .` and verify: `docker run -p 9090:9090 demo-webshop:spaces` applies a preset without a space (200) and shows the indicator at `/?space=octocat`; `docker run -e WORKSHOP_SHARED_MODE=true demo-webshop:spaces` exits non-zero with a log naming `WORKSHOP_ADMIN_TOKEN`; with `-e WORKSHOP_ADMIN_TOKEN=test` added, a preset without a space returns 401, with `X-Workshop-Space: octocat` it returns 200, and `/` without a space shows `data-workshop-space="default"` and the hint.
- [x] 11.5 Check rollback compatibility for persisted databases with a named volume, which takes uid 1000 ownership from the image's `/data`, so no `chown` is needed on Linux. Run `demo-webshop:spaces` with `-v spaces-rollback:/data`, create an `octocat` order through the API, then stop the container. Start the last image before spaces that uses `/data` (`ghcr.io/manykarim/demo-webshop:0.2.0`, or the recorded `build` digest of the last `main` run before this change) with the same volume, and verify that `/`, `/api/cart/` and an API checkout all return 200. Stop it and start `demo-webshop:spaces` again with the volume. Read the database with `docker run --rm -v spaces-rollback:/data --entrypoint python demo-webshop:spaces -c "import sqlite3; print(sqlite3.connect('/data/workshop.db').execute('select id, order_number, user_id, space from orders').fetchall())"`. Verify:
  - the order created by the old image has `space = 'default'`, the `octocat` order still has `space = 'octocat'`, and seeded orders keep `space IS NULL`;
  - under `X-Workshop-Space: hubot`, `GET /api/docs/orders/{id}/invoice.pdf` for the old image's order returns 404;
  - `POST /api/workshop/reset` without a space (local mode) deletes that order.

  Finish with `docker volume rm spaces-rollback`. If a bind mount is preferred, prepare it first with `mkdir -p tmpdata && sudo chown 1000:1000 tmpdata`.

## 12. Documentation

- [x] 12.1 Add a section with the heading `## Workshop spaces` to `docs/WORKSHOP-FEATURES.md`. `drift-coverage` keeps this section when it rewrites the document (`drift-coverage` tasks 13.1 and 15.1). The section is prose only and contains no tables (a JSON example in a code block is allowed), because `drift-coverage` parses the document's tables by their headings. It covers:
  - space precedence (header, then query, then cookie, then `default`; the header also wins over `?space=`) and the GitHub-handle format;
  - `default` as a reserved name, and `<handle>-ws` for legacy handles;
  - the 30-day cookie and switching back with `?space=default`;
  - that a new or reset space starts from the clean baseline, independent of the `default` space;
  - the cart fallback session `workshop-demo` in every space;
  - `POST /api/workshop/reset`, with a curl example;
  - the shared-mode 401 message and the bearer token;
  - the `space` and `version` fields in status, with an updated JSON example;
  - the `[data-workshop-space]` indicator, the shared-mode `default` indicator with its hint, and masking both in visual checks;
  - that agents and new browser contexts need the header or `?space=`;
  - signing in again after switching spaces (`flowline_auth_state` in `localStorage`);
  - Browser `extraHTTPHeaders` sending the header to every origin;
  - the workshop repository's `coolify` profile as the consumer.

  Verify every curl example against a seeded server on a throwaway store. Local-mode examples run against `TMP=$(mktemp -d) && WORKSHOP_DATABASE_URL=sqlite+aiosqlite:///$TMP/ws/workshop.db WORKSHOP_PDF_OUTPUT_DIR=$TMP/ws/pdfs uv run python -m tools.entrypoint uvicorn backend.app.main:app --port 9090` (the entrypoint creates the directories and seeds; generated PDFs stay out of `backend/app/static/pdfs`). Shared-mode examples run against a second start with `WORKSHOP_SHARED_MODE=true WORKSHOP_ADMIN_TOKEN=test` added, on port 9091 or after stopping the first server, with `<token>` replaced by `test`: a preset without a space returns 401 with `WWW-Authenticate: Bearer` and a body containing `?space=` and `X-Workshop-Space`; the same call with `X-Workshop-Space: <handle>` returns 200; the call in `default` with `Authorization: Bearer test` returns 200. Also verify that `grep -n "^## Workshop spaces" docs/WORKSHOP-FEATURES.md` finds exactly one line, and that `awk '/^## Workshop spaces$/{f=1;next} /^## /{f=0} f' docs/WORKSHOP-FEATURES.md | grep -c '^[[:space:]]*|'` prints `0`.
- [x] 12.2 Write `docs/COOLIFY-RUNBOOK.md` (new) per design D11, with `<coolify-host>` placeholders. Cover:
  - a Docker-image application from `ghcr.io/manykarim/demo-webshop` on port 9090 with a health check on `/health`; candidates deployed by digest (the `build` job's `digest` output of the candidate commit's `main` run, used only after that run's `publish` succeeded), never by `edge` or `sha-<short>`; the workshop deployed by its `workshop-<id>` tag;
  - the environment to set: `WORKSHOP_SHARED_MODE=true`, a secret `WORKSHOP_ADMIN_TOKEN` (`openssl rand -hex 32`) and `WORKSHOP_AI_PROVIDER=mock`;
  - what never to set, with the reasons: `WORKSHOP_DATABASE_URL` and `WORKSHOP_PDF_OUTPUT_DIR` (the image defaults under `/data` are required), `WORKSHOP_FLAG_*`, `WORKSHOP_APP_VERSION`, `WEB_CONCURRENCY` or `--workers`;
  - no persistent volume or storage on `/data`;
  - the rule never to change the `default` space on the shared instance, facilitator demos in a facilitator space, and the token only for rehearsal checks and restoring `default`;
  - procedures for deploy-and-verify (the version check for `X.Y.Z` and `workshop-<id>` tags; for a candidate deployed by digest, `dev` plus the running digest equal to the recorded `build` digest and the `org.opencontainers.image.revision` label equal to the candidate commit; no database or PDF path override; 401 without a space; the test-space indicator; `Space: default` with the hint without a space; status `v1` without a space; `invoice.pdf` 200 after a test checkout), rollback by a version tag or recorded digest (never `sha-<short>`), resetting one space, and resetting all (redeploy);
  - the scale-out contingency: N instances, participants split by the first character of the handle so that each of `a`-`z` and `0`-`9` belongs to exactly one instance, with registered counts per instance, a URL table, per-instance load tests with each share × 1.25 users run at the same time on a shared host, and leaks or 5xx treated as defects;
  - how to run the load test, and the difference between the early and the gating run;
  - an empty capacity record table (kind, image reference, commit, digest, `drift_and_bug` present, users, duration, p95, 5xx count, leak count) and an empty rehearsal record table;
  - checklists for after-merge, T-1 week, T-1 day (including the revision comparison or gating re-run) and day-of;
  - a note not to export access logs.

  Verify through a facilitator review, and by executing every command in the runbook during tasks 14.1-14.4 and 15.1.

## 13. Load test tooling

- [x] 13.1 Add a `loadtest` dependency group with `locust` to `pyproject.toml` and run `uv lock`. Verify that: `uv lock --check` passes; `uv run --group loadtest locust --version` prints a version; `uv export --locked --no-dev --format requirements-txt | grep -ci locust` prints `0`.
- [x] 13.2 Create `loadtest/locustfile.py` (new):
  - each simulated user takes the next space id (`load-001`, `load-002`, …) and its own `X-Session-ID`, and sends both on every request; wait time between tasks is 0.5-2 s;
  - in `on_start`, the user reads `GET /api/workshop/presets` once, applies preset `clean` in its own space and starts an expected flag map from `clean`'s flags; after every preset switch that returns 2xx, it merges the preset's `applied_flags` into the map without clearing keys the preset does not set;
  - weighted tasks cover `/` and `/products`, `GET /api/products/` (a plain API request with the user's space and session headers and no asset parsing, sent under the fixed request name `GET /api/products/`, so the `[slow-bug]` suffix from 13.3 is the only variation of that name), `/products/{id}`, `POST /api/cart/items`, `/cart`, form `POST /checkout` (after an add to cart, so the cart is not empty), preset switches, and `GET /api/workshop/status`; after a successful checkout, the user takes the order id from the confirmation page's `/api/docs/orders/<id>/invoice.pdf` link, records it for itself and in a process-wide registry keyed by its space, and clears its own expected cart contents, because the checkout empties the very cart it filled (the `leak:cart` bookkeeping of 13.3); preset switches use `stage1` to `stage4`, `buggy` and `clean`, plus `drift_and_bug` when the presets listing contains it;
  - for `/` and `/products`, the user parses the returned HTML and fetches every same-origin `<link rel="stylesheet" href>`, `<script src>` and `<img src>` with its headers, under the fixed request names `asset:stylesheet`, `asset:script` and `asset:image`; no asset path is hard-coded.

  Add `loadtest/` to `.dockerignore`, which `reproducible-image` owns (its design D9: a change that adds a root directory the image does not copy extends `.dockerignore` in the same task that creates the directory, so the directory never enlarges the context sent to the builder, which may be remote or a CI cache). Rebuild the image first with `docker build -t demo-webshop:spaces .` now that `loadtest/` exists, and verify that `docker run --rm --entrypoint sh demo-webshop:spaces -c 'ls -A /app'` lists only `.venv`, `backend` and `tools`, that `docker run --rm --entrypoint python demo-webshop:spaces -c "import locust"` exits non-zero, and that the D9 context probe in the form of `reproducible-image` task 6.3, `printf 'FROM busybox\nCOPY . /ctx\nRUN find /ctx -maxdepth 3 | sort\n' | docker build --progress=plain --no-cache -f - .`, lists no `/ctx/loadtest` line (`--progress=plain --no-cache` are required; without them the `RUN` output is suppressed or served from cache and the probe proves nothing). With `loadtest/` excluded from the context, the `ls -A /app` check no longer demonstrates that the runtime `COPY` list leaves the directory out; the context probe is what shows it never reaches the builder. Then start the target with `docker run --rm -d --name ws-load -p 9090:9090 demo-webshop:spaces`: `--rm` plus the image's fixed `/data` gives a fresh seeded store per run, so neither `WORKSHOP_DATABASE_URL` nor `WORKSHOP_PDF_OUTPUT_DIR` is set (design D11), and the image has the native PDF libraries, which the order check added in 13.3 needs. Verify with a headless run (`-u 3 -r 3 -t 60s --csv`) against `http://localhost:9090`: it exits 0, every request name appears in the CSV, including a `GET /api/products/` row, `asset:stylesheet` rows are present, no request returned 404, and the CSV has form `POST /checkout` rows without failures. This container run is *the task 13.2 command*, the single form that 13.3 and `drift-coverage` task 15.6 reuse. The pure-Python server `TMP=$(mktemp -d) && WORKSHOP_DATABASE_URL=sqlite+aiosqlite:///$TMP/ws/workshop.db WORKSHOP_PDF_OUTPUT_DIR=$TMP/ws/pdfs uv run python -m tools.entrypoint uvicorn backend.app.main:app --port 9090` stays usable for iterating on the locustfile, but only on a host that has the native PDF libraries (Pango/HarfBuzz): without them `api/docs.py` answers 503 (`reproducible-image` D8 and its task 3.3) while the confirmation page still links `/api/docs/orders/<id>/invoice.pdf`, so the 13.3 order check records `leak:order` and a 5xx.
- [x] 13.3 Extend `loadtest/locustfile.py`:
  - per-user leakage checks with failures named `leak:space`, `leak:stage`, `leak:cart`, `leak:indicator` and `leak:order`, where the expected `locator_stage` is derived from the expected flag map (`v4` if `LOCATOR_V4`, else `v3` if `LOCATOR_V3`, else `v2` if `LOCATOR_V2`, else `v1`), never from the last preset name; the expected cart that `leak:cart` compares against holds only the items added since the user's cart was last emptied server-side — in particular every form `POST /checkout` that returns 2xx (the same successful checkout that yields the order id in 13.2) empties it, because `checkout_submit` calls `CartService.clear_cart()` after creating the order on the very cart key the adds filled — so the user resets its expected items exactly then and leaves them unchanged on a 400 checkout;
  - the order check behind `leak:order` (design D10): once the user has an order and the registry holds an order of another load space, it requests `GET /api/docs/orders/<own latest id>/invoice.pdf` under the name `order-doc:own`, which must answer 200, and `GET /api/docs/orders/<another space's latest id>/invoice.pdf` under the name `order-doc:other`, which must answer 404, both with its own space header; any other status records `leak:order`. `order-doc:own` therefore needs a target with PDF rendering available, which is why the 13.2 command runs against the container: outside the image a 503 counts both as `leak:order` and as a 5xx in the quitting hook. Locust runs as a single process (no `--processes`, no distributed workers), so the registry is shared by all users;
  - `[slow-bug]` request names for `/products` and `GET /api/products/` exactly when `BUG_SLOW_RESPONSE` is true in the expected flag map, chosen before the request is sent;
  - a `quitting` hook that sets a non-zero exit code unless p95 of HTML and API requests without `[slow-bug]` is below 1000 ms, there are zero 5xx responses, zero `leak:` failures and zero failed `asset:` requests.

  Also add `loadtest/sequence_check.py` (new), a scripted single-user class that reuses the expected-map helpers. Verify with three runs:
  - (a) negative control against the last image before spaces (`docker run -p 9090:9090 ghcr.io/manykarim/demo-webshop:0.2.0`, or the recorded `build` digest of the last `main` run before this change) with `-u 5 -t 120s`: it exits non-zero and reports `leak:` failures, including `leak:order` (another space's invoice answers 200 there);
  - (b) the same run against the `demo-webshop:spaces` container from 13.2 (no rebuild is needed, because `loadtest/` is not in the image): it exits 0, the CSV contains `order-doc:own` and `order-doc:other` rows without failures, and requests to `/products` and `GET /api/products/` made after `buggy` appear under `[slow-bug]` names in the CSV, including requests made after a later stage preset. This checks only the naming, because before `drift-coverage` the bug adds no delay; the real delay is checked in 14.4;
  - (c) `loadtest/sequence_check.py` with `-u 1` against the 13.2 container (the local `uv run` server from 13.2 also works here, because with `-u 1` and no checkout the order check never arms) runs `clean` → `stage3` → `buggy` → status (expects `v3`) → `stage2` → `/products` and `GET /api/products/` (both named `[slow-bug]`) → status (expects `v2`) → `clean` → `/products` (named without the suffix): it exits 0 with no `leak:` rows.

## 14. Coolify validation: early candidate and gating capacity test

- [ ] 14.1 **Moved to `workshop-rollout` task 3.3** (not done here; the maintainer archived this change before rollout). After this change merges and the merge commit's `main` run of `image.yml` has passed `publish`, deploy that candidate on Coolify by digest (`ghcr.io/manykarim/demo-webshop@<digest>`, the `build` job's `digest` output from the run's step summary; not `edge` and not `sha-<short>`) following `docs/COOLIFY-RUNBOOK.md`. Verify:
  - `curl https://<coolify-host>/health` returns `{"status":"ok","version":"dev"}` (`reproducible-image` scenario *Image without a version tag*), the running image digest equals the recorded `build` digest, and the `org.opencontainers.image.revision` label (`docker buildx imagetools inspect --format '{{json .Image}}' ghcr.io/manykarim/demo-webshop@<digest>`) equals the merge commit;
  - the Coolify environment sets neither `WORKSHOP_DATABASE_URL` nor `WORKSHOP_PDF_OUTPUT_DIR`;
  - a preset POST without a space returns 401 with the space hint, and with `X-Workshop-Space: smoke-a` returns 200;
  - `https://<coolify-host>/?space=smoke-a` shows `data-workshop-space="smoke-a"`, and `https://<coolify-host>/` without a space shows `data-workshop-space="default"` with the hint;
  - `GET /api/workshop/status` without a space reports `v1` and no active bugs;
  - after an API add-to-cart and `POST /api/checkout/` under `smoke-a`, that order's `/api/docs/orders/{id}/invoice.pdf` returns 200 under `smoke-a`;
  - temporarily removing `WORKSHOP_ADMIN_TOKEN` makes the deployment fail with a log naming the variable, and the token is restored afterwards.
- [ ] 14.2 **Moved to `workshop-rollout` task 3.4** (not done here; the maintainer archived this change before rollout). Run the early load test from outside the Coolify host network against the 14.1 deployment: headless, 40 users (or registered participants × 1.25 once known), at least 10 minutes, with `--csv` and `--html` reports kept outside git and attached to the PR. This run tunes the pool and is not capacity evidence. Verify: the run is recorded in the runbook's capacity record as kind `early` with image reference, commit, digest, users, duration, p95, 5xx count and leak count; tuned `pool_size` and `max_overflow` values, if changed, are committed to `backend/app/core/db.py`, the new commit's candidate is deployed by digest once its `main` run has published and checked as in 14.1, and the run is repeated; any `leak:` failure or 5xx is fixed as a defect before 14.4. A failed p95 here is a warning for 14.4, not a scale-out decision.
- [ ] 14.3 **Moved to `workshop-rollout` task 3.5** (not done here; the maintainer archived this change before rollout). Rehearse the runbook procedures on the candidate deployment and record each outcome in the rehearsal record. Verify:
  - resetting one space with the documented curl returns its status to `v1` with an empty cart;
  - after a redeploy, a runtime order created before is 404, and status for `load-001` shows `v1` with an empty cart;
  - rolling back to `0.2.0` (the tag deployed by `reproducible-image` task 10.4) makes `/health` report `0.2.0`, and a preset POST without a space no longer returns 401;
  - redeploying the candidate by its recorded digest makes `/health` report `dev` with the candidate digest again, and a preset POST without a space returns 401 with the space hint.
- [ ] 14.4 **Moved to `workshop-rollout` task 4.3** (not done here; the maintainer archived this change before rollout). Gating capacity test (T-1 week). Prerequisites: `drift-coverage` and `acceptance-conformance` are merged, and `main` is green. Deploy the candidate of the release-candidate commit (the commit intended for `workshop-<id>`) by digest, taken from its `main` run after `publish` passed, following the runbook, and check `dev`, the digest and the revision label as in 14.1. Verify that `GET /api/workshop/presets` lists `drift_and_bug`, which shows that `drift-coverage` is in the build. Run the load test from outside the host network: headless, 40 users (or registered participants × 1.25), at least 10 minutes, with preset switches including `buggy` and `drift_and_bug`, reports kept. Verify:
  - Locust exits 0: p95 below 1 s excluding `[slow-bug]`, zero 5xx, zero `leak:` failures and zero failed `asset:` requests;
  - in the CSV, the `[slow-bug]` entries for `/products` and `GET /api/products/` have a minimum response time of at least 1000 ms, which shows the planted delay is active and excluded;
  - the capacity record has a `gating` row with the image reference, commit (revision label), digest, `drift_and_bug` present, users, duration, p95, 5xx count and leak count;
  - the reset and redeploy rehearsal from 14.3 passes on this deployment.

  If the single-instance run fails: keep its row; record in the runbook the instance count N, the handle-character split with registered counts per instance, and the URL table; deploy the N instances; re-run Locust against every instance with that instance's registered handles × 1.25 users (while registrations are incomplete, at least the target divided by N, rounded up), all at the same time when the instances share a Coolify host; verify that the recorded split assigns each of `a`-`z` and `0`-`9` to exactly one instance (a short script over the runbook's split table prints each of the 36 characters once, with its instance, and exits non-zero on a missing or duplicate character), that every run exits 0, and record one row per instance. A `leak:` failure or 5xx at any share is a defect to fix, not a reason to scale out further. Any application change merged after this run and before tagging, including pool tuning, requires repeating 14.4 on the new commit.

## 15. Workshop tag deployment

- [ ] 15.1 **Moved to `workshop-rollout` task 5.1** (not done here; the maintainer archived this change before rollout). After `acceptance-conformance` is merged and `workshop-<id>` is tagged, run the runbook's T-1 day checklist: deploy the `workshop-<id>` tag on Coolify, fill in the `<coolify-host>` placeholders in `docs/COOLIFY-RUNBOOK.md`, and compare the build with the gating run. The image's `org.opencontainers.image.revision` must equal the commit recorded in 14.4, or `git diff --stat <tested commit> workshop-<id> -- backend tools Dockerfile pyproject.toml uv.lock` must be empty (digests are not compared, because the version layer differs per tag). Otherwise, re-run the 14.4 load test against the `workshop-<id>` deployment before publishing the URL, record it, and apply the scale-out contingency if it fails. Then smoke-test with two spaces (preset isolation, the indicator, `Space: default` with the hint without a space, and a 401 without a space), redeploy to wipe the state, and hand the URL and the `X-Workshop-Space` convention to the maintainers of the workshop repository's `coolify` profile, including the note that agent configurations open `?space=<handle>` or send the header. Verify that `/health` reports `workshop-<id>`, that the rehearsal record contains the revision comparison or the re-run row, and that every checklist item is ticked.
- [ ] 15.2 **Moved to `workshop-rollout` task 5.2** (not done here; the maintainer archived this change before rollout). On workshop day, run the day-of checklist: a fresh redeploy, a `/health` version check, a status check without a space (`v1`, no active bugs), watching the logs for 5xx, and keeping the token with facilitators only. Verify that the health check returns the `workshop-<id>` version and status without a space reports `v1` before participants start, and that the log review for the day shows no 5xx responses.
- [x] 15.3 After the workshop day checklist in 15.2, archive this change on `main` with `openspec archive workshop-spaces -y` (design Migration Plan step 8). No sibling waits on this archive: `drift-coverage` is archived right after it merges (its task 17.3), `reproducible-image` in its task 10.6 and `acceptance-conformance` in its task 18.5, and those changes state that this change's archive state does not matter. Verify: `openspec/specs/workshop-spaces/spec.md` exists and contains `### Requirement: Space resolution` and `### Requirement: Shared deployment capacity`; `openspec list` no longer shows `workshop-spaces`.
