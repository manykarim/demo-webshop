## 1. uv release, uv project metadata and the PDF loader

- [x] 1.1 Before any code change, choose the uv release to pin in `Dockerfile` (`ghcr.io/astral-sh/uv:<X.Y.Z>`), `.github/workflows/image.yml` (`astral-sh/setup-uv`) and `[tool.uv] required-version`, and record it in the pull request description; the trial lock used uv 0.8.15, and every later task that generates or checks `uv.lock` uses this release. The order-document location (design D3) and the untagged candidate that is tagged only after every required verification (design D7) are maintainer decisions and need no further confirmation. The `workshop-<id>` publishing path is not rehearsed on the canonical package in this change (design D7, Migration Plan step 5); task 10.5 checks the images of the gated workshop tags. The design's open questions (tag mutability, git revision on `/health`, update automation, provenance attestations, untagged candidate cleanup) do not change what this change builds and stay open. Verify that the pull request description names the uv release and that `uv --version` in the environment used for 1.3 prints it.
- [x] 1.2 Migrate `pyproject.toml`. Move the `dev` list from `[project.optional-dependencies]` unchanged to `[dependency-groups] dev`. Set `requires-python = ">=3.12"`, remove the setuptools `[build-system]` table, and add `[tool.uv]` with `package = false` and `required-version = ">=<uv release from 1.1>"`. `[project.dependencies]` stays unpinned. Add `.python-version` (new) containing `3.12`. Verify these points. `uv sync` succeeds in the working tree; before this change it failed there with `Multiple top-level packages discovered in a flat-layout: ['backend', 'openspec']`, because the setuptools backend built the project and its flat-layout auto-discovery saw a second top-level package beside `backend/` — the untracked `openspec/` directory, so a checkout holding only tracked files did not hit the error. `uv run pytest --version` succeeds; the existing unit tests still import `weasyprint` through `order_service.py` and are run in 1.4, once the import is lazy. After `uv sync --no-dev`, `uv run --no-sync python -c "import pytest"` fails with `ModuleNotFoundError`.
- [x] 1.3 Generate and commit `uv.lock` (new) with the pinned uv release (`uv lock`). Verify these points. `uv lock --check` exits 0. The lock is universal: `pyproject.toml` has no `environments` key under `[tool.uv]`, and `grep -n "^supported-markers" uv.lock` returns nothing (uv writes a `[tool.uv] environments` restriction into the lock as `supported-markers`, not as `environments`; `resolution-markers` and `required-markers` can appear in a universal lock and are not checked). `uv export --locked --no-dev --format requirements-txt` lists `weasyprint` and no `pytest`. A short script over `uv.lock` confirms cp312 or abi3 manylinux wheels for both `x86_64` and `aarch64` for pydantic-core, uvloop, httptools, greenlet, cffi, pillow, brotli and zopfli.
- [x] 1.4 In `backend/app/services/pdf_service.py`, remove the module-level `from weasyprint import HTML`. Add a memoized loader that imports `weasyprint` on first use; on `ImportError` or `OSError` it marks rendering unavailable and logs one warning that includes the reason. Add a public `pdf_rendering_available()` and a `PDFRenderingUnavailable` exception, which `PDFService.render_pdf` raises when rendering is unavailable. Make writes atomic (design D8): `_render_pdf_sync` renders into a unique hidden temporary file in the output directory (`.<output_name>.<uuid4 hex>.tmp`) and moves it onto the target with `os.replace`; on any exception it removes the temporary file and re-raises. Verify with `backend/tests/unit/test_pdf_service.py` (new), with `settings.pdf_output_dir` monkeypatched to `tmp_path` and no native libraries needed. With `sys.modules["weasyprint"] = None`, two `render_pdf` calls raise `PDFRenderingUnavailable` and `caplog` records exactly one warning. The same holds when the import raises `OSError("cannot load library 'libpango-1.0-0'")`. With a fake `weasyprint` module whose `write_pdf` raises `ValueError`, the `ValueError` propagates unchanged and the output directory contains neither the target nor any leftover `.tmp` file. With a fake whose `write_pdf` asserts that it writes to a `.tmp` path in the same directory and that the target does not exist yet, then writes `%PDF-` bytes, the target exists with those bytes after `render_pdf` returns and no leftover `.tmp` file remains. With a fake that writes a body in two chunks with a short sleep in between, two threads rendering the same `output_name` leave the target with exactly one complete body and no leftover `.tmp` file. Because the temporary file is hidden, the test enumerates leftovers in a dotfile-aware way, for example `list(tmp_path.glob('*.tmp'))` or a suffix filter over `os.listdir(tmp_path)`, never `glob.glob(f'{tmp_path}/*.tmp')`, which skips names starting with a dot and would make all three assertions pass vacuously. `grep -rnE "^(from|import) weasyprint" backend tools` returns nothing. `uv run pytest backend/tests/unit` passes, including `test_order_service.py`, also on a host without the Pango and HarfBuzz libraries (5.2 proves the running app without them).

## 2. Shared test harness and version reporting

- [x] 2.1 Create the shared test harness of design D11, whose fixtures `workshop-spaces`, `drift-coverage` and `acceptance-conformance` reuse by their exact names instead of creating their own. `backend/tests/harness.py` (new) is a plain module with no fixtures and no top-level import from `backend.app`. Its context manager `isolated_app(tmp_dir, *, seed_users=False)` imports the app inside the function. Through `pytest.MonkeyPatch.context()`, it points `settings.database_url` to a SQLite file in `tmp_dir` and `settings.pdf_output_dir` to `tmp_dir/pdfs`, and resets the `async_engine` and `async_session_factory` globals in `backend/app/core/db.py`. It clears the flag cache `_cache` in `backend/app/core/feature_flags.py` on enter and on exit, while that module has one. In one `asyncio.run` it runs `init_db()`, `seed_products` and `seed_feature_flags` (or all of `seed_data.main()` when `seed_users=True`, used from 4.2) and disposes the engine. It yields a Starlette `TestClient` entered as a context manager so the lifespan runs, and on exit clears `app.dependency_overrides`, disposes the engine and restores every patched value. The module also declares `SCRUBBED_ENV_VARS = ("WORKSHOP_APP_VERSION",)`, `SCRUBBED_ENV_PREFIXES = ("WORKSHOP_FLAG_",)`, `PINNED_ENV_VARS = {}` and `pin_test_environment(environ, tmp_dir)`, which removes the scrubbed variables from `environ`, sets the pinned ones, and sets `WORKSHOP_DATABASE_URL` to a SQLite file in `tmp_dir` and `WORKSHOP_PDF_OUTPUT_DIR` to `tmp_dir/pdfs`. The root `backend/tests/conftest.py` (new) calls `pin_test_environment(os.environ, <tempfile.mkdtemp() directory>)` at module top, before any app import, and `pytest_unconfigure` removes that directory. It imports `backend.app` only inside fixture bodies. It provides the function-scoped fixtures `temp_database` (settings and engine globals only, no client, no seeding; yields the database file path as a `pathlib.Path`) and `app_client` (`isolated_app(tmp_path)`); users are not seeded, so no PDF rendering is involved. Tasks 3.1 and 4.2 add `pdf_unavailable`, `fake_weasyprint` and `seeded_app_client` to the same file. Verify with `backend/tests/unit/test_harness_environment.py` (new): on a plain dict holding `WORKSHOP_APP_VERSION`, `WORKSHOP_FLAG_LOCATOR_V2` and `WORKSHOP_AI_PROVIDER`, `pin_test_environment` removes the first two, keeps the third and sets both paths inside `tmp_dir`; with `PINNED_ENV_VARS` monkeypatched to `{"WORKSHOP_EXAMPLE": "x"}` it also sets that variable. Verify with `backend/tests/integration/test_harness.py` (new): `GET /health` returns 200, `GET /api/products/` returns 12 items, `temp_database` yields a path inside `tmp_path`, and two tests use different database files. One test enters `isolated_app` twice in a row with different directories: in the first, `PUT /api/admin/flags/NEW_CART_UI` with `{"enabled": true}` makes `GET /api/admin/flags` report it true; in the second, entered immediately afterwards and well within `feature_flag_cache_seconds`, it reports `NEW_CART_UI` false. After a test that used `app_client`, `settings.database_url` and `settings.pdf_output_dir` are back to their previous values, which point into the conftest's temporary directory and not into the repository. Finally, `touch "$marker"`, run `uv run pytest backend/tests`, and verify that `find . \( -path ./.venv -o -path ./.git \) -prune -o \( -name '*.db' -o -name '*_ORD-*.pdf' \) -newer "$marker" -print` prints nothing (plain `git status` cannot see these gitignored files).
- [x] 2.2 Add `app_version: str = "dev"` (`WORKSHOP_APP_VERSION`) to `Settings` in `backend/app/core/config.py`. `/health` in `backend/app/main.py` returns `{"status": "ok", "version": settings.app_version}` and reads the setting on every request. `FastAPI(version=...)` uses the same setting. `WorkshopStatus` in `backend/app/api/workshop.py` gains `version`. Verify with `backend/tests/integration/test_version.py` (new). With default settings, `/health` equals exactly `{"status": "ok", "version": "dev"}` and `/api/workshop/status` has `version` `dev`. With `settings.app_version` monkeypatched to `workshop-2026-10`, both endpoints report `workshop-2026-10`. A subprocess `python -c "from backend.app.main import app; print(app.version)"` prints `workshop-2026-10` with `WORKSHOP_APP_VERSION=workshop-2026-10` and `dev` without it. Also verify the scenario *Local development run* by hand: after `uv run uvicorn backend.app.main:app --port 9090` from the source checkout with `WORKSHOP_APP_VERSION` unset, `curl -s localhost:9090/health` returns `{"status":"ok","version":"dev"}`; repeat after creating a local, never-pushed tag on HEAD (`git tag v0.0.0-versioncheck`, deleted afterwards with `git tag -d`), which still reports `dev`.

## 3. PDF rendering boundaries

- [x] 3.1 In `backend/app/services/order_service.py`, add a module-level `build_document_context(order: Order) -> dict` (design D8). It builds the context from a persisted `Order` whose `items` are loaded: `order` (`order_number`, `order_date` as `created_at` formatted `%Y-%m-%d`, `subtotal`, `tax`, `total`, `status`), `customer` (`name`, `email`, `address` from the order's customer fields) and `items` (`product_id`, `name`, `quantity`, `unit_price`, `total_price`, and `image_url` as `""`, which keeps today's empty `src` in `order_summary.html`). `OrderService.generate_documents(order)` uses it for both the invoice and the summary and no longer takes the cart state. Add two fixtures to `backend/tests/conftest.py`: `pdf_unavailable` clears the loader memo and sets `sys.modules["weasyprint"] = None`; `fake_weasyprint` clears the memo and installs a fake module whose `HTML(string=...)` records each HTML string and whose `write_pdf` writes `%PDF-` bytes, and it yields the list of recorded HTML strings in render order. Both restore the previous state afterwards. Verify with `backend/tests/unit/test_document_context.py` (new): for a transient `Order` with `created_at` set and two `OrderItem`s, rendering `pdf/invoice.html` and `pdf/order_summary.html` from `backend/app/templates` in a Jinja `Environment(undefined=StrictUndefined)` with `build_document_context(order)` succeeds, and both outputs contain the customer name, the order date and the tax formatted with two decimals. `grep -n "cart_state" backend/app/services/order_service.py` shows no use inside `generate_documents`.
- [x] 3.2 In `backend/app/services/order_service.py`, extract a module-level `build_order(...)` beside `build_document_context`, which returns an unsaved `Order` with its items from the customer details and cart state and does not add or commit it. It takes no session and no `PDFService`, which is what lets seeding build its order history without constructing an `OrderService` (task 4.2): `OrderService.__init__` requires a `pdf_service` today and keeps requiring one. `OrderService.create_order` uses it, still commits before rendering documents, and catches only `PDFRenderingUnavailable` from `generate_documents`; in that case it returns an empty document list. Verify with `backend/tests/integration/test_pdf_degradation.py` (new), using `app_client` and `pdf_unavailable`. After `POST /api/cart/items` with `X-Session-ID: smoke-a`, `POST /api/checkout/` returns 200 with `status` `success`, an `ORD-` order number and `documents == []`, and the order row exists. The form `POST /checkout` with header `x-session-id` returns 200, and the page contains `Order ORD-` and `confirmed`. With `PDFService.render_pdf` patched to raise `RuntimeError`, the API checkout raises in the `TestClient`, so other rendering errors are not hidden. `grep -nE "^def build_order\(" backend/app/services/order_service.py` matches, so the helper sits at module level. `backend/tests/unit/test_order_service.py` still passes.
- [x] 3.3 In `backend/app/api/docs.py`, look up the order first, so a missing order still returns 404. If `pdf_rendering_available()` is false, the invoice and summary endpoints answer `503` with `{"detail": "PDF rendering is unavailable in this environment"}`, even when a stale file exists. Otherwise they return the existing file, or render the missing document with `build_document_context(order)` for both the invoice and the summary; delete the hand-built context dicts and `_build_item_payload`. Verify in `backend/tests/integration/test_pdf_degradation.py`. With `pdf_unavailable`: `invoice.pdf` and `summary.pdf` of a created order return 503 with exactly that body; order id 99999 returns 404 `Order not found`; a stale `invoice_<order_number>.pdf` in the temporary output directory still yields 503. With `fake_weasyprint`: `invoice.pdf` of a checkout order returns 200 with `content-type: application/pdf`. For an order created with `build_order` plus a commit and no rendering, as seeding does after 4.2, `summary.pdf` and `invoice.pdf` both return 200 with `application/pdf` and a `%PDF` body, and the recorded summary HTML contains the customer name, the order date and the formatted tax. For an order created through `POST /api/checkout/`, whose two files are then deleted from the temporary `pdf_output_dir`, the HTML recorded for each document on demand equals the HTML recorded for it at checkout. `grep -n "_build_item_payload" backend/app/api/docs.py` returns nothing.

## 4. Idempotent seeding

- [x] 4.1 In `backend/app/seeds/seed_data.py`, change `seed_products` to insert only missing SKUs and never update existing rows. `seed_feature_flags` stays insert-missing. The `ensure_product_columns` and `ensure_order_columns` helpers still run on every `main()`. Verify with `backend/tests/integration/test_seed_idempotency.py` (new), using `temp_database` and `seed_data.main()`. A fresh file gets 12 products. After one product's price and one flag's `enabled` value are changed with SQL, a second `main()` keeps both changes and still counts 12 products. A database whose `products` table was created with raw DDL without `rating` and `review_count` has both columns after `main()`.
- [x] 4.2 Change `seed_users` to insert only missing emails. For a new user, addresses, payment methods and the seeded order history (with each fixture order's status) are created with the module-level `build_order` of task 3.2 and `session.add`, then committed once per user. Seeding never calls `create_order`, never renders PDFs and constructs neither an `OrderService` nor a `PDFService` (an `OrderService` cannot be built without one). Seeding treats `USERS_FIXTURES`, `PRODUCT_FIXTURES` and the flag fixtures as read-only (design D4): read `billing_address_index` with `payment_data.get("billing_address_index")` and build the `PaymentMethod` arguments from a filtered copy, `{k: v for k, v in payment_data.items() if k != "billing_address_index"}`. Do not combine `.get` with `**payment_data`, which would pass the extra key to `PaymentMethod` and raise `TypeError`, and never `pop`, `del` or assign items on fixture data. Add the `seeded_app_client` fixture (`isolated_app(tmp_path, seed_users=True)`: products, flags and the demo users `jamie@flowlinesupply.com` and `alex.productlead@example.com` with their order history) to `backend/tests/conftest.py`; it is the fixture later changes use wherever seeded users or orders are needed. Verify in `backend/tests/integration/test_seed_idempotency.py`:
  - After two `main()` runs, the counts of users, addresses, payment methods, orders and order items are equal, and seeded order numbers are identical. A runtime order with `user_id` NULL inserted between the runs survives. No `order_items` row references a missing order. The temporary `pdf_output_dir` is empty. `grep -nE "PDFService|OrderService" backend/app/seeds/seed_data.py` returns nothing.
  - With `build_order` patched to raise for the second fixture user, `main()` raises: the first user is complete and the second has no rows. A following unpatched `main()` completes the second user without changing the first user's order numbers.
  - Fixtures stay unchanged: a `copy.deepcopy(seed_data.USERS_FIXTURES)` taken before the first `main()` equals `seed_data.USERS_FIXTURES` after the second `main()`, and again after the patched failing run and its retry.
  - Billing links survive a retry: after the unpatched retry, a stdlib `sqlite3` query shows that every payment method of both fixture users has a non-null `billing_address_id` equal to the id of that user's address at the fixture's `billing_address_index` (addresses ordered by id): Jamie's Visa 4242 links to Home, Amex 3782 to Studio, and Alex's Mastercard 5454 to Primary.
  - Billing links survive an earlier in-process seed: one test seeds two separate fresh database files one after the other, and both show the same links, so test order cannot hide a regression.
  - Lazy seeded documents: with `seeded_app_client` and `fake_weasyprint`, `POST /api/auth/login` as `jamie@flowlinesupply.com` / `demo123` returns two orders, the temporary PDF directory is still empty, and every order's `invoice_url` and `summary_url` then returns 200 with `application/pdf` and a `%PDF` body.
- [x] 4.3 Check the seed CLI `tools/seed_db.py` against a real file. Run `uv run python -m tools.seed_db` twice with `WORKSHOP_DATABASE_URL` and `WORKSHOP_PDF_OUTPUT_DIR` pointing to a temporary directory. Verify with a stdlib `sqlite3` query that the counts of products, feature flags, users, orders and order items are identical after both runs, that no payment method has a NULL `billing_address_id`, and that the second run exits 0.

## 5. Container entrypoint

- [x] 5.1 Create `tools/entrypoint.py` (new), which runs as `python -m tools.entrypoint <command...>`. It configures INFO logging and derives the database directory from `settings.database_url` (relative or absolute SQLite paths; other URLs skip the check). It creates that directory and `settings.pdf_output_dir` if they are missing. If the current user cannot write them, it exits non-zero with a message naming the directory, the effective database URL, the uid and the fix (`chown -R 1000:1000 <dir>` for bind mounts, or `--user`). It runs `seed_data.main()` and `shutdown_db()` in one `asyncio.run`, exits non-zero if seeding fails, exits non-zero with a usage message when no command is given, and otherwise calls `os.execvp` with the given command. Verify with `backend/tests/unit/test_entrypoint.py` (new), using `temp_database` from `backend/tests/conftest.py`. An unwritable temporary directory (mode `0o500`; skipped when running as root) leads to a non-zero exit and stderr naming the path, the database URL and `chown`. On the happy path with a temporary database and a mocked `os.execvp`, 12 products are seeded and `execvp` receives exactly the given command. When seeding raises, the exit is non-zero and `execvp` is not called. With no arguments, the exit is non-zero with the usage text.
- [x] 5.2 Verify graceful PDF degradation in a real environment without native libraries. Start a `python:3.12-slim-trixie` container without the Pango and HarfBuzz packages. Mount the source read-only at `/src` and use it as the working directory (`-v "$PWD":/src:ro -w /src`), install the pinned uv, and set `UV_PROJECT_ENVIRONMENT=/tmp/venv`, `WORKSHOP_DATABASE_URL=sqlite+aiosqlite:////tmp/w.db` and `WORKSHOP_PDF_OUTPUT_DIR=/tmp/pdfs`. Run `uv sync --locked --no-dev`, then put that environment on `PATH` the way the image does (design D5, task 6.2): `export PATH=/tmp/venv/bin:$PATH` inside the container shell, not through `docker run -e`, where the host would expand `$PATH`. Confirm that `command -v python uvicorn` resolves both to `/tmp/venv/bin`, then run `python -m tools.entrypoint uvicorn backend.app.main:app --host 0.0.0.0 --port 9090` with the port published. Without that `PATH`, `python` is the system interpreter without FastAPI and the entrypoint's `os.execvp` cannot find `uvicorn`; the equivalent alternative is `uv run --no-sync python -m tools.entrypoint uvicorn backend.app.main:app --host 0.0.0.0 --port 9090`, which also puts the environment's `bin` on `PATH` for the process that `execvp` replaces. Verify from the host: `/` and `/health` return 200; an API add-to-cart followed by `POST /api/checkout/` returns `success` with `documents == []`, which is the scenario *Checkout without PDF libraries*; that order's `invoice.pdf` returns 503 with `PDF rendering is unavailable in this environment`, which is the scenario *Invoice request without PDF libraries*; and the log contains the unavailable warning exactly once.

## 6. Build context and Dockerfile

- [x] 6.1 Add `.dockerignore` (new) excluding `.git`, `.github/`, `.venv`, `**/.env`, `**/*.db`, `openspec/`, `docs/`, `backend/tests/`, `.claude/`, `**/__pycache__`, `backend/app/static/pdfs/invoice_ORD-*.pdf` and `backend/app/static/pdfs/summary_ORD-*.pdf`. `.github/` is added here because this change creates it and the image never copies it, which is the rule design D9 states for every later change that adds a root directory. Add `workshop.db` and the two `*_ORD-*.pdf` patterns to `.gitignore`, and remove the 36 generated `invoice_ORD-*`/`summary_ORD-*` PDFs from version control. Verify that `git ls-files backend/app/static/pdfs` lists only `example_invoice.pdf` and `example_order_summary.pdf`, and that `git check-ignore -v workshop.db backend/app/static/pdfs/invoice_ORD-TEST1234.pdf` matches both paths.
- [x] 6.2 Rewrite the root `Dockerfile` as a two-stage build per design D1, D3, D5 and D6. Both stages use `python:3.12-slim-trixie@sha256:<digest>`. The builder copies `/uv` from `ghcr.io/astral-sh/uv:<pinned>`, sets `UV_PYTHON_DOWNLOADS=never`, `UV_COMPILE_BYTECODE=1`, `UV_LINK_MODE=copy` and `UV_PROJECT_ENVIRONMENT=/app/.venv`, copies only `pyproject.toml` and `uv.lock`, and runs `uv sync --locked --no-dev` with a cache mount. The runtime stage installs `libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz0b libharfbuzz-subset0 fonts-dejavu-core curl` with `--no-install-recommends` and removes the apt lists. It creates uid/gid 1000 and `/data` owned by 1000, copies `/app/.venv`, and copies `backend/` and `tools/` owned by root. It sets `PATH`, `PYTHONUNBUFFERED=1`, `WORKDIR /app`, `WORKSHOP_DATABASE_URL=sqlite+aiosqlite:////data/workshop.db` and `WORKSHOP_PDF_OUTPUT_DIR=/data/pdfs`, then `USER 1000` and `EXPOSE 9090`. It adds `HEALTHCHECK --interval=10s --timeout=3s --start-period=30s --retries=3 CMD curl -fsS http://127.0.0.1:9090/health || exit 1`, `ENTRYPOINT ["python", "-m", "tools.entrypoint"]`, and `CMD ["uvicorn", "backend.app.main:app", "--host", "0.0.0.0", "--port", "9090", "--proxy-headers", "--forwarded-allow-ips", "*"]` without `--workers`. `ARG APP_VERSION=dev` and `ENV WORKSHOP_APP_VERSION=$APP_VERSION` come last. There is no `VOLUME` and no build-time seeding. Verify that `docker build -t demo-webshop:local .` succeeds and that `docker image inspect demo-webshop:local` shows only `9090/tcp` exposed, no volumes, and the entrypoint, command, health check and three `WORKSHOP_*` values above. `docker run --rm --entrypoint id demo-webshop:local` prints `uid=1000`. `docker run --rm --entrypoint sh demo-webshop:local -c 'command -v uv'` exits non-zero. `docker run --rm --entrypoint python demo-webshop:local -c "import sys, weasyprint; print(sys.version_info[:2])"` prints `(3, 12)`. `/data` is owned by 1000, `/app/backend` is owned by root, and `touch /app/backend/x` fails. A whitespace-only change in `backend/app/main.py` rebuilds with the `uv sync` step reported as `CACHED`.
- [x] 6.3 Verify the build context hygiene of D9 with probes where the runtime `COPY` would pick them up. Create these untracked files: `backend/app/.env` and `tools/.env`, each containing `WORKSHOP_AI_API_KEY=leak-test`; `backend/workshop.db`; and `backend/app/static/pdfs/invoice_ORD-LEAKTEST.pdf`, so the PDF check does not depend on leftover generated files. Rebuild `demo-webshop:local`. Verify with `docker run --rm --entrypoint sh demo-webshop:local -c 'ls -A /app; ls /app/backend/app/static/pdfs; find /app/backend /app/tools \( -name .env -o -name "*.db" \); ls -d /app/backend/tests; grep -rl leak-test /app/backend /app/tools || true'` that `/app` lists only `.venv`, `backend` and `tools`, the PDF directory lists only the two `example_*.pdf` files, `find` prints nothing, `/app/backend/tests` does not exist, and `leak-test` is found nowhere. Negative control: with `**/.env` temporarily removed from `.dockerignore`, the same build under a throwaway tag (`-t demo-webshop:negctl`) contains `/app/backend/app/.env`, which shows the check depends on the exclusions; restore the pattern and delete that image afterwards, so `demo-webshop:local`, which section 7 runs, stays the build made with the complete exclusion list. Optionally, confirm the context itself with `printf 'FROM busybox\nCOPY . /ctx\nRUN find /ctx -maxdepth 3 | sort\n' | docker build --progress=plain --no-cache -f - .` while a root `.env` and `workshop.db` exist: neither `/ctx/.env`, `/ctx/workshop.db`, `/ctx/.git` nor `/ctx/.venv` is listed. Remove all probe files afterwards.
- [x] 6.4 Delete `backend/Dockerfile` and `requirements.txt`. In `.env.example`, change the CORS origins from port 8000 to 9090, and comment out `WORKSHOP_DATABASE_URL` with a one-line note that it applies to source runs only, where `./workshop.db` is already the default, and that the image and compose always use `/data/workshop.db` (design D10). Verify these points. `git ls-files backend/Dockerfile requirements.txt` returns nothing. `grep -n "^WORKSHOP_DATABASE_URL" .env.example` returns nothing. `git grep -nI --untracked -e 'requirements\.txt' -e 'backend/Dockerfile' -e '8000' -- . ':!openspec' ':!.claude' ':!uv.lock' ':!README.md' ':!docs'` returns nothing (exit code 1). `uv.lock` is excluded because its hashes and URLs can contain `8000` by chance; `git grep` skips ignored files such as caches and a local `workshop.db`; the bare `8000` pattern still catches forms like `EXPOSE 8000` and `"--port", "8000"`; `README.md` is rewritten in 8.2.
- [x] 6.5 Verify the requirement *Locked dependency set* on the built image. Scenario *Build with a current lockfile*: every distribution installed in `/app/.venv`, listed by `importlib.metadata` in the container, appears with the same version in `uv.lock`. Every entry of `uv export --locked --no-dev --no-hashes --no-emit-project --format requirements-txt` whose marker applies to Linux is installed. Scenario *Build with an outdated lockfile*: in a temporary copy of the repository, add a dependency (for example `rich`) to `[project.dependencies]` without running `uv lock`. Verify that `docker build .` fails at the `uv sync --locked` step with ``The lockfile at `uv.lock` needs to be updated, but `--locked` was provided.``, and that `uv lock --check` in that copy exits non-zero.

## 7. Smoke script and local container verification

- [x] 7.1 Create `tools/smoke_image.py` (new), which uses only the standard library. It takes `--base-url` (default `http://localhost:9090`), `--expect-version` (default `dev`), `--phase fresh|restarted|recreated` and `--state <file>`, whose default is one fixed path under the system temporary directory (`<tempfile.gettempdir()>/demo-webshop-smoke-state.json`), so the three phases of one container, run as separate invocations, share a single state file without any caller naming it. The default must not point into the repository, where it would become an untracked file in the build context that 6.3 and D9 probe. Every invocation in this change relies on these defaults and passes only `--phase`, plus `--expect-version` where the expected version is not `dev` (7.4, 9.4). In phase `fresh`, it checks that `/` returns 200 and contains `Flowline Supply`; that `/health` equals `{"status": "ok", "version": <expected>}` and `/api/workshop/status` has that `version`; that `/api/products/` returns 12 items; and that `POST /api/auth/login` as `jamie@flowlinesupply.com` / `demo123` returns a non-empty `orders` list. For every order in that list, it fetches `invoice_url` and `summary_url` and checks 200, `application/pdf` and a `%PDF` body, which covers lazy rendering of seeded documents. It then adds to cart with a random `X-Session-ID`, runs `POST /api/checkout/`, checks that the runtime order's `invoice.pdf` and `summary.pdf` return 200 with `application/pdf` and a `%PDF` body, and checks that `/static/pdfs/invoice_<order_number>.pdf` for that order returns 404 after the document was rendered. Finally it writes the runtime order id, the seeded order numbers, the first seeded order's `summary_url` and the `--base-url` it ran against to the state file, overwriting it, so each container's `fresh` run replaces the previous one's state. Phase `restarted` checks for 12 products, unchanged seeded order numbers, a 200 for the runtime order's `invoice.pdf`, and a 200 `application/pdf` with a `%PDF` body for the first seeded order's `summary.pdf`. Phase `recreated` checks for 12 products, a working demo login, and a 404 for the runtime order's `invoice.pdf`. Phases `restarted` and `recreated` read the state file and exit non-zero with a named check when it is missing, unreadable, or was written for a different `--base-url`, so a stale or absent state file fails the run instead of silently skipping the runtime-order assertions (without that guard, `recreated`'s 404 check would pass for the wrong reason). Each failure exits non-zero with the failing check's name. Rebuild `demo-webshop:local` (`docker build -t demo-webshop:local .`) before the container checks of this section: task 6.4 deleted `backend/Dockerfile`, which sits inside the `backend/` tree the runtime stage copies, after the builds of 6.2 and 6.3. Verify that `grep -nE "^(import|from) " tools/smoke_image.py` shows only standard-library modules, that phase `fresh` passes against `docker run -d --name shop -p 9090:9090 demo-webshop:local` once it is healthy, that the script exits non-zero with a readable message when run with `--base-url http://localhost:9099`, where nothing listens, and that after a successful `fresh` run, deleting the state file makes `--phase restarted` exit non-zero naming the missing state. Remove that container (`docker rm -f shop`) at the end of this task, so 7.2 can start a container with the same name and host port.
- [x] 7.2 Verify the scenario *Default start* and the health check locally. Start with `docker run -d --name shop -p 9090:9090 demo-webshop:local`. Verify these points. `docker inspect --format '{{.State.Health.Status}}' shop` reaches `healthy` within 40 seconds. `/` and `/health` answer on port 9090. `docker logs shop` shows `Seeding completed` before `Uvicorn running on http://0.0.0.0:9090`. `docker exec shop cat /proc/1/cmdline` shows uvicorn. `curl -s -H 'Host: shop.example' -H 'X-Forwarded-Proto: https' localhost:9090/` contains `https://shop.example/products/`. Graceful stop: `time docker stop -t 30 shop` returns in under 5 seconds, far below the 30-second kill timeout, so an ignored SIGTERM would show up as a 30-second wait. `docker inspect --format '{{.State.ExitCode}}' shop` then prints `0`, not `137` (SIGKILL). `docker logs shop` ends with uvicorn's `Application shutdown complete` and `Finished server process` lines, which is the check that does not depend on how the process is started (under `--init` the exit code would be 143). Negative check: `docker run -d --name no-http demo-webshop:local python -m http.server 9999` is seeded but serves nothing on 9090, and it reaches `unhealthy` after the start period and retries. Remove both containers (`docker rm -f shop no-http`) before 7.3.
- [x] 7.3 Verify the requirements *Seed on first start* and *Order documents in the container* (scenarios *PDFs inside the container* and *No static URL for order documents*) locally. On a new container, `tools/smoke_image.py --phase fresh` passes, including its `/static/pdfs/` 404 check, and `docker exec shop ls -A /data/pdfs` lists the runtime order's `invoice_ORD-*.pdf` and the seeded orders' documents fetched by the smoke script and no entry whose name ends in `.tmp`, and `docker exec shop find /data/pdfs -name '*.tmp' -print` prints nothing; `-A` and `find` are required because the atomic-write temporaries of 1.4 and D8 are hidden `.<output_name>.<uuid4 hex>.tmp` files, which plain `ls` would never show (both commands are already proven available in this image by 6.3). `docker exec shop ls /app/backend/app/static/pdfs` lists only the two `example_*.pdf` files, which are not hidden. After `docker restart shop` and waiting for `healthy`, `--phase restarted` passes. It reads the default state file written by this container's `fresh` run, so the order number it re-checks and the `invoice.pdf` it fetches are the ones created in that run, and `--phase recreated` below reads the same file, which is what lets it expect a 404 for that order's `invoice.pdf`. After `docker rm -f shop` and a new `docker run` from the same image, `--phase recreated` passes, and `docker inspect --format '{{json .Mounts}}' shop` prints `[]`. With an overridden command `docker run -d -p 9091:9091 demo-webshop:local uvicorn backend.app.main:app --host 0.0.0.0 --port 9091`, `/api/products/` on 9091 returns 12 items. `docker run --rm --mount type=tmpfs,destination=/data,tmpfs-mode=0755 demo-webshop:local` exits non-zero before uvicorn starts, with the `chown` hint and the database URL in the output. A named volume (`-v shopdata:/data`) starts `healthy`.
- [x] 7.4 Verify version reporting from a built image. Run `docker build --build-arg APP_VERSION=workshop-2026-10 -t demo-webshop:ws .` with the default build of 7.1 still current, so the two builds differ only in the build argument; every step before the final `ARG`/`ENV` is reported as `CACHED`. A container from `demo-webshop:ws`, started with `-p 9090:9090` once the 7.3 containers are removed so that the smoke script's default base URL applies, answers `/health` with status 200 and exactly `{"status":"ok","version":"workshop-2026-10"}`, and `/api/workshop/status` reports the same `version`. `tools/smoke_image.py --phase fresh --expect-version workshop-2026-10` passes. A container from `demo-webshop:local` reports `"version":"dev"`.

## 8. Compose file and documentation

- [x] 8.1 Add `compose.yaml` (new) with one service: `build: .` with the `APP_VERSION` build argument defaulting to `dev`, `ports: ["9090:9090"]`, `env_file` `.env` with `required: false`, an `environment:` block with `WORKSHOP_DATABASE_URL: sqlite+aiosqlite:////data/workshop.db` and `WORKSHOP_PDF_OUTPUT_DIR: /data/pdfs` (design D10; `environment` takes precedence over `env_file`), and no volumes and no health check override. Verify these points without a `.env` file first. `docker compose config` succeeds. `docker compose up -d --build --wait` exits 0. `curl -s localhost:9090/health` reports `dev`. `tools/smoke_image.py --phase fresh` passes. After `docker compose up -d --force-recreate --wait`, `--phase recreated` passes, so no anonymous volume carries data over. `docker compose down` removes the container. Then verify with a real `.env`: run `cp .env.example .env` and append the old line `WORKSHOP_DATABASE_URL=sqlite+aiosqlite:///./workshop.db` to simulate a `.env` from before this change. `docker compose config` shows `WORKSHOP_DATABASE_URL: sqlite+aiosqlite:////data/workshop.db` for the service. `docker compose up -d --build --wait` exits 0 and `tools/smoke_image.py --phase fresh` passes. `docker compose exec <service> ls /data/workshop.db` succeeds, and `docker compose exec <service> ls /app` lists no `workshop.db`. Finally run `docker compose down` and delete the temporary `.env`.
- [x] 8.2 Rewrite `README.md` per design D10. Remove the references to `tests/robot`, `tools/mcp_generate.py`, robotframework-selfhealing-agents, doctestlibrary, robotframework-aiagent and `rfbrowser init`. Document the uv workflow (`uv sync`; `uv run python -m tools.seed_db`, which no longer refreshes an existing database, so delete `workshop.db` to reset; `uv run uvicorn backend.app.main:app --reload --port 9090`; `uv run pytest backend/tests`). Document image usage (`docker run -p 9090:9090 ghcr.io/manykarim/demo-webshop:<tag>`; a restart keeps data, recreation resets it; bind mounts must be owned by uid 1000, or use `--user`), `docker compose up --build` (`.env` configures AI and CORS settings, while the database and PDF locations are fixed to `/data` in the container), the version check via `/health` (`edge` and other images built from a branch report `dev`), the tag scheme (`sha-<short-sha>` as a moving per-commit pointer, `X.Y.Z`, `workshop-<id>`, `edge`, no `latest`; pin a version tag or digest) and rollback by deploying the previous version tag or a recorded digest. Document PDFs: generated files land in `WORKSHOP_PDF_OUTPUT_DIR` (`backend/app/static/pdfs` for local runs, `/data/pdfs` inside the image, which is what the checkout `documents` field reports, or `[]` when rendering is unavailable), they are downloaded through `/api/docs/orders/{id}/invoice.pdf` and `/summary.pdf`, not `/static/pdfs/`, and outside the container the PDF endpoints answer 503 when the native libraries are missing. Add a breaking-change note for former `pip install -r requirements.txt` users that points to `uv export --no-dev --format requirements-txt`, and link `docs/WORKSHOP-FEATURES.md`. Verify these points. `grep -nE "tests/robot|mcp_generate|selfhealing|doctestlibrary|aiagent|rfbrowser|8000" README.md` returns nothing. Repository-wide, `git grep -nIE --untracked "tests/robot|mcp_generate|selfhealing|doctestlibrary|aiagent|rfbrowser" -- . ':!openspec' ':!.claude'` matches only under `docs/user-stories/` and the unused `visual_baseline_dir` default in `backend/app/core/config.py`, which stays because pydantic-settings rejects unknown `WORKSHOP_*` entries in existing `.env` files. `requirements.txt` appears only in the breaking-change note. `grep -n "static/pdfs" README.md` shows only the local-run sentence. Every command in the development and compose sections was run successfully once, and every image-section command (plain run, restart vs. recreate, a bind mount owned by uid 1000, a named volume, `--user`) was run once with `demo-webshop:local` in place of `ghcr.io/manykarim/demo-webshop:<tag>`; the published reference is verified in 10.2.
- [x] 8.3 Update `docs/test-prompts/checkout-and-auth.md` line 32. PDF artifacts are asserted through `/api/docs/orders/{id}/invoice.pdf` and `/summary.pdf` instead of files under `backend/app/static/pdfs/` (D3). Leave `docs/user-stories/` to `acceptance-conformance` and say so in the pull request description, naming `checkout-and-orders.md` line 30 (`/static/pdfs/`) and the stale Robot Framework and tooling references in `ai-and-operations.md` (lines 35, 39 and 41) and `checkout-and-orders.md` (line 31), which stay in the legacy files. Verify that `grep -rn "static/pdfs" docs` returns only the user-story line and that the pull request description contains the hand-off.

## 9. GitHub Actions workflow

- [x] 9.1 Create `tools/resolve_image_version.py` (new), which uses only the standard library. It reads `GITHUB_REF` and prints `app_version=<value>`, also appending that line to `$GITHUB_OUTPUT` when the variable is set. The mapping is `refs/tags/workshop-<id>` → `workshop-<id>`, `refs/tags/vX.Y.Z` → `X.Y.Z`, anything else → `dev`. It exits non-zero when a `workshop-` tag has characters outside `[A-Za-z0-9_.-]` or is longer than 128 characters. Verify with `backend/tests/unit/test_resolve_image_version.py` (new) that `refs/tags/workshop-2026-10`, `refs/tags/v0.2.0`, `refs/heads/main`, `refs/heads/feature-x` and `refs/pull/7/merge` map to `workshop-2026-10`, `0.2.0`, `dev`, `dev` and `dev`, and that `refs/tags/workshop-bad+id` and a 129-character workshop tag exit non-zero with a message.
- [ ] 9.2 **Moved to `workshop-rollout` task 1.1** (not done here; the maintainer archived this change before rollout). Create `.github/workflows/image.yml` (new) with its triggers (push to `main`; tags `v[0-9]+.[0-9]+.[0-9]+` and `workshop-*`; `pull_request`; `workflow_dispatch`), permissions `contents: read` and `packages: write`, concurrency grouped per ref with `cancel-in-progress` only for pull requests, and every action pinned by a full commit SHA with a version comment. Add the `check` job: checkout, `astral-sh/setup-uv` with the pinned uv release, `uv lock --check`, `uv sync --locked` and `uv run pytest backend/tests`. Verify that actionlint reports no findings, that `grep -n "uses:" .github/workflows/image.yml | grep -vE "@[0-9a-f]{40} # v"` returns nothing, and that the `check` job is green on the pull request.
- [ ] 9.3 **Moved to `workshop-rollout` task 1.2** (not done here; the maintainer archived this change before rollout). Add the `build` job, which needs `check` (design D7). It runs `tools/resolve_image_version.py` and `docker/metadata-action` with `type=sha,prefix=sha-,format=short`, `type=semver,pattern={{version}}`, `type=match,pattern=^workshop-.*$,group=0`, `type=edge,branch=main` and `flavor: latest=false`. It declares exactly these job outputs, with these fixed names: `digest` (set below), which `smoke`, `publish` and the later `test` (`drift-coverage`) and `conformance` (`acceptance-conformance`) jobs read to address the candidate; `app_version` (the resolved `APP_VERSION`), which `smoke` passes to the smoke script; and `tags` (the computed tag list, `sha-<short>` plus the human-readable tag if any, one full image reference per line), which only `publish` reads, as the tags it promotes (9.5). Outside pull requests, it runs `setup-qemu`, `setup-buildx`, a GHCR login with `GITHUB_TOKEN`, and `build-push-action` for `linux/amd64,linux/arm64` with `cache-from`/`cache-to: type=gha,mode=max`, the `APP_VERSION` build argument, the metadata labels (including `org.opencontainers.image.revision`), `org.opencontainers.image.version=<APP_VERSION>`, no `tags` input, and `outputs: type=image,name=ghcr.io/manykarim/demo-webshop,push-by-digest=true,name-canonical=true,push=true`. It pushes the candidate by digest without any tag, sets the job output `digest` to the pushed index digest (`sha256:…`), and writes the digest, ref, `APP_VERSION` and the computed tag list (one full image reference per line) to `$GITHUB_STEP_SUMMARY`; the step summary is the only durable record of the tag list, because a finished run's job outputs cannot be read back through the API and a blocked candidate never reaches `publish`. On pull requests, it builds amd64 only with `load: true` and no login or push, starts the image with only `-p 9090:9090`, waits for `healthy`, and runs `tools/smoke_image.py` phases `fresh`, `restarted` (after `docker restart`) and `recreated` (after `docker rm -f` and a new `docker run`); later changes add their pull-request checks as further steps after the `recreated` phase, against the same loaded container. Also on pull requests, a dry-run step proves the `workshop-*` mapping without publishing: it creates a local, never-pushed git tag `workshop-dryrun-${{ github.run_id }}` on HEAD, runs `tools/resolve_image_version.py` with `GITHUB_REF=refs/tags/workshop-dryrun-<run_id>`, and runs `docker/metadata-action` with `context: git` and the same tag rules. It asserts that the computed tags contain `workshop-dryrun-<run_id>` and `sha-<short>` and no `latest`, and that `app_version` is `workshop-dryrun-<run_id>`. If `context: git` does not resolve the local tag reliably, keep only the `resolve_image_version.py` assertion and note in the pull request that the `type=match` rule is then first checked live by task 10.5 (a), which reads the computed tag list and `APP_VERSION` from the `build` step summary of the `acceptance-conformance` task 18.1 run, on the blocked rehearsal tag `workshop-gatetest-1` and before any real workshop tag is cut. Verify that the multi-arch `build-push-action` step in the workflow file has no `tags` input, so an unverified candidate never carries a tag, and that the `build` job's `outputs:` map declares exactly `digest`, `app_version` and `tags`. Verify on the pull request run that `build` is green including all three smoke phases and the dry-run step, that the login and push steps are skipped, and that GHCR gained no package version. Prerequisite for this and every later packages-API check: the GitHub packages REST API needs a gh token with the `read:packages` scope, even for a public package, and the maintainer's token does not have it today (its scopes are `gist`, `read:org`, `repo`, `workflow`), so the endpoint would answer 403 instead of listing tags once the package exists. Before the first such check, run `gh auth refresh -h github.com -s read:packages` (this adds the scope and keeps the existing ones, and affects only the local token, not the workflow's `GITHUB_TOKEN`) and verify that `gh auth status` lists `read:packages` among the token scopes and that `gh api 'users/manykarim/packages?package_type=container'` returns 200 with a JSON list, possibly empty, and not 403 `You need at least read:packages scope`. With the scope in place, verify that `gh api user/packages/container/demo-webshop/versions` returns HTTP 404 `Package not found.` (not 403), or, if the package already exists, lists the same versions as before the run. This check and the one in 10.1 use the authenticated-user route `user/packages/...`, because the package is private until 10.2 and `users/manykarim/packages/...` serves public packages only, so the by-username route would answer 404 for an existing private package as well; `users/manykarim/...` is used only in 9.5 and 10.5 (a), which run after 10.2. The scope probe above stays on `users/manykarim/packages?package_type=container`: "List packages for a user" returns the packages the requesting user has access to, so it works while the package is private and still distinguishes 200 from a 403 missing-scope answer. Record the amd64-only pull request build duration in the pull request as a baseline.
- [ ] 9.4 **Moved to `workshop-rollout` task 2.1** (not done here; the maintainer archived this change before rollout). Add the `smoke` job (needs `build`, not on pull requests) as a matrix on `ubuntu-24.04` and `ubuntu-24.04-arm`. It logs in to GHCR read-only, because the package stays private until task 10.2 and the untagged candidate can only be pulled by digest. It runs `ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}` with only `-p 9090:9090` and waits for `healthy`, printing `docker logs` and failing after 90 seconds. It prints `docker exec <container> uname -m`. Then it runs `tools/smoke_image.py` phases `fresh`, `restarted` and `recreated` with `--expect-version ${{ needs.build.outputs.app_version }}`. Verify with actionlint now, and on the first `main` run (10.1): both legs are green, the arm leg prints `aarch64`, the amd64 leg prints `x86_64`, the `fresh` phase expects and gets `dev`, and on both architectures the runtime order's `invoice.pdf` and `summary.pdf` and every seeded order's `invoice.pdf` and `summary.pdf` render as `application/pdf` while `/static/pdfs/invoice_<order_number>.pdf` returns 404.
- [ ] 9.5 **Moved to `workshop-rollout` task 2.2** (not done here; the maintainer archived this change before rollout). Add the `publish` job with `needs: [build, smoke]` (`build` because it reads that job's outputs) and `if: ${{ !cancelled() && github.event_name != 'pull_request' && needs.build.result == 'success' && needs.smoke.result == 'success' }}`. The later changes extend it to the final form of design D7: `drift-coverage` task 16.2 adds `test` to `needs` and `needs.test.result == 'success'` to the condition, and `acceptance-conformance` task 6.2 adds `conformance` and `(needs.conformance.result == 'success' || !startsWith(github.ref, 'refs/tags/workshop-'))`, giving `needs: [build, smoke, test, conformance]`. The job logs in, takes the tag list from `needs.build.outputs.tags` (one full image reference per line, computed once in `build`: `sha-<short>` always, plus `edge`, `X.Y.Z` or `workshop-<id>` when present), so the tag names are derived in exactly one place, and runs one `docker buildx imagetools create` with a `-t` per line on `ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}`, the verified digest, without rebuilding. Because the list always contains `sha-<short>`, an empty human-readable part (a `workflow_dispatch` on another branch) needs no extra condition. It then runs `docker logout ghcr.io` and, for each created tag, an anonymous `docker buildx imagetools inspect --raw` that asserts that `linux/amd64` and `linux/arm64` are present (ignoring `unknown/unknown` attestation entries) and that the digest equals the tested one. It writes the promoted tags and the digest to `$GITHUB_STEP_SUMMARY`. Verify with actionlint now, that the job's `imagetools create` step reads `needs.build.outputs.tags` and derives no tag name of its own, and on the first `main` run (10.1–10.2): the anonymous inspect fails while the package is private and passes after the rerun, and right after the run the digests of `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:edge` and of `:sha-<short>` both equal the `build` job's `digest` output. After 10.2, dispatch the workflow on a scratch branch (`gh workflow run image.yml --ref <branch>`): the run is green, both `smoke` legs expect and get version `dev` (scenario *Image without a version tag* for a `sha-<short>`-only image), `publish` promotes only that commit's `sha-<short>`, and the `edge` digest is unchanged. Also verify that a run whose `smoke` fails creates no tag: on the same scratch branch, push a commit that makes `tools/smoke_image.py` fail its `fresh` phase on purpose and dispatch again; `smoke` fails, `publish` is skipped, `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short of that commit>` fails because the tag does not exist, and `gh api users/manykarim/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest output of that run>") | .metadata.container.tags'` prints `[]` (this check needs the `read:packages` scope from 9.3; the anonymous `docker buildx imagetools inspect` above stays as the second check and cannot replace it, because inspect can only show that one name does not resolve, never that a digest carries no tag at all). Delete the scratch branch afterwards.

## 10. Rollout

- [ ] 10.1 **Moved to `workshop-rollout` task 1.6** (not done here; the maintainer archived this change before rollout). Merge the change into `main` after review. Verify on the first workflow run that `check`, `build` and both `smoke` legs are green, and that `publish` created `sha-<short>` and `edge` on this run's candidate, which `gh api user/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest output of that run>") | .metadata.container.tags'` shows by listing both tags. Use the authenticated-user route `user/packages/...` here, not `users/manykarim/packages/...`: the by-username route serves public packages only and answers 404 until 10.2 has made the package public, while the authenticated-user route also serves the owner's private package; both need the `read:packages` scope from 9.3. As a second, registry-level check, run `docker login ghcr.io` (the package is still private, so an anonymous inspect cannot work here) and verify that `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:edge` and `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short>` both resolve to the `build` job's `digest` output. `publish` itself fails only at its anonymous inspect step (expected while the package is private). Record the duration of the multi-arch `build` job (cold cache) in the pull request next to the amd64 baseline from 9.3.
- [ ] 10.2 **Moved to `workshop-rollout` task 2.3** (not done here; the maintainer archived this change before rollout). One-time step: set `ghcr.io/manykarim/demo-webshop` to public in the GitHub package settings, confirm the package is linked to the repository, and re-run the failed `publish` job until it is green. Verify the scenario *Anonymous pull on Apple Silicon* for `edge`. Run `docker logout ghcr.io` and `docker pull ghcr.io/manykarim/demo-webshop:edge` on an amd64 machine and on an Apple Silicon machine. `docker image inspect --format '{{.Architecture}}'` prints `amd64` and `arm64` respectively, and `docker run -p 9090:9090 ghcr.io/manykarim/demo-webshop:edge` followed by `curl localhost:9090/health` returns `{"status":"ok","version":"dev"}` on both, which is the scenario *Image without a version tag* for `edge`. This also completes the README image-section check from 8.2: run those commands exactly as written with `<tag>` = `edge`.
- [ ] 10.3 **Moved to `workshop-rollout` task 3.1** (not done here; the maintainer archived this change before rollout). Switch the Coolify application from building the repository to the Docker image `ghcr.io/manykarim/demo-webshop:edge`. Use port 9090 and health check path `/health`. Remove any persistent storage, so the first start seeds fresh data, and do not set `WORKSHOP_APP_VERSION`. Verify that `curl https://<coolify-host>/health` returns `{"status":"ok","version":"dev"}`, that the deployed image digest equals the `edge` digest, that the home page loads, that the demo login `jamie@flowlinesupply.com` returns its order history, and that one seeded order's `summary_url` opens as a PDF.
- [ ] 10.4 **Moved to `workshop-rollout` task 3.2** (not done here; the maintainer archived this change before rollout). Push the git tag `v0.2.0`. Verify these points. The workflow is green. `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:0.2.0` lists `linux/amd64` and `linux/arm64`, and its digest equals the `build` job's digest output of the `v0.2.0` run (from the step summary), not merely the current `sha-<short>` tag. The image's `org.opencontainers.image.revision` label (`docker buildx imagetools inspect --format '{{json .Image}}'`) equals the tagged commit, and right after the run `sha-<short>` resolves to the same digest. After `docker logout ghcr.io`, `docker pull ghcr.io/manykarim/demo-webshop:0.2.0` on an Apple Silicon machine gives `docker image inspect --format '{{.Architecture}}'` = `arm64`, and a container from `0.2.0` reports `"version": "0.2.0"`. `docker manifest inspect ghcr.io/manykarim/demo-webshop:latest` fails. Record the `build` job duration of this run (multi-arch, warm cache from `main`); if it exceeds 15 minutes, open a follow-up to switch `build` to the native per-architecture matrix of design D7. Then switch Coolify to `0.2.0`, verify that `/health` reports `0.2.0`, and rehearse rollback: redeploy `edge` (`/health` reports `dev` and the deployed digest equals the `edge` digest), then `0.2.0` again.
- [ ] 10.5 **Moved to `workshop-rollout` task 4.6** (not done here; the maintainer archived this change before rollout). Check the image level of the first workshop tags, which `acceptance-conformance` pushes once its gate exists; this change itself pushes no `workshop-*` git tag and never publishes a rehearsal image under a `workshop-*` name (design D7, Migration Plan steps 5 and 6). Before merge, the `APP_VERSION` mapping is proven by the 9.1 unit tests and, when the 9.3 dry run resolves the local tag, the `type=match` rule too; otherwise that rule is first checked live in (a) below. The promotion mechanism is proven live on `v0.2.0` in 10.4. `acceptance-conformance` checks only the gate behavior in its tasks 18.1 and 18.3 and refers to this task for the image-level checks. (a) Blocked rehearsal tag, right after `acceptance-conformance` task 18.1 pushed `workshop-gatetest-1` and its `publish` job was skipped (scenario *Failed verification publishes no workshop tag*): take the `digest` output of that run's `build` job from its step summary; `gh api users/manykarim/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest>") | .metadata.container.tags'` prints `[]` (needs the `read:packages` scope from 9.3; only the packages API can show that a digest carries no tag at all), and `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short of the tagged commit>` fails or resolves to a different digest. From the same step summary, also verify the tag mapping on a real `workshop-*` ref, unconditionally and whether or not the 9.3 dry run fell back: the computed tag list contains `ghcr.io/manykarim/demo-webshop:workshop-gatetest-1` and `ghcr.io/manykarim/demo-webshop:sha-<short of the tagged commit>` and no `latest` and no `edge`, and the recorded `APP_VERSION` is `workshop-gatetest-1`. This is the live confirmation of the `type=match` rule and the `flavor: latest=false` flavor on a tag that is never published; a blocked candidate still proves the mapping, because `build` computes the tags before `publish` is skipped. (b) First real workshop tag, right after the `publish` job of the `acceptance-conformance` task 18.3 run succeeded (scenarios *Workshop tag publishing*, *Anonymous pull on Apple Silicon* and *Setup check reads the version* for a published workshop image): after `docker logout ghcr.io`, `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:workshop-<id>` lists `linux/amd64` and `linux/arm64`, and its digest equals that run's `build` `digest` output; right after the run, `:sha-<short>` of the tagged commit resolves to the same digest; the `org.opencontainers.image.revision` label (`docker buildx imagetools inspect --format '{{json .Image}}'`) equals the full SHA of the tagged commit; `docker pull ghcr.io/manykarim/demo-webshop:workshop-<id>` on an Apple Silicon machine without credentials gives `docker image inspect --format '{{.Architecture}}'` = `arm64`, and a container started from it with only `-p 9090:9090` answers `/health` with status 200, `"status": "ok"` and `"version": "workshop-<id>"`. Verify that the results of (a) and (b), with both run URLs and digests, are recorded in a comment on this change's pull request.
- [x] 10.6 After 10.5, archive this change on `main` with `openspec archive reproducible-image -y` (design Migration Plan step 7). Verify that `openspec/specs/workshop-image/spec.md` exists and contains `### Requirement: Order documents in the container`, and that `openspec list` no longer shows `reproducible-image`.