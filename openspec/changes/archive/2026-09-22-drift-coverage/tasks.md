## 1. Prerequisites and dependencies

- [x] 1.1 Confirm that `reproducible-image` is merged into the base of this branch (it may still be an active OpenSpec change until its task 10.6): `pyproject.toml` has `[dependency-groups] dev`, `uv.lock` exists, `backend/app/services/pdf_service.py` no longer imports `weasyprint` at module top (`PDFRenderingUnavailable` exists), `seed_users` inserts only missing users, `.gitignore` covers `workshop.db` and `*_ORD-*.pdf`, and `.github/workflows/image.yml` has the jobs `check`, `build`, `smoke` and `publish`, with the `build` outputs `digest`, `app_version` and `tags` and `publish.needs: [build, smoke]`. The shared harness of its design D11 exists: `backend/tests/harness.py` (`isolated_app`, `SCRUBBED_ENV_VARS`, `SCRUBBED_ENV_PREFIXES`, `PINNED_ENV_VARS`, `pin_test_environment`) and the root `backend/tests/conftest.py` with the fixtures `temp_database`, `app_client`, `seeded_app_client`, `pdf_unavailable` and `fake_weasyprint`. Also note whether `workshop-spaces` is already merged (`get_effective_flags` present in `backend/app/core/feature_flags.py`, `backend/tests/spaces/` present), because tasks 2.1 and 15.1 depend on it. Verify that `uv lock --check && uv sync --locked` succeeds; that `grep -n "PDFRenderingUnavailable" backend/app/services/pdf_service.py` finds the class; that `grep -cE "^def (temp_database|app_client|seeded_app_client|pdf_unavailable|fake_weasyprint)\(" backend/tests/conftest.py` prints `5` and `grep -n "def isolated_app" backend/tests/harness.py` finds the helper; that `git check-ignore workshop.db` succeeds; that `grep -nE "^  (check|build|smoke|publish):" .github/workflows/image.yml` lists all four jobs; and that `uv run pytest backend/tests` passes before any code change.
- [x] 1.2 Add `pytest-playwright` and `beautifulsoup4` to the uv `dev` group (`uv add --dev pytest-playwright beautifulsoup4`) and commit the regenerated `uv.lock`. Verify that `uv lock --check` passes, that `uv run python -c "import bs4, pytest_playwright"` exits 0, and that `uv run playwright install chromium` succeeds.

## 2. Flag seam, stage precedence and drift view

- [x] 2.1 Provide the single flag seam `get_effective_flags` as a FastAPI dependency in `backend/app/core/feature_flags.py`. If `workshop-spaces` is merged, reuse its dependency unchanged. Otherwise add an interim `get_effective_flags(request, session) -> dict[str, bool]` that returns `list_feature_flags(session)`. Verify with a unit test in `backend/tests/unit/test_drift_view.py` (new): a route declaring the dependency sees the dict supplied through `app.dependency_overrides[get_effective_flags]`.
- [x] 2.2 Add `LOCATOR_FLAGS` and `effective_stage(flags)` to `backend/app/core/workshop.py` (4 if `LOCATOR_V4`, else 3 if `LOCATOR_V3`, else 2 if `LOCATOR_V2`, else 1). Verify with unit tests in `backend/tests/unit/test_workshop.py`: `{}`→1, V2→2, V3→3, V4→4, V2+V3→3, V2+V4→4, all three→4.
- [x] 2.3 Define the mapping structure in `backend/app/core/workshop.py`: `COVERED_IDS`, `COVERED_CLASSES`, `DATA_TEST_VALUES`, `STABLE_IDS` (exactly `main-content`, `price-min-range`, `price-max-range` and the uncovered newsletter ids `newsletter-email`, `newsletter-hint` and `newsletter-alert`) and one `StageSpec` each for stages 2–4 (`ids`, block and exact `classes`, `keep_data_test`, `layout`). Start with the product-card keys; groups 5, 8 and 9 add the keys for the other flows. Verify with unit tests: replacement names are unique across stages and never equal a stage-1 name; `keep_data_test` is true only for stage 2; stage 3 has no class entries, and its id entries are all form-field ids; only stage 4 has `layout` entries, and its keys are a subset of `product-card`, `product-hero-actions` and `checkout-field`; `STABLE_IDS` equals exactly those six ids and is the only allowlist the template scan (5.10) consults, so the mapping and the scan cannot drift apart; no stage renames an id in `STABLE_IDS`, and no `StageSpec.ids` key or value is one of them; the stage 2 card block rename is `item-card` and the stage 4 one is `product-tile` (`rendered_stage()` in `workshop-spaces`' `backend/tests/spaces/helpers.py` relies on these names).
- [x] 2.4 Implement `DriftView` for one stage in `backend/app/core/workshop.py`: `id(key)`, `cls("key other-key")` (a block rename also renames `block__*` and `block--*`, and an exact override wins over the block rule), `test(key)` (returns the `data-test` attribute markup, or nothing in stages 3–4), `layout(component)` and a `stylesheet_href` placeholder. An unknown key raises. Verify with unit tests in `backend/tests/unit/test_drift_view.py` covering block derivation, exact-override precedence, the unknown-key error, `test()` being empty in stages 3 and 4, and identical output from two instances for the same stage.
- [x] 2.5 Remove `LOCATOR_STAGES`, `ID_TRANSFORMS`, `get_class_name`, `get_element_id`, `should_remove_data_test` and `get_data_test_attr` from `backend/app/core/workshop.py`. Rewrite `TestLocatorVariations` and `TestDataTestAttributes` in `backend/tests/unit/test_workshop.py` against `effective_stage` and `DriftView`; the inherited `"button--primary": "btn-main"` entry and its assertion go with them, because the shared `button` block is uncovered and stable (design Decision 3) and `btn-main` is reused as the stage-2 name of the card action hook `product-card__add` (5.2). Verify that `grep -rnE "get_class_name|get_element_id|ID_TRANSFORMS|LOCATOR_STAGES|get_data_test_attr" backend` returns nothing and that `uv run pytest backend/tests/unit/test_workshop.py` passes.
- [x] 2.6 Add the `workshop_view` dependency, which builds `WorkshopView(stage, drift)` from `get_effective_flags`, stores it on `request.state.workshop_view`, and sets and resets a `ContextVar` for the request. In `_mount_static_and_templates` (`backend/app/main.py`), register the Jinja global `drift` as a proxy that raises when no view is set. Declare `workshop_view` on the page routes `/`, `/products`, `/products/{product_id}`, `/cart`, `GET /checkout` and `POST /checkout`. Routes keep passing the full `feature_flags` dict from the seam for now, because `product_card.html`, `home.html` and `products.html` still read `LOCATOR_*` and `BUG_*` keys until group 5 (narrowed in 5.10). Verify with unit tests in `backend/tests/unit/test_drift_view.py`: rendering a template that uses `drift` outside a request raises; two consecutive requests with different overridden flags each render their own stage; the `ContextVar` is unset after each response. Also verify that `/products` rendered with `LOCATOR_V2` overridden still contains `item-card`, so the old inline drift is intact.

## 3. Planted-bug registry, seed rows and presets

- [x] 3.1 Add the ordered `PLANTED_BUGS` registry (`flag`, `flow`, `trigger`, `defect`, `scope`) to `backend/app/core/workshop.py` for `BUG_MISSING_BUTTON`, `BUG_WRONG_PRICE`, `BUG_BROKEN_LINKS`, `BUG_SLOW_RESPONSE` and `BUG_CHECKOUT_TOTAL`. Add a bug view with the helpers `card_price`, `card_href`, `hides_add_to_cart` and `checkout_total`, built from the same flags; attach it to `WorkshopView` as `bugs` and register the `bugs` Jinja global proxy in `_mount_static_and_templates`. Remove the `apply_bug_*` helpers. Verify with rewritten `TestIntentionalBugs` tests: with the flag on, product 3's card price is ×1.15 rounded to cents, product 4 links to `/products/invalid-4`, product 5's button is hidden and product 4's is not; `checkout_total` replaces only `total` with `subtotal`; every helper is a no-op with its flag off and returns the same result for stages 1–4. Verify in `backend/tests/unit/test_drift_view.py` that rendering a template that uses `bugs` outside a request raises. Also verify that `grep -rn "apply_bug_" backend` returns nothing.
- [x] 3.2 Generate the bug rows of `FEATURE_FLAGS` in `backend/app/seeds/seed_data.py` from `PLANTED_BUGS`, which adds `BUG_CHECKOUT_TOTAL` (disabled). Verify with unit tests: every registry flag appears exactly once and disabled; the cheapest seeded price × 0.07 is ≥ 0.01; running `seed_feature_flags` on a temporary SQLite database (the shared harness's `temp_database` fixture) that already has `BUG_WRONG_PRICE=true` and no `BUG_CHECKOUT_TOTAL` inserts `BUG_CHECKOUT_TOTAL=false` and leaves `BUG_WRONG_PRICE` true.
- [x] 3.3 Add `build_presets()` to `backend/app/core/workshop.py` (design Decision 12). `backend/app/api/workshop.py` imports its result instead of the local `PRESETS`; `_get_active_bugs` iterates `PLANTED_BUGS` in registry order; the status, preset and flags responses report `locator_stage = f"v{effective_stage(flags)}"`, and `_get_locator_stage` is removed; `GET /api/workshop/presets` describes `drift_and_bug`. Verify with unit tests on `build_presets()`: `stage1` sets `LOCATOR_V2`, `LOCATOR_V3` and `LOCATOR_V4` false, so `effective_stage(build_presets()["stage1"])` is 1 (there is no `LOCATOR_V1`; stage 1 is the absence of locator flags); `stage2`–`stage4` each set exactly their own locator flag true and the other locator flags false; none of `stage1`–`stage4` contains a bug key; `buggy` sets all five registry bugs true and no locator keys; `clean` sets every locator and bug flag false and `AI_DETERMINISTIC` true; `drift_and_bug` sets `LOCATOR_V4`, `BUG_WRONG_PRICE` and `BUG_CHECKOUT_TOTAL` true and every other locator and bug flag false; `ai_chaos` is unchanged.

## 4. Contract test harness

- [x] 4.1 Create `backend/tests/contract/__init__.py` and `backend/tests/contract/conftest.py` (both new) on the shared harness from `reproducible-image` (design Decision 7). The conftest imports `backend.app` only inside fixture bodies. It sets no environment variables, patches no settings, resets no engine globals, clears no flag cache and defines no fixture named `temp_database`, `app_client`, `seeded_app_client`, `pdf_unavailable` or `fake_weasyprint`. Its package-scoped fixture `contract_client` enters `isolated_app(tmp_path_factory.mktemp("contract"))` from `backend/tests/harness.py` (products and flags, no users, because contract tests need none) and yields the `TestClient`. The conftest also provides a `render(path, flags, session_id)` helper that sets and clears `app.dependency_overrides[get_effective_flags]` in `try/finally` and sends `x-session-id`, a cart helper that posts to `/api/cart/items` with `X-Session-ID`, a product lookup by SKU, and a `subprocess_env` fixture: a copy of `os.environ` with `WORKSHOP_DATABASE_URL` and `WORKSHOP_PDF_OUTPUT_DIR` set to the values of `settings.database_url` and `settings.pdf_output_dir` inside `contract_client`. Contract tests that place orders also request the root fixture `fake_weasyprint`. Verify that `backend/tests/contract/test_harness.py` (new) passes: it renders `/` in stages 1 and 2 with status 200; asserts that `PUL-RNG-003`→3, `NIM-LGT-004`→4, `ATL-DSK-005`→5 and `CAS-BOT-012`→12; and asserts that the engine's database path (`make_url(str(get_engine().url)).database`) and `Path(settings.pdf_output_dir)` both resolve inside the pytest temporary base directory. Also verify that `grep -nE "setenv|async_engine|async_session_factory|_cache" backend/tests/contract/conftest.py` finds nothing; that the tests pass when other suites import the app first (`uv run pytest backend/tests/unit backend/tests/integration backend/tests/contract`); and that the run leaves no data behind: create a marker with `touch "$marker"` before the run, then `find . \( -path ./.venv -o -path ./.git \) -prune -o \( -name '*.db' -o -name '*_ORD-*.pdf' \) -newer "$marker" -print` prints nothing.
- [x] 4.2 Implement the semantic snapshot in `backend/tests/contract/semantic.py` (new), using `beautifulsoup4` with the `html.parser` backend. The snapshot is the document-order sequence of normalized visible text (excluding `script`, `style` and `template`), explicit and implicit roles, accessible names (`aria-labelledby`, `aria-label`, `label[for]` or a wrapping label, `alt`, name from content), field `name` and `type`, form `action` and `method`, hyperlink targets (`href` of `a[href]` and `area[href]` only; resource URLs such as `link[href]` and `script[src]` are not part of the snapshot), and the content data attributes. It takes an optional `replacements` list of (regex, placeholder) pairs applied to text and `href` values; without it the snapshot is unchanged. Define `ORDER_RESULT_MASK = [(r"ORD-[0-9A-F]{8}", "ORD-<n>"), (r"/api/docs/orders/\d+/", "/api/docs/orders/<id>/")]` next to it. Verify with `backend/tests/contract/test_semantic_snapshot.py` (new) on HTML fixtures: renaming ids (with `for` and `aria-*` references renamed consistently), classes and `data-test`, adding a role-less `div` or `span` wrapper, or changing only `<link rel="stylesheet" href>` (for example `/assets/styles.aaa.css` to `/assets/styles.bbb.css`) leaves the snapshot unchanged; changing text, `aria-label`, a `for` target, a field `name`, an `a` `href`, a form `action`, a content data attribute or the content order changes it. Two success-alert fixtures that differ only in order number and order id (in the message and in both `invoice.pdf` and `summary.pdf` hrefs) are equal with `ORDER_RESULT_MASK` and differ without it; with the mask, changing the link text, the path shape (`invoice.pdf` to `receipt.pdf`), the customer email or the message wording still changes the snapshot.
- [x] 4.3 Implement hook extraction and per-stage expectations in `backend/tests/contract/hooks.py` (new): id set, class set, `data-test` multiset, and the ancestor tag chain (tag names up to `body`) of an element. Stage 2: the `data-test` multiset is equal and not empty, covered ids and classes differ, and the ancestor chains of the structural anchors are equal. Stage 3: no `data-test`, the classes are equal, only form-field ids differ, and the chains are equal. Stage 4: no `data-test`, covered ids and classes differ, and the structural oracle holds. Add the hand-written coverage oracle `backend/tests/contract/coverage_oracle.py` (new): stage-1 selectors grouped by flow, each naming the stages that must break it; an entry must match in stage 1 and in every stage not named, so a `[data-test='…']` entry breaks in stages 3–4 and matches in stage 2, a class entry or a non-form-field id breaks in stages 2 and 4, and a form-field id breaks in stages 2–4. Add the hand-written structural oracle `backend/tests/contract/structure_oracle.py` (new): each stage 4 layout component maps to its pages and anchors found through stable hooks (design Decision 7). Add `backend/tests/contract/test_stage_contract.py` (new), parametrized over a `COVERED_PAGES` list × stages 1–4, which asserts snapshot equality with stage 1, the stage expectations, the coverage oracle, and for the structural oracle: each anchor matches at least once on a listed page, its ancestor chain differs from stage 1 in stage 4, and the oracle's components equal the keys of `StageSpec(4).layout`. Verify that `backend/tests/contract/test_hooks.py` (new) passes on HTML fixtures (including an oracle entry that wrongly matches in a stage it names, and an anchor that matches nowhere, both reported) and that `uv run pytest backend/tests/contract/test_stage_contract.py --collect-only` collects.

## 5. Templates: drift and bugs per flow

- [x] 5.1 Remove the "Workshop Mode Active" banner from `backend/app/templates/home.html` and `backend/app/templates/products.html`. In `home.html`, fix the two `<h2 class="section-title">` elements that are closed with `</h3>` (lines 145 and 164). Verify with a contract test: `/` and `/products`, rendered with every locator flag and every registry bug enabled, contain none of the texts "Workshop Mode Active", "Locator V" or "Bug:"; each `h2.section-title` on `/` contains only its heading text.
- [x] 5.2 Rewrite the macro in `backend/app/templates/components/product_card.html` to use only `drift.cls`, `drift.id`, `drift.test`, `drift.layout("product-card")`, `bugs.card_price`, `bugs.card_href` and `bugs.hides_add_to_cart`. Remove the `feature_flags` parameter from the macro and from all its call sites. Replace the hidden-button placeholder ("Button unavailable", `data-test="hidden-button-placeholder"`) with an empty `aria-hidden` spacer. Use role-less wrapper elements for the stage 4 `wrapped` layout. The add-to-cart button renders `class="button button--primary {{ drift.cls('product-card__add') }}"`: the shared `button` classes are uncovered and stay literal in every stage (design Decision 3), and `product-card__add` is the covered hook, which follows the `product-card` block rename in stage 4 (`product-tile__add`) and gets the stage-2 exact override `product-card__add → btn-main`, reusing the name today's macro already uses. Add the card keys, including that override, to the `StageSpec` tables. Verify by adding `/products` to `COVERED_PAGES`, `[data-test='product-card']` (breaks in stages 3–4), `.product-card__title a` and the card's `.product-card__add` (break in stages 2 and 4) to the coverage oracle, and the `product-card` entry (every `button[data-product]` on `/products`) to the structural oracle; `uv run pytest backend/tests/contract -k products` passes. A contract test asserts that with `BUG_MISSING_BUTTON` the card for product 5 has no add-to-cart button and no "Button unavailable" text, while product 4's card has a button, in stages 1–4.
- [x] 5.3 Convert the listing page (`products.html` grid and search section) and the home page hero search (including its results grid) and the featured, popular and premium grids (`home.html`) to `drift.*` hooks, with `data-test` added through `drift.test` on inputs and buttons (design Decision 3, A4). Route every `product-grid` wrapper through `drift.cls("product-grid")`: in `home.html` the hero search results grid and the featured, popular and premium grids, and in `products.html` the listing grid and the search results grid, so none keeps the class literally. Register the search results container id `search-results` in `COVERED_IDS` and render it through `drift.id("search-results")` on both pages (the accessible hooks that go with it are added in 8.1). Route the search results section's own `search-results` class (today `<section class="search-results" …>` in both pages) through `drift.cls("search-results")` as well, and register it in `COVERED_CLASSES` as a block, so that name never reaches the markup literally and the template scan of 5.10 passes; like every other block it is renamed in stages 2 and 4 only, because stage 3 keeps classes. The same stage-1 name is then both a covered id and a covered class, exactly as `auth-modal` (5.6) and `chat-widget` (5.7) already are, and its class replacement and its id replacement differ in each stage (for example stage 2 class `results-panel`, stage 2 id `results-list`), so 2.3's invariant that replacement names are unique across stages and never equal a stage-1 name still holds and the two `## Drift mapping` rows stay distinguishable by their Kind. The stylesheet's `.search-results` and `.search-results[hidden]` rules follow automatically through the per-stage rewrite of 6.1. The search field wrappers and the submit buttons keep the shared, uncovered `form-field` and `button`/`button--*` classes. Verify by adding `/` to `COVERED_PAGES` and to the coverage oracle `.hero__search`, `.product-grid`, `.search-results` and `#search-results` (break in stages 2 and 4) and the search input's `data-test` entry, for example `[data-test='search-input']` (breaks in stages 3–4); `uv run pytest backend/tests/contract -k "home or products"` passes.
- [x] 5.4 Convert `backend/app/templates/product_detail.html` (`product-hero*`, the add-to-cart button, related and trending cards) to `drift.*` hooks. The detail add-to-cart button gets a `data-test` value distinct from the card's `add-to-cart-btn` (for example `detail-add-to-cart`), because related cards on the same page also carry `add-to-cart-btn`. Add the stage 4 layout `drift.layout("product-hero-actions")` with role-less wrappers inside the purchase actions (design Decision 3) and its `StageSpec` entry. Route both `product-grid` wrappers in `product_detail.html` (the "You might also like" and "Trending in the studio" sections) through `drift.cls("product-grid")`, so no template keeps that covered class literally. The hero add-to-cart button keeps the shared, uncovered `button button--primary button--lg` classes; its covered hook is the `product-hero*` block. Verify by adding `/products/3` to `COVERED_PAGES`; `.product-hero` and `.product-grid` (break in stages 2 and 4) and `[data-test='detail-add-to-cart']` (breaks in stages 3–4) to the coverage oracle; and the `product-hero-actions` entry (`button[data-product="3"]` on `/products/3`) to the structural oracle, whose ancestor chain differs in stage 4. A contract test looks cards up by SKU, first asserts that the expected cards are rendered (it fails if one is missing), and asserts in stages 1–4: the related cards on `/products/3` drift in stage 2; with `BUG_WRONG_PRICE`, the related card for `CAS-BOT-012` (product 12, the only other Health product, $79.00) shows $90.85 while the product 3 hero price stays $189.00, and with the flag off the card shows $79.00; with `BUG_BROKEN_LINKS`, the `CAS-BOT-012` card links to `/products/invalid-12` and the trending card for `NIM-LGT-004` (product 4) links to `/products/invalid-4`, and with the flag off they link to `/products/12` and `/products/4`; with `BUG_MISSING_BUTTON`, the related card for `ATL-DSK-005` (product 5) has no add-to-cart button while the one for `AUR-NEU-001` (product 1) has one. Product 3's own card never appears on `/products/3`, whose hero is product 3 itself, so the same test checks product 3's card on `/products/12` (related cards: products 3, 5, 1 and 10): with `BUG_WRONG_PRICE`, the related card for `PUL-RNG-003` shows $217.35 while the product 12 hero price stays $79.00, and with the flag off that card shows $189.00. This is the detail-card coverage that `acceptance-conformance` relies on for `BUG_BROKEN_LINKS` (WEB-003 AC-5).
- [x] 5.5 Convert the primary navigation and cart badge in `backend/app/templates/base.html` (`site-nav*`, `badge`, `primary-nav-menu`) to `drift.*` hooks. Resolve `aria-controls` through `drift.id`, keep the skip link `href="#main-content"` and `id="main-content"` stable, and add `data-test` on the cart badge. Verify that the coverage oracle entries `.site-nav__link--cart .badge` and `#primary-nav-menu` match in stages 1 and 3 and not in stages 2 and 4 (scenario "Cart badge drifts"), that the badge's `data-test` entry (for example `[data-test='cart-count']`) matches in stages 1–2 and not in stages 3–4, and that the snapshot of `/` is equal across stages with an unchanged skip-link target.
- [x] 5.6 Convert the sign-in modal and account menu in `base.html` (`auth-modal*`, `auth-email`, `auth-password`, `auth-modal-title` with `aria-labelledby` through `drift.id`, `account-dropdown*`) to `drift.*` hooks, with `data-test` on the inputs, buttons and account menu trigger. The overlay element that carries `hidden` (today `div.auth-modal`) gets an id through `drift.id("auth-modal")`, added to `COVERED_IDS`; `role="dialog"`, `aria-modal` and `aria-labelledby` stay on the inner card. The account menu panel (today `div.account-dropdown__panel`, which carries `hidden`) gets an id through `drift.id("account-panel")`, also added to `COVERED_IDS`, so the trigger can point at it with `aria-controls` (8.1); it is not a form field, so only stages 2 and 4 rename it. Stage 3 renames `auth-email` and `auth-password`. Verify that the coverage oracle entries `#auth-email` and `#auth-password` match in stage 1 and not in stages 2–4 (scenario "Sign-in modal drifts"), that `#account-panel` and `.account-dropdown__trigger` match in stages 1 and 3 and not in stages 2 and 4 (stage 3 keeps classes and non-form-field ids), that `[data-test='account-menu-trigger']` matches in stages 1–2 and not in stages 3–4, and that the dialog keeps its accessible name in the snapshot of every stage.
- [x] 5.7 Convert the chat widget and add-to-cart flash in `base.html` (`chat-widget*`, `chat-launcher`, `chat-input`, `chat-message`, `flash`, `flash-message`) to `drift.*` hooks, with `data-test` on the chat input and launcher. The widget gets its id through `drift.id("chat-widget")` in the template instead of from runtime JS. The confirmation region keeps `role="status"` and `aria-live="polite"`. The two message `<template>` children the script clones from (user first, assistant second) are added to the message container in 9.3. Stage 3 renames `chat-input`. Verify that the coverage oracle entry `#chat-input` matches in stage 1 and not in stages 2–4, that `.chat-launcher` and `#flash-message` match in stages 1 and 3 and not in stages 2 and 4, that `[data-test='chat-launcher']` matches in stages 1–2 and not in stages 3–4, and that the snapshot of `/` is equal across stages.
- [x] 5.8 Convert `backend/app/templates/cart.html` (`cart-item*`, `cart-summary*`) to `drift.*` hooks, with `data-test` on the item rows and summary totals. Verify by adding `/cart` with items and empty to `COVERED_PAGES`, `.cart-item` and `.cart-summary__total` (break in stages 2 and 4) and their `data-test` entries (for example `[data-test='cart-item']` and `[data-test='cart-total']`, break in stages 3–4) to the coverage oracle; `uv run pytest backend/tests/contract -k cart` passes.
- [x] 5.9 Convert the checkout form and result message in `backend/app/templates/checkout.html` (`checkout-form`, `checkout-email|name|address|team-size|notes`, `checkout-email-hint`, `checkout-alert*`) to `drift.*` hooks, with `for` and `aria-describedby` resolved through `drift.id`, `data-test` on the fields, the submit button and the result message, stage 3 form-field id renames, and the stage 4 `checkout-field: grouped` layout. The field wrapper labels keep the shared, uncovered `form-field` class. Verify by adding `GET /checkout` (with items and empty) and the empty-cart `POST /checkout` 400 render to `COVERED_PAGES`; `#checkout-email` (breaks in stages 2–4, scenario "Checkout form drifts"), `.checkout-form` and `.checkout-alert` (break in stages 2 and 4) and the result message's `data-test` entry (for example `[data-test='checkout-result']`, checked on the `POST /checkout` renders, breaks in stages 3–4) to the coverage oracle; and the `checkout-field` entry (`form[action="/checkout"]` fields named `email`, `name`, `address`, `team_size`, `notes` on `/checkout`) to the structural oracle. In stage 4 each field's ancestor chain differs from stage 1 while the `label[for]` associations and field names stay identical in the snapshot (scenario "Stage 4 restructures checkout"). The contract tests pass.
- [x] 5.10 Add `backend/tests/unit/test_template_scan.py` (new). It scans `backend/app/templates/**` and fails on any `LOCATOR_` or `BUG_` token, a literal `data-test=`, a literal `id="…"` whose value is not in `STABLE_IDS`, or a name in `COVERED_IDS` or `COVERED_CLASSES` (including the `block__*` and `block--*` names derived from a covered block) written literally inside a `class="…"` attribute, that is anywhere outside a `drift.cls(…)`, `drift.id(…)` or `drift.layout(…)` argument (design Decision 1). A covered name matches only as a whole class token (the whole space-separated word inside the attribute), never as a substring, so the uncovered literal `class="category-badge"` in `home.html`, `products.html` and the card macro is accepted although the covered class `badge` is a substring of it. That covered-name rule is what keeps the name-based stylesheet rewrite of group 6 safe, and it is why a class an uncovered flow writes literally (`form-field`, `button`/`button--*`) is not covered. The newsletter form in `home.html` stays untouched: its three ids are in `STABLE_IDS` (task 2.3), so the scan passes without converting it. The literal-id rule matches the whole attribute name, so `data-product-id="…"` (removed in 8.5) is not mistaken for an `id`. Once it passes on the converted templates, narrow `feature_flags` in the page contexts in `backend/app/main.py` to the non-workshop flags `NEW_CART_UI` and `MOBILE_UI_V1`. Verify that the scan passes on the real templates, including `home.html`'s newsletter block; that a fixture containing `id="newsletter-email"`, `id="newsletter-hint"`, `id="newsletter-alert"`, `<label for="newsletter-email">` and `class="category-badge"` is accepted, while the negative cases (fixture strings containing `LOCATOR_V2`, `data-test="x"`, `id="auth-email"`, a slider `id` missing from `STABLE_IDS`, `class="product-grid"` and `class="product-card__title"`) are reported; and that a contract test asserts that `response.context["feature_flags"]` on `/` and `/products` contains no `LOCATOR_*` or `BUG_*` key while both pages still render every stage.

## 6. Stylesheet variants per stage (design Decision 5, A2)

- [x] 6.1 Move `backend/app/static/styles.css` to `backend/app/assets/styles.css` (new directory). In `backend/app/core/workshop.py`, generate one variant per distinct class mapping at startup by rewriting covered `.class` tokens only in selector preludes, with the same resolver the templates use. Each variant gets a content digest, and stages 1 and 3 share one variant. Verify with unit tests in two parts. (a) Against the real stylesheet: the stage 2 and stage 4 variants contain no covered stage-1 class name (matched as whole class tokens, like the scans of 5.10 and 9.4, so `category-badge` and `site-nav-open` are not hits); no `form-field`, `form-field__icon`, `button` or `button--primary|ghost|text|lg` token is rewritten in any variant, which is what pins the invariant that a class written literally by an uncovered flow never drifts (design Decisions 3 and 5, enforced in markup by the scan rule of 5.10) — stated as two checks, because three real rules put a shared token in the same prelude as a covered one: the rules whose preludes hold only uncovered tokens (`.form-field`, `.form-field input, .form-field textarea`, `.form-field__icon`, `.button`, `.button--primary`, `.button--primary:hover, .button--primary:focus-visible`, `.button--ghost`, `.button--ghost:hover, .button--ghost:focus-visible`, `.button--text`, `.button--lg` and `.button[aria-busy="true"]`) are byte for byte identical in every variant, and in the three mixed rules (`.button:focus-visible, .theme-toggle:focus-visible, .site-nav__link:focus-visible, .product-card__link:focus-visible`; `.product-card--compact .button`; `.auth-modal__form .form-field`) only the covered tokens change while the `.button` and `.form-field` tokens stay as they are; `@media (max-width: 900px)` and `@media (max-width: 600px)` and every declaration block are unchanged byte for byte; selectors without covered class tokens (`*, *::before, *::after`, `body`, `a`, the `:focus-visible` rules on `a`, `button`, `input` and `textarea`, `input[type="range"]`, and the uncovered names that merely contain a covered one, `.category-badge` with its `[data-category="…"]` variants and `body.site-nav-open`) are unchanged; element-plus-class compounds such as `body.theme-dark` keep the element name; inside `@media (max-width: 600px)`, `.site-nav__items>*` keeps its child combinator and only its class token changes; stages 1 and 3 have the same digest; the source contains no `[class` attribute selector. (b) Against a small synthetic stylesheet written in the test and labeled as a fixture: an at-rule prelude with a decimal (`@media (max-width: 40.5em)`), a value without a leading zero (`margin: .5rem`) and a class-like string inside a declaration come through unchanged.
- [x] 6.2 Serve the variants from memory through a small Starlette app mounted with `app.mount("/assets", …)` containing `Route("/styles.{digest}.css")` (`Content-Type: text/css`, `Cache-Control` with `immutable`, 404 for an unknown digest). A mount is not listed in `/openapi.json` and does not run FastAPI app-level dependencies, so it stays outside the space resolution of `workshop-spaces` (design Decision 5). Implement `drift.stylesheet_href`, and link it from `base.html` instead of `/static/styles.css`. Verify with a contract test: in each stage the page's stylesheet link returns 200 with those headers and contains that stage's renamed card class; `/static/styles.css` returns 404; the snapshot of `/` is still equal across stages 1–4.

## 7. Browser smoke harness (pytest-playwright)

- [x] 7.1 Add `[tool.pytest.ini_options]` to `pyproject.toml`: register the marker `browser` and deselect it by default (`addopts = "-m 'not browser'"`), so that `uv run pytest backend/tests` runs without browsers. `acceptance-conformance` later extends this same `addopts` to `-m 'not browser and not conformance'`. Verify that `uv run pytest --markers | grep browser` lists the marker and that `uv run pytest backend/tests --collect-only -q` collects no test under `backend/tests/browser`.
- [x] 7.2 Create `backend/tests/browser/__init__.py` and `backend/tests/browser/conftest.py` (both new). The conftest imports the app only inside fixture bodies and defines no fixture with a canonical harness name. It overrides pytest-base-url's session-scoped `base_url` fixture for this directory (design Decision 8). With `--base-url`, the fixture returns that value and the tests target the running server. Without it, the fixture seeds a temporary database with `python -m tools.seed_db` in a subprocess (with temporary `WORKSHOP_DATABASE_URL` and `WORKSHOP_PDF_OUTPUT_DIR` passed explicitly in the subprocess environment), starts uvicorn from source on a free port with the same environment, waits for `/health`, returns that URL and stops the server at session end. `base_url` is not set in `pyproject.toml`, and no project-specific environment variable selects the target: the only target selector is pytest-base-url's `--base-url` option, whose default that plugin itself fills from `PYTEST_BASE_URL`. A `stage` fixture is parametrized over `stage1`–`stage4`, applies the preset through `POST /api/workshop/preset` and applies `clean` afterwards. Verify that `uv run pytest -m browser backend/tests/browser/test_harness.py` (new) passes without `--base-url`: `/health` returns `ok`, and `/api/workshop/status` reports `v1`–`v4` for the matching parameter. Verify that it also passes with `--base-url http://localhost:9090` against a container of a locally built image (`docker build -t demo-webshop:local . && docker run -d -p 9090:9090 demo-webshop:local`), where a test asserts that `base_url` equals the option value. Verify that `grep -n "base_url" pyproject.toml` finds nothing and that `grep -rn "PYTEST_BASE_URL" backend/tests pyproject.toml` finds nothing, so no project-specific environment variable can point the suite somewhere else. Also verify with the marker and `find` check from 4.1 that the run creates no database or order PDF inside the repository.
- [x] 7.3 Add `backend/tests/browser/test_drift_smoke.py` (new). It uses only role, label and text locators and covers: add to cart from a product card on `/` (the badge count increases; the flow runs on `/` and not on `/products`, because `<output>` carries the implicit role `status` and the price filter therefore adds two more matches there, see 8.1 and 8.2); sign-in with `jamie@flowlinesupply.com`, closing the dialog once by its close button and once by clicking the backdrop before signing in, and opening the account menu through its trigger (the trigger shows the user's first name and the panel's "Log out" button becomes visible), then signing out through the "Log out" button (the confirmation reads "Signed out successfully." and the "Log in" button is visible again) — the trigger's `aria-expanded` flip and the three lists located by their accessible names "Saved addresses", "Payment methods" and "Recent orders" are added to this flow in 8.3, not here, because 8.1 is what puts those attributes and labels into `base.html` and 8.3 is where `app.js` starts writing `aria-expanded`; today's trigger carries no `aria-expanded` and the three `<ul>` elements have no accessible name, so asserting them at this point would fail the stage-1 run below; checkout autofill after sign-in (email, name, address); search suggestions with keyboard selection; mobile navigation at 390×844; the theme toggle (`theme-dark` on `body`, `aria-pressed` flips, the toggle's label text flips between "Dark mode" and "Light mode", and exactly one of the two icons is visible); the chat widget returning a mock AI answer; and, on `/products`, exactly one button with the accessible name "Add <product> to cart" per card. Verify that `uv run pytest -m browser backend/tests/browser/test_drift_smoke.py -k stage1` passes. Stages 2–4 are made green by groups 8 and 9.
- [x] 7.4 Add the aria snapshot cross-check to `backend/tests/browser/test_drift_smoke.py`. For the pages the smoke flows visit (at least `/`, `/products`, `/products/3`, `/cart`, `/checkout`, signed out and with the same cart), `page.locator("body").aria_snapshot()` in stage N must equal stage 1. Both sides of every cross-stage comparison in the browser suite are taken in one and the same browser context, and therefore in one and the same workshop space (none before 15.5, the stage fixture's own space after it), against the same session and cart, within a single test run: the test builds the shared state once (signed out, the fixed cart), applies preset `stage1` through `POST /api/workshop/preset` from that context (`page.request.post`, so it carries the context headers), records the baseline for the pages above, then applies the parametrized stage preset without touching the cart and snapshots the same pages again. No baseline is cached across parametrizations, contexts or spaces, so the `[data-workshop-space]` indicator that `workshop-spaces` renders in the site header of every non-`default` space — part of the body's accessibility tree — is identical on both sides. Use preset `stage1`, not `clean`, for the baseline pass: stage presets set only locator flags (design Decision 12), so the two passes differ in exactly the locator flags, while `clean` would also reset bug and AI flags and diverge from the stage side; `clean` stays what the fixture applies in teardown (7.2). Verify that it passes for `stage1`–`stage4` (run `uv run pytest -m browser -k aria`).

## 8. Shop script binds only to stable hooks (design Decision 4, A1)

- [x] 8.1 Add the stable accessible hooks to the templates, identical in every stage: `nav[aria-label="Primary navigation"]`, whose mobile toggle stays the nav's **only direct-child `button`** and keeps its `aria-controls` pointing at the menu it opens, so it is located as `nav[aria-label="Primary navigation"] > button[aria-controls]` (`:scope > button[aria-controls]`) while every other `aria-controls` button of the header (the "Log in" button and the account trigger) sits inside that menu — the primary navigation gets no `drift.layout` variant in any stage (design Decision 1's layout list is exhaustive: `product-card`, `product-hero-actions`, `checkout-field`), which is what makes this one structural step stage-independent; the "Log in" button with `aria-haspopup="dialog"` and `aria-controls` resolved through `drift.id("auth-modal")` to the overlay that carries `hidden` (not to the inner `role="dialog"` card), with no stage adding a wrapper that fills the overlay, and with no `aria-expanded` on it (`app.js` toggles the overlay's `hidden`, not an expanded state), so inside the nav menu `button[aria-expanded][aria-controls]` is exactly the account trigger; the chat launcher's `aria-haspopup` and `aria-controls` (resolved through `drift.id("chat-widget")`) moved from runtime JS into `base.html`, the launcher staying a direct child of `body` in every stage so it is located as `body > button[aria-haspopup="dialog"][aria-controls]` and is never confused with the "Log in" button, which carries the same pair inside the nav; `role="search"` on the search forms with `input[name="query"]`; `aria-pressed` on the theme toggle, whose label stays its only `<span>` (the moon path loses its static `hidden`, because the sun/moon swap moves into the per-stage stylesheet in 8.2); `aria-live` on the cart badge (without `role="status"`); the add-to-cart confirmation region keeping `role="status"` and `aria-live="polite"` as the only element with the `role="status"` attribute in `base.html` — note that `<output>` carries the implicit ARIA role `status`, so on `/products` the two price outputs expose that role too; the uniqueness statement covers the explicit attribute, which is what `app.js` (`document.querySelector('[role="status"]')`) and the contract test below rely on, and no browser check may depend on page-wide uniqueness of the status role (8.2, 8.4). Add the hooks that replace the account-menu, chat and search markers removed in 8.5, identical in every stage: the account menu trigger gets `aria-expanded="false"` and `aria-controls` resolved through `drift.id("account-panel")` to the panel that carries `hidden` (5.6), keeps the user-name `<span>` as its only `<span>`, and stays inside the nav menu in every stage, so it is located as that menu's only `button[aria-expanded][aria-controls]` (the mobile toggle carries the same two attributes but is the nav's direct child and therefore outside the menu, and the theme toggle carries `aria-pressed`, not `aria-expanded`); the panel's three `<ul>` elements get `aria-label="Saved addresses"`, `aria-label="Payment methods"` and `aria-label="Recent orders"`, matching their `<h3>` text, and the logout button gets `aria-label="Log out"`, equal to its visible text (no `aria-haspopup` on the trigger: the panel has no `menu` or `menuitem` roles, and `aria-haspopup="dialog"` already belongs to the "Log in" button and the chat launcher); the chat message container gets `role="log"` and `aria-label="Conversation"`, and the chat close button keeps its `aria-label="Close AI assistant"` as its hook; the search results section in `home.html` and `products.html` gets `role="region"` with `aria-label="Search results"`, its results container carries the id from `drift.id("search-results")` (5.3), and the clear button points at that id with `aria-controls` while keeping its page-specific visible text. In the price filter panel of `products.html`, give the two range inputs `aria-label="Minimum price"` and `aria-label="Maximum price"` and the literal stable ids `price-min-range` and `price-max-range` (in `STABLE_IDS`, task 2.3), and render `<output for="price-min-range">` and `<output for="price-max-range">`; the sliders get no `name`, so the filter submit sends no extra parameters. Verify that `uv run pytest backend/tests/contract` passes (the snapshot contains the new attributes, equal in every stage), that the template scan (5.10) passes, that a contract test finds exactly one element with the `role="status"` attribute on `/`, `/products` and `/checkout` in every stage and exactly two `<output>` elements on `/products` and none on `/` and `/checkout` (so a page-wide role locator is unambiguous on `/` and `/checkout` and must not be used on `/products`), that the same test asserts in every stage, on `/`, `/products` and `/checkout`, that `nav[aria-label="Primary navigation"] > button[aria-controls]` matches exactly one element (the mobile toggle) whose `aria-controls` resolves to the nav menu and which is not itself inside that menu, that the menu contains exactly one `button[aria-expanded][aria-controls]` (the account trigger) whose `aria-controls` resolves to the panel that carries `hidden` and contains `button[aria-label="Log out"]`, that `button[aria-haspopup="dialog"]` matches exactly one element inside the nav (the "Log in" button, which carries no `aria-expanded`) and exactly one direct child of `body` (the chat launcher, whose `aria-controls` resolves to the chat widget) — so the documented lookups of 8.2, 8.3 and 8.4 can never resolve to the wrong element — that the trigger and the theme toggle each contain exactly one `<span>`, that on `/checkout` `form[action="/checkout"]` has exactly one direct-child `<p>` — the signed-in note of 8.3 — which itself contains exactly one `<span>`, and that the three account lists and the search results region are found by their accessible names, and that `uv run pytest -m browser -k stage1` still passes.
- [x] 8.2 Rebind `backend/app/static/app.js` for the cart badge (`nav a[href="/cart"] [aria-live]`), add to cart (`button[data-product]` with `data-product-name`, confirmation naming the product, for example "Pulse Bio Ring added to cart."), mobile navigation (the nav is `document.querySelector('nav[aria-label="Primary navigation"]')`, the toggle is `nav.querySelector(":scope > button[aria-controls]")` — the nav's only direct-child button, 8.1 — and the menu is `document.getElementById(toggle.getAttribute("aria-controls"))`) and the theme toggle (`button[aria-pressed]` in the primary navigation, label `toggle.querySelector("span")`). In `addToCart`, delete the dead `button.dataset.quantity` read and post the constant `quantity: 1` — no template renders `data-quantity`, so the current read already always falls back to 1, and the analytics payload keeps its `quantity` field — and derive the analytics `source` from the button's own context instead of `button.closest("[data-test='product-card']")`, which 9.4 rejects. Collapse the `productName` fallback chain in the same function to `button.dataset.productName` with a plain default string: its two fallback steps are the other `button.closest("[data-test='product-card']")` and a `.product-card__title a` lookup, both of which 9.4 rejects, and every button this handler serves already carries `data-product-name` (the card macro on both card variants, the hero button in `product_detail.html`, and the search fragment through the same macro, 9.1). `updateThemeToggleUI` writes only `aria-pressed`, the toggle's `aria-label` and the label span's `textContent`, and no longer swaps the icons: add rules to `backend/app/assets/styles.css` that show the sun or the moon path from `body.theme-dark` and the uncovered `theme-toggle` block, so no covered class and no script write is involved (8.1 drops the moon path's static `hidden`). The two `<path>` elements need a hook of their own for that, because `data-icon-sun` and `data-icon-moon` are the only thing telling them apart today and 8.5 removes them from the templates and rejects them in `backend/app/assets/styles.css`: give the two paths in `base.html` stable uncovered classes of the `theme-toggle` block (for example `theme-toggle__icon--sun` and `theme-toggle__icon--moon`), which the template scan of 5.10 accepts because they are not covered names and 8.5 leaves untouched because it removes only `data-*` markers. Neither rule then contains a covered class token, so the per-stage rewrite of 6.1 leaves both byte-identical in every variant. Rebind `showFlash`, which serves the add-to-cart confirmation and errors, the sign-in welcome, the sign-out notice and search errors: find the region with `document.querySelector('[role="status"]')` instead of `#flash-message`, remove the `FLASH_ID` constant, and never assign `className`; show and hide with `classList.add/remove("is-visible")`, and rename the variants to the state classes `is-success`, `is-error` and `is-info`, set with `classList` and all cleared on hide. Change the `.flash.success` and `.flash.error` rules in `backend/app/assets/styles.css` to `.flash.is-success` and `.flash.is-error`. Replace the `dataset.mobileNavBound` and `dataset.themeBound` guards with a module-level `WeakSet`. JS state is kept only in `is-*`, `hidden` and `aria-*`. Extend the add-to-cart smoke test, which clicks a product card's add-to-cart button on `/` (not on `/products`, where the price filter's two `<output>` elements expose the implicit role `status` as well, 8.1): the confirmation, located with `get_by_role("status")`, contains the product name, and after it hides, the region's class list (read with `evaluate` on that role-located element) equals the class list recorded right after page load (so no stage-1 `flash` class comes back in stages 2 and 4). Keep the bare role locator rather than `filter(has_text=…)`: the region is empty right after page load, so the baseline recording must resolve it while it has no text. Verify that `uv run pytest -m browser -k "add_to_cart or mobile_nav or theme"` passes for `stage1`–`stage4` (scenario "Add to cart in stage 3"), including that `get_by_role("status")` resolves to exactly one element on the page the flow uses and that exactly one of the two theme icons is visible before and after the toggle in every stage (the assertion 7.3 already makes, now served by the stylesheet), and that `grep -nE "flash-message|FLASH_ID|flash\.className|dataset\.(mobileNav|theme)Bound|dataset\.quantity|data-icon-|data-theme-label|closest\(.\[data-test" backend/app/static/app.js` returns nothing (the remaining `className` assignments in script-built markup go away in 9.3 and are banned by 9.4).
- [x] 8.3 Rebind the sign-in modal, account menu and checkout autofill in `app.js`. The "Log in" button is `nav[aria-label="Primary navigation"] button[aria-haspopup="dialog"]` (the chat launcher carries the same attribute but sits outside the nav). The overlay is `document.getElementById(loginButton.getAttribute("aria-controls"))`, which also works while the button is hidden for a signed-in user; open and close by toggling `overlay.hidden`; the dialog is `overlay.querySelector('[role="dialog"]')`, its form `dialog.querySelector("form")`, the alert `form.querySelector('[role="alert"]')`, and the close button is found by its `aria-label`; a backdrop click is `event.target === overlay`, replacing `matches("[data-auth-modal]")`; the chat Escape guard in `handleChatKeydown` uses the same overlay lookup and checks `!overlay.hidden`. The login form fields are `form.elements.email` and `form.elements.password`. Name the account-menu lookups that replace the `data-auth-*` markers removed in 8.5 (hooks added in 8.1): the nav menu is resolved exactly as in 8.2 (`navToggle = nav.querySelector(":scope > button[aria-controls]")`, `navMenu = document.getElementById(navToggle.getAttribute("aria-controls"))`) and the trigger is `navMenu.querySelector('button[aria-expanded][aria-controls]')`, which is unique in every stage because the nav's own toggle carries the same two attributes but is a direct child of `<nav>` and therefore outside the menu, the theme toggle carries `aria-pressed` and not `aria-expanded`, and the "Log in" button carries `aria-haspopup="dialog"` and `aria-controls` but no `aria-expanded` (8.1); a page-wide `[aria-expanded][aria-controls]` lookup is forbidden here, because `app.js` also writes `aria-expanded` on the chat launcher and on the search combobox input. The panel is `document.getElementById(trigger.getAttribute("aria-controls"))`, the user name is `trigger.querySelector("span")`, the three lists are `panel.querySelector('ul[aria-label="Saved addresses"|"Payment methods"|"Recent orders"]')` and the logout button is `panel.querySelector('button[aria-label="Log out"]')`. Opening and closing sets `trigger.aria-expanded` and `panel.hidden`; the signed-in and signed-out states toggle `hidden` on the "Log in" button and on the trigger, so the `[data-auth-region]` and `[data-auth-menu]` wrappers need no replacement hook; the outside-click close becomes `!panel.contains(event.target) && !trigger.contains(event.target)`, replacing `!event.target.closest("[data-auth-region]")`. The checkout form is `form[action="/checkout"]` with `form.elements.email|name|address|team_size`; the signed-in note is that form's only direct-child `<p>` (`form.querySelector(":scope > p")`; the form's other direct children are its three `fieldset`s, the alert `div` and the submit button, and the stage 4 `checkout-field` layout regroups only the form's fields, so no stage adds a second one), it keeps its whole sentence, and the user's name is written into the note's only `<span>` — the same "only `<span>`" pattern as the account trigger (8.1) — so both `[data-auth-note]` and `[data-auth-note-name]` have a replacement hook; `hidden` on the note keeps carrying the signed-in state. Verify that `uv run pytest -m browser -k "sign_in or account_menu or logout or autofill"` passes for `stage1`–`stage4`, including closing the dialog by the close button and by the backdrop and leaving no visible overlay afterwards (scenario "Checkout autofill in stage 4"), that after autofill the signed-in note is visible and its text contains the user's full name *and* the rest of its sentence, so writing the name did not replace the note's own text, and that this task extends the account-menu flow of 7.3 with the two assertions deferred there — the trigger's `aria-expanded` flips to `true` on open and back to `false` on close, and the three lists located by their accessible names "Saved addresses", "Payment methods" and "Recent orders" (hooks from 8.1) are non-empty — so that the menu opens, lists the three labelled sections and signs out in every stage.
- [x] 8.4 Rebind the chat widget (the launcher is `document.querySelector('body > button[aria-haspopup="dialog"][aria-controls]')` — a direct child of `body`, unlike the "Log in" button, which carries the same pair inside the header nav (8.1) — and the widget is `document.getElementById(launcher.getAttribute("aria-controls"))`; `app.js` keeps writing `aria-expanded` on the launcher, which is why it is never sought through a page-wide `[aria-expanded][aria-controls]` selector, `textarea[name="question"]`, close button `widget.querySelector('[aria-label="Close AI assistant"]')`, message list `widget.querySelector('[role="log"]')`, form `textarea.form`), the search form, input, clear button and results region (`form[role="search"]`, `input[name="query"]`; the region is `document.querySelector('[role="region"][aria-label="Search results"]')`, its clear button is `region.querySelector('button[aria-controls]')`, the results container is `document.getElementById(clearButton.getAttribute("aria-controls"))`, and the region itself is shown and hidden with `hidden`; the empty state comes from the server fragment (9.1), so `[data-search-empty]` needs no replacement hook), and the typeahead anchor (the input's enclosing `label`, `input.closest("label")`, instead of `.form-field`) in `app.js`. Replace the `dataset.chatBound` and `dataset.chatPromptBound` guards with a `WeakSet` and delete the `widget.id = "chat-widget"` fallback (the id comes from the template, 5.7). Rebind the product filter: the form is `document.querySelector('input[name="price_min"]').form`; the hidden inputs are `form.elements.price_min` and `form.elements.price_max`; the sliders are `input[type="range"][aria-label="Minimum price"]` and `[aria-label="Maximum price"]`; each output is `output[for]`, and its slider is `document.getElementById(output.htmlFor)`; the reset defaults come from the sliders' own `min` and `max` attributes (`minSlider.min`, `maxSlider.max`), which the template renders from the same price bounds, so the wrapper's `data-price-min-default` and `data-price-max-default` go away with the other `data-price-*` attributes (8.5); chip state stays on `.chip--selectable`, which does not drift. Replace `chat-message--loading` with `aria-busy="true"` on the pending message, set and cleared with `setAttribute`/`removeAttribute` and never through `className` or a modifier of the covered `chat-message` block (design Decision 4), and move both stylesheet rules that target removed hooks in `backend/app/assets/styles.css` to their semantic replacements: `[data-search-form].is-loading` to the search form's hook, and `.chat-message--loading::after` to `[role="log"] [aria-busy="true"]::after` with its four declarations unchanged (`content: "Thinking..."`, `display`, `color`, `font-size`), so the pending indicator keeps rendering. The `[role="log"]` scope is required, not decorative: `addToCart` sets `aria-busy` on the add-to-cart button (8.2) and the stylesheet already has a `.button[aria-busy="true"]` rule, so an unscoped `[aria-busy="true"]::after` would print "Thinking..." on that button. Neither replacement selector contains a class token, so the per-stage rewrite of 6.1 leaves both byte-identical in every variant. Add `test_price_filter` to `backend/tests/browser/test_drift_smoke.py` with the `stage` fixture: on `/products`, press ArrowRight on `get_by_role("slider", name="Minimum price")`, assert that the minimum price output text changes — read through the slider's `output[for="price-min-range"]`, never through a page-wide `get_by_role("status")`, which matches the two outputs and the confirmation region on that page (8.1) — click "Apply filters" and assert that the URL contains the new `price_min`. Verify that `uv run pytest -m browser -k "chat or search or price_filter"` passes for `stage1`–`stage4`; that `grep -rn "chat-message--loading\|data-search-form" backend/app/static/app.js backend/app/assets/styles.css backend/app/templates` returns nothing (the stylesheet is the case the script scan of 9.4 cannot catch, because it is the rewrite source and legitimately carries covered names); and that a unit test extending the 6.1 variant tests asserts that every stage variant contains exactly one `[role="log"] [aria-busy="true"]::after` rule with the `content: "Thinking...";` declaration and no `--loading` selector.
- [x] 8.5 Remove the behavior-only markers from the templates and the stylesheet in every stage: `data-cart-count`, `data-auth-*`, `data-chat-toggle|widget|close|form|messages`, `data-search-*`, `data-nav-*`, `data-theme-*`, `data-icon-*`, `data-filter-form`, `data-price-slider`, `data-price-output`, `data-price-min-default`, `data-price-max-default`, `data-event`, `data-product-id` and `data-product-wrapper` (design Decision 4). The per-page "No products matched…" paragraphs go with `data-search-empty`, because the server fragment renders that state now (9.1). Keep the content data attributes (`data-product`, `data-product-name`, `data-category`, `data-chat-prompt`). Extend `backend/tests/unit/test_template_scan.py` to reject the removed markers in `backend/app/templates/**` and `backend/app/assets/styles.css`. Verify that the scan test passes, that `grep -nE "dataset\.[A-Za-z]+Bound|widget\.id *=" backend/app/static/app.js` returns nothing, that `grep -rn "data-price-\|priceMinDefault\|priceMaxDefault" backend/app/templates backend/app/static/app.js backend/app/assets/styles.css` returns nothing, that `grep -rnE "data-auth-|data-search-|data-theme-|data-icon-|data-chat-(toggle|widget|close|form|messages)" backend/app/templates backend/app/static/app.js` returns nothing (every removed marker has the replacement hook named in 8.1–8.4, so no reader is left behind), and that `uv run pytest -m browser` passes for `stage1`–`stage4`.

## 9. Search results and script-built markup (design Decision 6, A3)

- [x] 9.1 Add the hidden route `GET /search/results?query=` (`include_in_schema=False`, depends on `workshop_view`) to `backend/app/main.py`. It uses the same `ProductService.search_products` as `/api/search/` and renders `backend/app/templates/components/search_results.html` (new) with the product-card macro. The fragment renders no grid wrapper of its own, or renders it through `drift.cls("product-grid")`, so that covered class never reaches the markup literally (5.10), and it renders the no-results state itself ("No products matched your query."), so `app.js` needs no empty-state hook. Verify by adding `/search/results?query=desk` to `COVERED_PAGES`; the contract tests pass; a contract test asserts that the fragment shows name, price and "Add to cart" for each result, that a query with no matches returns the empty-state text and no card, and that with `BUG_MISSING_BUTTON` the Atlas Standing Desk card (product 5) has no add-to-cart button in stages 1–4.
- [x] 9.2 Replace `renderProductCards` in `app.js` with inserting the `/search/results` fragment, which also removes the extra icon-only add-to-cart button. `performAndRenderSearch` fetches `/search/results?query=` + `encodeURIComponent(query)` (the typeahead keeps using the JSON `/api/search/suggest`), reads `await response.text()` and inserts it without `innerHTML`: `list.replaceChildren(document.createRange().createContextualFragment(html))`, which parses in body context, so the fragment's `<article>` cards land as real nodes. The empty and clear paths use `list.replaceChildren()` instead of `innerHTML = ""`, and emptiness is decided from the rendered fragment, so the assignment that 9.3 forbids is never introduced. Do not route the fragment through a `<template>` whose `innerHTML` is assigned; a `DOMParser` plus `replaceChildren(...doc.body.children)` is an acceptable equivalent, but pick one. Remove `renderProductCards`, and `formatCurrency` if nothing else uses it. Verify that `grep -n "renderProductCards" backend/app/static/app.js` returns nothing, that `grep -n "innerHTML" backend/app/static/app.js` reports no line in the search code path, and that a smoke test searching "desk" on `/`, then adding the first result to the cart, passes for `stage1`–`stage4`.
- [x] 9.3 Render `<template>` elements with `drift` hooks inside their components for the suggestion dropdown and options (listbox id from `drift.id`), chat messages and account-menu list items. Every component the script clones from holds exactly one `:scope > template` child — the typeahead anchor `label` (the dropdown template), the listbox inside that dropdown (the option template) and each of the three account-menu `<ul>` elements (8.1) — with one exception: the chat log (`[role="log"]`, 8.1) holds exactly two `<template>` children in a fixed document order, the user message shell first and the assistant message shell second, both before the server-rendered welcome message. Each shell renders its root element through `drift.cls("chat-message")` plus `drift.cls("chat-message--user")` or `drift.cls("chat-message--assistant")`, so the covered modifiers follow the block rename (2.4) and are styled by the per-stage stylesheet (group 6) instead of being written from the script, which 9.4 forbids. In `app.js`, clone templates via `:scope > template`, selecting by position where a component holds more than one — `appendChatMessage(role, …)` reads `log.querySelectorAll(":scope > template")` once and takes index 0 for `"user"` and index 1 for `"assistant"` (equivalently `:scope > template:nth-of-type(1)` and `(2)`), and bails out if the list does not hold exactly two — fill them with `textContent`, and remove `uniqueId` and every `innerHTML` use: the suggestion dropdown's `hide()` and `renderSuggestions` clear with `list.replaceChildren()` instead of `innerHTML = ""`, and the account menu's empty-state list items ("No saved addresses yet.", "No saved payment methods.", "No orders yet.") are built with `document.createElement("li")` and `textContent` instead of `innerHTML` string literals, so no `innerHTML` assignment is left anywhere in the file. The pending answer is an assistant clone carrying `aria-busy="true"` (8.4); when the answer or the error text arrives, that same node is reused — `aria-busy` removed, `textContent` set — so today's `wrapper.className = "chat-message chat-message--<role>"` and `loadingEl.classList.add("chat-message--assistant")` both disappear with no replacement and no `chat-message*` literal is left in `app.js`. No template carries a marker attribute or an id of its own, which the live DOM check of 9.5 would reject anyway. Suggestion options get no `data-index`; the selected option's index is its position among the listbox's `[role="option"]` children. Keep the query highlight without `innerHTML`: split `item.name` around case-insensitive matches of the trimmed query, append plain text nodes for the parts that do not match, and wrap each match in `document.createElement("mark")` with its `textContent` set; the category keeps plain `textContent`. Remove `highlightMatch`, and `escapeHtml` if nothing else uses it. Verify that `grep -nE "uniqueId|innerHTML|dataset\.index|highlightMatch" backend/app/static/app.js` returns nothing (a flat ban now that 9.2 and 9.3 removed the last uses; the earlier `innerHTML *= *[^"]` pattern also matched `innerHTML = ""` through the zero-width ` *`); that a contract test asserts, for stages 1–4, that `[role="log"]` has exactly two direct-child `<template>` elements, that each other template-bearing component named above has exactly one, that the two log templates' content roots carry different `class` attributes equal to that stage's `drift`-resolved `chat-message` + `chat-message--user` (first) and `chat-message` + `chat-message--assistant` (second) — compared against `drift.cls` output for the stage, never hard-coded — and that the second one equals the `class` attribute of the server-rendered welcome message in the same stage, so a reordering or a missing variant fails; that `uv run pytest -m browser -k "search or sign_in or chat"` passes for `stage1`–`stage4`, the chat flow asserting in every stage that after a question the user bubble and the assistant reply carry different `class` attributes, each equal to the corresponding template root's `class` attribute in the server-rendered HTML of the same page and stage (read with `evaluate` on the role-located log; class names are not drift hooks, design Decision 8), and that `aria-busy` is absent from the reply once it renders; and that after typing "desk", the first `get_by_role("option")` contains a `mark` element whose text equals "desk" case-insensitively (read with `evaluate` on the role-located option; `mark` is not a drift hook) in `stage1`–`stage4`, which keeps `docs/test-prompts/homepage-and-search.md` true.
- [x] 9.4 Add `backend/tests/unit/test_script_scan.py` (new). It fails if `backend/app/static/app.js` contains: any stage-1 name of a covered id or class, including the `block__*` and `block--*` names derived from a covered block (the same derivation rule as the template scan of 5.10, which is what rejects `chat-message--user` and `chat-message--assistant`) — matched, like 5.10, as a whole token and never as a substring, and only inside string literals, so the stable JS state class `"site-nav-open"` stays allowed although the covered block name `site-nav` is a prefix of it, and a local variable named `badge` or `flash` is not mistaken for the covered classes of the same name; `data-test`; a removed behavior marker (including its `dataset` camelCase form); a `dataset.<name>` property or `data-<name>` string literal outside the content data attributes (`product`, `productName`, `category`, `chatPrompt` and their `data-` forms); a runtime write of a data attribute or id (`dataset.<name> =` or `dataset[…] =` as an assignment, not `==` or `===`; `setAttribute`, `toggleAttribute` or `removeAttribute` with a `data-` name; `Object.assign(<x>.dataset`; `.id =`; `setAttribute("id"`); any `className` assignment; or any `innerHTML` use (9.2 and 9.3 leave none), so the bans live in a permanent test and not only in one-off greps. Verify that it passes and that its negative fixtures are reported: `"#auth-email"`, `"flash-message"`, `el.dataset.chatBound = "true"`, `button.dataset.quantity`, `widget.id = "x"`, `el.setAttribute("data-x", "1")`, `wrapper.dataset.priceMinDefault`, `wrapper.classList.add("chat-message--user")`, `el.className = "flash"`, `el.innerHTML = "<li>x</li>"` and `list.innerHTML = html`; and that the positive fixtures `document.body.classList.toggle("site-nav-open", true)` and `const badge = nav.querySelector("[aria-live]")` are not reported.
- [x] 9.5 Add the live DOM check to `backend/tests/browser/test_drift_smoke.py` (design Decision 8). After each smoke flow in each stage, collect every attribute name and id in the document with `page.evaluate`. Every `data-*` name must be a content data attribute or `data-test`, and `data-test` only in stages 1 and 2. Every id must also appear in the server-rendered HTML of the same page and stage, including `<template>` content (fetched with `page.request.get` and parsed with `beautifulsoup4`). Verify that `uv run pytest -m browser` passes for `stage1`–`stage4`, and that the check fails when a test first runs `document.body.dataset.probe = "1"` or adds an element with a new id (negative case, marked `xfail(strict=True)`).

## 10. Checkout summary and BUG_CHECKOUT_TOTAL

- [x] 10.1 Add `checkout_summary(cart_state)` to `backend/app/services/order_service.py`. It returns `subtotal`, `tax` and `total` from `calculate_totals(cart_state["items"])` with the same default tax rate as `OrderService.create_order`. Verify with a unit test in `backend/tests/unit/test_order_service.py`: two items at 39.50 give 79.00 / 5.53 / 84.53, and an empty cart gives zeros.
- [x] 10.2 Pass `summary = bugs.checkout_total(checkout_summary(cart_state))` from `GET /checkout` and from both `POST /checkout` renders in `backend/app/main.py`. In `checkout.html`, show Subtotal, Shipping ("Complimentary"), Tax (the amount) and Total, with drifting `checkout-summary*` hooks and `data-test` on the totals. Verify by adding `.checkout-summary__total` (breaks in stages 2 and 4) and the total's `data-test` entry (for example `[data-test='checkout-total']`, breaks in stages 3–4) to the coverage oracle, and with `backend/tests/contract/test_checkout_totals.py` (new): with items, the displayed total equals the displayed subtotal plus tax; "Calculated after address" no longer appears; the `/checkout` snapshot is equal across stages.
- [x] 10.3 Cover the scenario "Summary matches the order" in `test_checkout_totals.py`. Request the root harness fixture `fake_weasyprint`, which yields the HTML strings rendered at checkout in render order. Place an order through `POST /checkout`, and assert that the subtotal, tax and total shown on the checkout page before submission equal the created order (read by the order number from the success message) and the amounts in the recorded invoice HTML (the entry whose `<title>` is `Invoice <order number>`). Verify that the test passes with `BUG_CHECKOUT_TOTAL` off.
- [x] 10.4 Cover the `BUG_CHECKOUT_TOTAL` scenarios in `test_checkout_totals.py`. With the flag on and a cart containing only the cheapest product, the displayed total differs from the displayed subtotal plus tax, while subtotal and tax are correct ("Inconsistent summary"). After placing the order, the order total equals its subtotal plus tax, and the invoice HTML recorded by `fake_weasyprint` shows the correct amounts ("Order stays correct"). Repeat in stages 1–4. Verify that the tests pass.

## 11. BUG_SLOW_RESPONSE scope

- [x] 11.1 Add the dependency `planted_delay("catalogue")` in `backend/app/core/workshop.py` and attach it to `GET /products` (`backend/app/main.py`) and `GET /api/products/` (`backend/app/api/products.py`). When the bug is active, it awaits `asyncio.sleep(random.uniform(1.0, 3.0))` after `get_effective_flags` resolves and before the route's own database queries (design Decision 11 covers the interim seam's connection use). Verify with a contract test that patches `asyncio.sleep`: exactly one recorded sleep between 1 and 3 s for `/products` and for `/api/products/`, and none for `/`, `/products/3`, `/cart`, `/checkout`, `/api/cart/` and `/search/results?query=desk`.
- [x] 11.2 Cover the scenario "Slow catalogue" with real sleeps in `backend/tests/contract/test_planted_bugs.py` (new): with `BUG_SLOW_RESPONSE` enabled, `/products` takes at least 1.0 s and the following `/cart` request takes less than 1.0 s. Verify that the test passes.

## 12. Control endpoints not advertised

- [x] 12.1 Pass `include_in_schema=False` for the `workshop` and `admin` routers in `configure_routes()` (`backend/app/main.py`). Verify with `backend/tests/contract/test_schema_visibility.py` (new): `/openapi.json` lists no path under `/api/workshop/`, `/api/admin/`, `/search/results` or `/assets/`, while `/api/products/` is still listed; `/docs` returns 200; `POST /api/workshop/preset` with `{"preset": "stage2"}` returns `success`, and `/api/workshop/status` then reports `v2` ("Facilitator applies a preset"); `GET /api/admin/flags` returns 200.

## 13. Drift mapping documentation

- [x] 13.1 Rewrite `docs/WORKSHOP-FEATURES.md` with the sections and exact headings of design Decision 14, in this order:
  - `## Stages and precedence`;
  - `## Stable contract`: visible text, roles, accessible names, labels, field `name`, form `action`/`method`, hyperlink `href`, content order, the content data attributes `data-product`, `data-product-name`, `data-category` and `data-chat-prompt`, JS state classes, the uncovered shared blocks `form-field` and `button` with its modifiers (`button--primary`, `button--ghost`, `button--text`, `button--lg`), `STABLE_IDS` (the skip-link target `main-content`, the price range ids and the newsletter form ids), and the header shape the script binds to (the mobile navigation toggle is the only direct-child `button` of `nav[aria-label="Primary navigation"]` and sits outside the menu it controls, the account trigger sits inside that menu, and the chat launcher is a direct child of `body`; design Decision 3);
  - `## Drift mapping`: one table per covered flow under a `###` flow heading, with the columns `Kind | Stage 1 | Stage 2 | Stage 3 | Stage 4` (`—` = unchanged, kinds `id|class-block|class-exact|data-test|layout`), including one `layout` row per stage 4 component (`product-card`, `product-hero-actions`, `checkout-field`). The listing-and-cards table carries a `class-exact` row for `product-card__add` (`btn-main` in stage 2) and no `button--primary` row, and the new id rows `account-panel` and `search-results` plus a `class-block` row for `search-results` (the section class and the container id share the stage-1 name but drift to different replacements, task 5.3), matching the `StageSpec` tables that 13.2 compares against;
  - `## Planted bugs`: one table with the columns `Flag | Flow | Trigger | Defect`, one row per registry bug in registry order;
  - `## Presets`: the flags each preset owns, with `drift_and_bug` as stage 4 plus `BUG_WRONG_PRICE` and `BUG_CHECKOUT_TOTAL`;
  - `## Control endpoints`: hidden from the schema, still callable, responses readable and naming active bugs, with a link to `## Workshop spaces` for space scoping, reset and the shared-mode 401.

  Keep an existing `## Workshop spaces` section from `workshop-spaces` unchanged as the last section. If `workshop-spaces` is not merged yet, leave room for it (added in 15.1). Remove the `data-event` fallback example and the card-only exercises. Verify that `grep -nE "data-event|Workshop Mode Active|data-price-" docs/WORKSHOP-FEATURES.md` returns nothing; that `grep -E "^## " docs/WORKSHOP-FEATURES.md` prints the six headings above in that order, followed by `## Workshop spaces` once it is present; and that the presets section names all eight presets. If `workshop-spaces` is merged, also verify that `awk '/^## Workshop spaces$/{f=1;next} /^## /{f=0} f' docs/WORKSHOP-FEATURES.md | grep -c '^[[:space:]]*|'` prints `0`.
- [x] 13.2 Add `backend/tests/contract/test_docs_sync.py` (new). It parses only the tables under `## Drift mapping` and `## Planted bugs` in `docs/WORKSHOP-FEATURES.md` (design Decision 14), not by position or by the file's total table count, and asserts that: the mapping rows, including the `layout` rows, equal the `StageSpec` tables in `backend/app/core/workshop.py` (no missing and no stale rows); every id and class token removed or added between stage 1 and stage N on every page in `COVERED_PAGES` is explained by a row, taking block rules into account; the bug table equals `PLANTED_BUGS`. Verify that it passes against the rewritten document, that its negative case (a temporary copy with one mapping row deleted) fails (scenario "Documentation check"), and that a temporary copy with an extra unrelated section containing its own table, or with a prose `## Workshop spaces` section after `## Control endpoints`, still passes.

## 14. Contract suite completion

- [x] 14.1 Extend `backend/tests/contract/test_stage_contract.py` to the full matrix: every page in `COVERED_PAGES` (including `/cart` and `/checkout` with items and empty, the empty-cart and success renders of `POST /checkout`, and `/search/results?query=desk`) × stages 1–4 × bug sets {none, `buggy`}, with `planted_delay` patched to zero. Assert snapshot(stage N, `buggy`) == snapshot(stage 1, `buggy`). The `POST /checkout` success render is posted with `fake_weasyprint` active, with identical form data (name, email, address) and an identical cart in every case, and only that render is compared with `ORDER_RESULT_MASK`; before masking, assert that it contains exactly one order number matching `ORD-[0-9A-F]{8}`, that this number resolves in the database to the order the request created, and that both document hrefs carry that order's id. Add the flow-completeness assertion: every flow in the spec's "Flow coverage" requirement has at least one `[data-test='…']` coverage-oracle entry that breaks in stages 3–4 and at least one id or class entry that breaks in stage 2. Verify that `uv run pytest backend/tests/contract/test_stage_contract.py` passes ("Bugs combine with drift").
- [x] 14.2 Add `backend/tests/contract/test_stage_scenarios.py` (new) with the named scenarios. "Stage 2 keeps semantics": the `/checkout` visible text, roles, accessible names, field labels and names are equal to stage 1, while the ids of the checkout form fields (`checkout-email|name|address|team-size|notes`) and the `checkout-form` class differ from stage 1, and each label's `for` still points to its field's id. "Stage 3 removes test hooks": `/products` has no `data-test` attribute anywhere, and each card shows the same name, price and "Add to cart" text as in stage 1. "Stage 4 restructures": the card wrapper chain differs from stage 1, and for every card on `/products` that contains a `button[data-product]` (in a clean flag state that is every card of the listing grid; the category-preview cards of that page render no add-to-cart button in any flag state, because `products.html` calls the macro with `show_actions=False`, so they are outside this check) both stable locators still find its add-to-cart button inside that card — exactly one descendant with button role whose normalized visible text is "Add to cart" (the locator the spec scenario names) and exactly one descendant with button role whose accessible name is "Add <product name> to cart" — and the two resolve to the same element, so stage 4 cannot move the visible text out of the button into a role-less wrapper. Assert that the set of checked cards is not empty, so the check cannot pass vacuously, and assert the same two locators in a stage 1 render. "Stage 4 restructures checkout": on `/checkout` each field found by its `name` has a different ancestor tag chain than in stage 1, and its label still names it. Verify that the tests pass.
- [x] 14.3 Add `backend/tests/contract/test_presets_api.py` (new), which goes through the real endpoints. "Switching stages": `stage2` then `stage3` via `POST /api/workshop/preset` gives status `locator_stage == "v3"`, and every page in `COVERED_PAGES` has the same hooks as an overridden stage-3 render. "Conflicting flags": `LOCATOR_V2` and `LOCATOR_V4` via `POST /api/workshop/flags` render stage 4 hooks on every page, including the product card. "Buggy preset": `active_bugs` equals the five registry flags. "Clean preset" and a freshly seeded database report no active bugs and `locator_stage == "v1"`. "Stage preset keeps active bugs": `stage1` applied after `buggy` leaves all five bugs active and the status reports `v1`. Verify that the tests pass.
- [x] 14.4 Add the catalogue scenarios to `backend/tests/contract/test_planted_bugs.py`, repeated in stages 1–4. "Missing button": product 5's card has no add-to-cart button and product 4's card has one. "Price mismatch is detectable in one run": with `BUG_WRONG_PRICE`, product 3's card on `/products` shows 1.15× the price, and after adding product 3 through `/api/cart/items`, `/cart` shows the actual unit price, and the `/checkout` summary and the order created through `POST /checkout` use the actual price. `BUG_BROKEN_LINKS`: product 4's card links to `/products/invalid-4`, and a request to that link returns a client error (assert `400 <= status < 500`; today a JSON 422 from the `int` path validation, and possibly an HTML 404 once `acceptance-conformance` adds its not-found page for WEB-003 AC-9, so do not pin the exact code) and contains no product detail content (no product 4 name heading and no add-to-cart button). With the flag off, product 4's card links to `/products/4`, which returns 200. Do not add an error handler in this change. Verify that the tests pass.
- [x] 14.5 Add the scenario "Heal-vs-hide preset" to `test_presets_api.py`: after `POST /api/workshop/preset` with `buggy` and then `drift_and_bug` (so the preset's absoluteness is observable), the status reports `v4` and exactly `BUG_WRONG_PRICE` and `BUG_CHECKOUT_TOTAL`, `/products` has the stage 4 hooks, product 3's card shows the wrong price, and the `/checkout` total for a non-empty cart does not equal subtotal plus tax. In the same responses, the stage-1 selectors of the bugged flows match nothing: `[data-test='product-price']`, `.product-card__price` and `[data-test='add-to-cart-btn']` on `/products`, and the checkout total's `data-test` selector, `.checkout-summary__total` and `#checkout-email` on `/checkout`; each of them matches in a stage 1 render with the same bugs. Product 3's card price and the checkout total are still found through role, label or text. Verify that the test passes.
- [x] 14.6 Add `backend/tests/contract/test_determinism.py` (new). For `/`, `/products` and `/checkout` in stages 2–4, two different session ids produce identical ids, classes, `data-test` values and tag structure. A fresh interpreter (a subprocess that imports the app and renders the same page, started with the contract conftest's `subprocess_env` fixture so it uses the temporary database and PDF directory) produces identical hook sets and stylesheet digests ("Repeated requests", restarts). Verify that the tests pass.
- [x] 14.7 Run the whole suite locally. Verify that `uv run pytest backend/tests` and `uv run pytest -m browser backend/tests/browser` both pass, and that the marker and `find` check from 4.1, taken around both runs, prints no database or order PDF file.

## 15. Rebase onto workshop-spaces

- [x] 15.1 After `workshop-spaces` is merged, rebase this branch onto it (design Decision 15). `get_effective_flags` becomes the space-aware dependency, and the interim body is deleted. Point `baseline_flags()` in `backend/app/core/feature_flags.py` at this change's sources: the seeded `FEATURE_FLAGS` defaults from `backend/app/seeds/seed_data.py` (bug rows generated from `PLANTED_BUGS`, task 3.2) with `build_presets()["clean"]` from `backend/app/core/workshop.py` applied on top, both still imported lazily inside the function. Resolve the expected conflicts:
  - in `backend/app/api/workshop.py`, status keeps `version` and `space` and uses `effective_stage` and `PLANTED_BUGS`, presets come from `build_presets()` and are written in one transaction through the space write path, and `reset` is kept;
  - in `backend/app/templates/base.html`, the space indicator and its hint stay outside `drift`;
  - in `backend/app/main.py`, the space cart keys and the `workshop_view` dependencies both remain;
  - the `.workshop-space` and `.workshop-space-hint` rules from `workshop-spaces` (its task 9.2) end up unchanged in `backend/app/assets/styles.css` (add them by hand if rename detection did not carry them), and `backend/app/static/styles.css` does not come back;
  - in `docs/WORKSHOP-FEATURES.md`, keep drift-coverage's sections 1–6 and the `## Workshop spaces` section verbatim as the last section, and drop duplicate reset or 401 text from `## Control endpoints` in favor of a link.

  Run `backend/tests/spaces/` unchanged. Update `rendered_stage()` in `backend/tests/spaces/helpers.py` only if the `StageSpec` card block names differ from `item-card` and `product-tile`. Verify that `grep -rn "list_feature_flags" backend/app` returns nothing; that `grep -n "build_presets" backend/app/core/feature_flags.py` finds the baseline import; that a unit test in `backend/tests/unit/test_workshop.py` asserts that `baseline_flags()` contains every key of `build_presets()["clean"]` with that preset's value and every `PLANTED_BUGS` flag as false; that `test -f backend/app/static/styles.css` fails; that every stage's stylesheet variant contains the `.workshop-space` and `.workshop-space-hint` rules unchanged; that `grep -rn "static/styles.css" . --exclude-dir=.git --exclude-dir=.venv --exclude-dir=openspec` finds nothing outside the 404 assertion from 6.2 in `backend/tests/contract/` and the two files 15.6 (b) and (c) repair if `workshop-spaces` left a reference there (`backend/tests/spaces/test_space_resolution.py`, `loadtest/locustfile.py`; expected: none, so this grep normally already finds only the 404 assertion, and 15.6 re-runs it as the hard check); that `grep -n "^## Workshop spaces" docs/WORKSHOP-FEATURES.md` finds exactly one line and `grep -nE "data-event|Workshop Mode Active" docs/WORKSHOP-FEATURES.md` finds nothing; that `uv run pytest backend/tests/unit backend/tests/contract backend/tests/spaces` passes; and that `git diff --stat main -- backend/tests/spaces` is empty unless `helpers.py` had to change (the full suite is verified in 15.6).
- [x] 15.2 Add `[data-workshop-space]` with its text and the shared-mode hint `.workshop-space-hint` with its text to the stable contract (the template scan allow-list, the stable-contract table in `docs/WORKSHOP-FEATURES.md`, and the allowed `data-*` names of the live DOM check in 9.5). Verify with a contract test: pages requested with `X-Workshop-Space: alice` in stages 1, 2 and 4 contain the identical indicator element and text; with `settings.shared_mode` and `settings.admin_token` monkeypatched on and no space, the hint element and text are identical in stages 1, 2 and 4; and `test_docs_sync.py` still passes; `uv run pytest -m browser -k stage2` passes with a space header set.
- [x] 15.3 Extend `backend/tests/contract/test_determinism.py` to spaces: apply `stage2` in spaces `alice` and `bob` through `POST /api/workshop/preset` with `X-Workshop-Space`, and assert identical hook sets for `/products` and `/checkout`, while the `default` space still renders stage 1. Verify that the test passes.
- [x] 15.4 Extend `backend/tests/contract/test_schema_visibility.py`: `/openapi.json` does not list `/api/workshop/reset`, and `POST /api/workshop/reset` is still callable; the GET paths of the `fastapi.routing.APIRoute` entries in `app.routes` (without `{`) still include `/api/workshop/status`, `/api/workshop/presets`, `/api/admin/flags` and `/search/results`, although the schema hides them. Confirm that the no-enumeration test in `workshop-spaces`' `backend/tests/spaces/test_shared_mode.py` builds its GET list from `app.routes`, not from `/openapi.json`, and asserts that the list contains `/api/workshop/status`, `/api/workshop/presets`, `/api/admin/flags`, `/`, `/products`, `/cart` and `/checkout` before calling them. Change that test only if this is missing, and then only to that form. Verify that `uv run pytest backend/tests/contract/test_schema_visibility.py backend/tests/spaces/test_shared_mode.py` passes with the routers hidden.
- [x] 15.5 Make the browser `stage` fixture use its own space per stage through `extra_http_headers` (`X-Workshop-Space`). The cross-stage rule of 7.4 then applies inside that space: the aria-snapshot baseline is recorded in the stage fixture's own space by applying preset `stage1` there first, never in `default` and never in another stage's space, so the `[data-workshop-space]` indicator in the site header is identical on both sides of the comparison — the same rule 15.2 applies to the contract tests inside space `alice`. Writes from the browser context stay allowed in shared mode because the space is not `default` (`workshop-spaces` write guard). Verify that `uv run pytest -m browser` passes for `stage1`–`stage4` against one server; that `uv run pytest -m browser -k aria` passes for `stage1`–`stage4` with the per-stage spaces active and that `uv run pytest -m browser -k "aria and stage3"` passes on its own (the two-pass form makes each parametrization self-contained); that the aria test asserts the `Space: <the fixture's space id>` text is present and identical in the stage-N snapshot and in the stage-1 baseline, so a dropped or ignored `X-Workshop-Space` header cannot make the comparison pass for the wrong reason; and that `/api/workshop/status` of the `default` space still reports `v1` afterwards.
- [x] 15.6 Confirm that the `workshop-spaces` tests and tools work with drift (design Decision 15), and edit only what is missing:
  - (a) `backend/tests/spaces/test_page_flags.py` checks the seam with `NEW_CART_UI` in `response.context["feature_flags"]` and drift with `rendered_stage()` on `/products`. Only if it reads `LOCATOR_*` or `BUG_*` from `response.context["feature_flags"]`, replace that with `response.context["request"].state.workshop_view.stage` (2 under header `octocat` and 1 without a header, on all six page routes; for `POST /checkout`, the empty-cart 400 render).
  - (b) `backend/tests/spaces/test_space_resolution.py` checks `/static/app.js` and the stylesheet href linked from `/` (fetched without a space) with header `-bad--name-`. Only if it still requests `/static/styles.css`, switch it to those two URLs, asserting 200 and no `workshop_space` `Set-Cookie`.
  - (c) `loadtest/locustfile.py` fetches the assets linked from each loaded page under `asset:stylesheet`, `asset:script` and `asset:image`. Only if it still requests `/static/styles.css`, read the stylesheet `href` from each loaded page instead.
  - (d) `rendered_stage()` in `backend/tests/spaces/helpers.py` returns `v1`–`v4` for `/products` under presets `stage1`–`stage4` (any update happens in 15.1).
  - (e) Add `backend/tests/contract/test_space_assets.py` (new): with `?space=octocat`, `/` returns 200 with a `workshop_space` cookie and links a stylesheet that also returns 200.

  Verify that `uv run pytest backend/tests` passes, including `backend/tests/spaces`; that the pull request lists which of (a)–(c) needed an edit (expected: none); that `grep -rnE 'feature_flags"\]\["(LOCATOR|BUG)' backend` returns nothing; that `grep -rn "static/styles.css" . --exclude-dir=.git --exclude-dir=.venv --exclude-dir=openspec` now finds only the 404 assertion from 6.2 in `backend/tests/contract/`, with (b) and (c) applied if they were needed; and that the `workshop-spaces` headless locust run (its task 13.2 command) exits 0 with `asset:stylesheet` rows and no 404 failures.

## 16. CI test job (design Decision 16, A5)

- [x] 16.1 Make sure the `check` job in `.github/workflows/image.yml` runs the unit and contract tests (`uv run pytest backend/tests`, with the browser marker deselected by default). Verify that `uv run pytest backend/tests --collect-only -q | grep contract` lists the contract tests and that the job is green on a pull request run.
- [ ] 16.2 **Moved to `workshop-rollout` task 1.3** (not done here; the maintainer archived this change before rollout). Add drift-coverage's verification to `.github/workflows/image.yml` on `reproducible-image`'s job graph (design Decision 16; `reproducible-image` D7: `build` pushes the candidate by digest with no tag, and only `publish` creates `sha-<short>`, `edge`, `X.Y.Z` and `workshop-<id>`).
  - (a) Pull requests: at the end of `build`, after the `tools/smoke_image.py` `recreated` phase, add steps guarded by `if: github.event_name == 'pull_request'` that run against that same running container of the locally loaded amd64 image (`-p 9090:9090`): pinned `astral-sh/setup-uv`, `uv sync --locked`, `uv run playwright install --with-deps chromium`, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090`, and `docker logs` on failure.
  - (b) Other events: add a `test` job with `needs: build`, `if: github.event_name != 'pull_request'`, `runs-on: ubuntu-24.04`, `permissions` `contents: read` and `packages: read`, and actions pinned by full SHA. It checks out the commit, runs pinned `setup-uv` and `uv sync --locked`, and runs `uv run pytest backend/tests/unit backend/tests/contract`. It then logs in to GHCR read-only (needed while the package is private, until `reproducible-image` task 10.2), runs `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}`, and waits for `healthy`, printing `docker logs` and failing after 90 seconds. Finally it runs `uv run playwright install --with-deps chromium` and `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090`.
  - (c) Change `publish` from `needs: [build, smoke]` to `needs: [build, smoke, test]` and append `&& needs.test.result == 'success'` to its condition, giving `if: ${{ !cancelled() && github.event_name != 'pull_request' && needs.build.result == 'success' && needs.smoke.result == 'success' && needs.test.result == 'success' }}`. `acceptance-conformance` task 6.2 later adds `conformance`. A failing `test` therefore leaves the candidate with no tag at all, `sha-<short>` included.

  Verify that actionlint reports no errors. Verify that on a pull request run the `build` job's browser steps show the `stage1`–`stage4` parametrizations green, `test` and `publish` are skipped, and GHCR gained no package version. Verify that on the first `main` run after the merge, `test` runs the image at the `build` job's `digest` output and is green, `publish` starts only after `test` finished, and `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short>` shows that digest. Verify that a failing `test` blocks every tag: on a scratch branch, push a commit that makes one browser smoke assertion fail on purpose and dispatch the workflow (`gh workflow run image.yml --ref <branch>`). `check`, `build` and `smoke` pass, `test` fails, `publish` is skipped, `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short of that commit>` fails because the tag does not exist, and `gh api users/manykarim/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest output of that run>") | .metadata.container.tags'` prints `[]`. That packages-API call needs the `read:packages` scope from `reproducible-image` task 9.3 (`gh auth refresh -h github.com -s read:packages`, which affects only the local token); the by-username route is the right one here, because this change runs after `reproducible-image` task 10.2 has made the package public. Delete the scratch branch afterwards.

## 17. Release and handover

- [ ] 17.1 **Moved to `workshop-rollout` task 2.4** (not done here; the maintainer archived this change before rollout). After merging to `main`, check the published `edge` image, which `publish` tagged only after `test` passed for that candidate. Run `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop:edge` and wait for `healthy`, then `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090`, and `curl -s localhost:9090/openapi.json`. Verify that all four stages pass and that no `/api/workshop/` or `/api/admin/` path is listed. Version and `workshop-<id>` tags are cut after `acceptance-conformance`, not in this change.
- [x] 17.2 Write the migration notice for the facilitator material in the workshop repository (manykarim/ai-engineering-robotframework) as a section of the drift-coverage pull request description. It covers: (1) the new preset `drift_and_bug` (stage 4 plus `BUG_WRONG_PRICE` and `BUG_CHECKOUT_TOTAL`); (2) that `stage1` no longer turns off bugs and `clean` is the reset; (3) the removed banner; (4) the control endpoints hidden from `/docs`; (5) that the status, presets, preset, flags and admin flags responses still name active bugs by flag key and are readable without a token, so workshop material applies presets through a script or suite setup whose output the coding agent does not read, participant agent instructions (for example the participant repository's `CLAUDE.md`) tell agents not to call `/api/workshop/*` or `/api/admin/*` while diagnosing failures or healing, and permission deny rules for those paths are at most a speed bump; (6) the removed behavior `data-*` markers, including every `data-price-*` attribute; (7) the stylesheet move from `/static/styles.css` to `/assets/styles.<digest>.css`; (8) the removed "Button unavailable" placeholder; (9) the new search-result card markup without the icon-only button; (10) the updated `docs/WORKSHOP-FEATURES.md`; (11) rollback by redeploying the previous `X.Y.Z` tag or a recorded digest, never `sha-<short>`. Verify that the pull request description contains all eleven items.
- [x] 17.3 Right after the merge, and before `acceptance-conformance` starts, archive this change with `openspec archive drift-coverage -y` on `main`. Verify that `test -f openspec/specs/planted-bugs/spec.md` and `test -f openspec/specs/locator-drift/spec.md` succeed, that the "Buggy preset" scenario in `openspec/specs/planted-bugs/spec.md` lists `BUG_CHECKOUT_TOTAL`, and that `openspec list` no longer shows `drift-coverage` as an active change.
