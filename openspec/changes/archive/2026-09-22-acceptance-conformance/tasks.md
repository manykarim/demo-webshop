## 1. Prerequisites and local target

- [ ] 1.1 **Moved to `workshop-rollout` task 2.6** (not done here; the maintainer archived this change before rollout). Confirm the Migration Plan prerequisites: `reproducible-image`, `workshop-spaces` and `drift-coverage` are merged into `main`, `drift-coverage` was archived right after merging (its task 17.3), and `edge` is published. `reproducible-image` may still be an active change (it is archived in its task 10.6, after its task 10.5 has checked this change's workshop tags), and `workshop-spaces`' archive state does not matter. Verify: `uv lock --check && uv sync --locked` succeeds; `compose.yaml` and `.github/workflows/image.yml` exist; the workflow has the jobs `check`, `build`, `smoke`, `test` and `publish`, the `build` job's `outputs:` map declares `digest`, `app_version` and `tags`, and `publish` has `needs: [build, smoke, test]`; `backend/tests/harness.py` defines `isolated_app`, and `grep -cE "def (temp_database|app_client|seeded_app_client|pdf_unavailable|fake_weasyprint)\(" backend/tests/conftest.py` prints 5; `workshop-spaces` is merged as well, which none of the later groups can work without: `backend/app/core/spaces.py` exists, `grep -q "def get_effective_flags" backend/app/core/feature_flags.py` and `grep -rq "X-Workshop-Space" backend/app` succeed, `backend/tests/spaces/conftest.py` defines the `shared_mode` fixture, `grep -qE "shared_mode|admin_token" backend/app/core/config.py` succeeds, and `backend/tests/harness.py` lists `WORKSHOP_ADMIN_TOKEN` in `SCRUBBED_ENV_VARS` and `"WORKSHOP_SHARED_MODE": "false"` in `PINNED_ENV_VARS` (none of these names exists on a branch without that change, so the check cannot pass vacuously); `pyproject.toml` has `pytest-playwright` in `[dependency-groups] dev` and a `[tool.pytest.ini_options]` that registers the `browser` marker with `addopts = "-m 'not browser'"`; `uv run python -c "from backend.app.core.workshop import PLANTED_BUGS, build_presets; assert 'BUG_CHECKOUT_TOTAL' in {b.flag for b in PLANTED_BUGS}; assert 'drift_and_bug' in build_presets()"` exits 0; `test -f openspec/specs/planted-bugs/spec.md && grep -q '### Requirement: Bug registry' openspec/specs/planted-bugs/spec.md` succeeds and `openspec list` no longer shows `drift-coverage`; an anonymous `docker pull ghcr.io/manykarim/demo-webshop:edge` succeeds.
- [x] 1.2 Start the local target used by all later groups (`docker compose up -d --build`, or `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop:edge`) and probe the sibling interfaces the checks rely on. Verify with curl, and paste the transcript into the foundation PR description. With `X-Workshop-Space: cf-probe`: `POST /api/workshop/reset` returns 200; `POST /api/workshop/preset` with `{"preset": "clean"}` returns `"status": "success"`; `GET /api/workshop/status` reports `space`, `version`, `locator_stage` `v1`, empty `active_bugs` and `all_flags` containing `NEW_CART_UI`, `MOBILE_UI_V1` and `SEARCH_V2`; preset `stage3` reports `v3`, and preset `drift_and_bug` after `clean` reports `v4` with exactly `BUG_WRONG_PRICE` and `BUG_CHECKOUT_TOTAL` active; `POST /api/workshop/flags` with `{"flags": {"BUG_CHECKOUT_TOTAL": true}}` makes status list that bug; `GET /api/cart/` and `DELETE /api/cart/` without `X-Session-ID` report `"session": "workshop-demo"`, and an item added there without `X-Session-ID` is not visible in `default`. Without any space header: `POST /api/workshop/reset` returns 200 (local mode), status reports `space` `default`, and `GET /api/cart/` without `X-Session-ID` reports `"session": "workshop-demo"`. `GET /health` returns `ok` and a version. Any mismatch is fixed in the owning change, not worked around in checks.

## 2. Story set import and index (D1)

- [x] 2.1 Import the nine in-scope stories verbatim from `manykarim/ai-workshop-rbcn-2026` at commit `580e816` (`docs/RF-MCP/1.3.RF-MCP_Automate_Scenarios/user_stories/webshop/{web,api}/`) into `docs/user-stories/`, keeping the source file names (all new): `WEB-002_browse_product_catalogue.md`, `WEB-003_view_product_detail.md`, `WEB-004_search_products.md`, `WEB-005_manage_cart.md`, `WEB-006_complete_checkout.md`, `WEB-007_user_authentication.md`, `API-005_cart_operations.md`, `API-006_checkout_order.md` and `API-007_user_login.md`. Apply only the normalization `http://localhost:8000` → `http://localhost:9090`. Verify: for each file, `diff <(gh api "repos/manykarim/ai-workshop-rbcn-2026/contents/<source path>?ref=580e816" -H "Accept: application/vnd.github.raw" | sed 's#localhost:8000#localhost:9090#g') docs/user-stories/<file>` is empty; `grep -c "^### AC-"` gives 13, 9, 8, 9, 11, 10, 11, 13 and 11 (95 in total); `grep -rn "localhost:8000" docs/user-stories` finds nothing; `grep -ilE "drift|feature flag" docs/user-stories/WEB-*.md docs/user-stories/API-*.md` finds nothing. The glob names exactly the nine files this task imports: the three legacy files still sit beside them in `docs/user-stories/` until task 2.2 moves them into `legacy/` and do contain both tokens, and `README.md` (task 2.3) and `CONFORMANCE.md` (task 2.4) legitimately contain them too, so the check is scoped to the story files, like the guard's token check (design D4 check 9) and the parser's `^(WEB|API|AI)-\d{3}_.+\.md$` pattern in task 3.2.
- [x] 2.2 Move `shopping-experience.md`, `checkout-and-orders.md` and `ai-and-operations.md` with `git mv` into `docs/user-stories/legacy/` (new directory). Prepend the D1 header note to each: superseded for the flows covered by WEB-002–007 and API-005–007, still the only description of the home page, AI helper and operations topics, and neither maintained nor gated. Verify: `git status` shows three renames; each legacy file starts with the note; `grep -rnE "user-stories/(shopping-experience|checkout-and-orders|ai-and-operations)" --exclude-dir=.git --exclude-dir=openspec .` finds no stale link.
- [x] 2.3 Write `docs/user-stories/README.md` (new) with seven sections. (1) Provenance: source repository, path, commit `580e816`, import date and the normalization list. (2) Story table: ID, title, file and number of active criteria. (3) ID rules: `<STORY>_<AC>`, IDs never reassigned, removing a criterion always requires a withdrawn-table entry (the guard rejects a silent removal), the ID is kept when the verified behavior is unchanged or the correction is a D1 clarification, otherwise the criterion is withdrawn and replaced by the next free number, withdrawn criteria are removed from the story file, the `## Revisions` entry format (version, criterion, original wording, new wording, reason), and the rule that Revisions and withdrawn-table reasons are written from the shopper's or API consumer's point of view and never reveal a defect, flag, drift stage or detection purpose. (4) Withdrawn criteria table with columns ID, version withdrawn, reason and replacement ID, empty for now. (5) The D1 interpretation rules, verbatim: quoted UI text is matched against visible text, case-insensitive, with whitespace runs counted as one space and never removed (with the "Add to Cart", "Tax", "Log out" and "Logout" examples); accessible names only for labels, names, fields found by label and controls without visible text; "an error message is displayed" means a message that appears as a result of the action; the rules on examples, notes and test data. (6) "For downstream consumers": at a `workshop-<id>` tag, convert only `docs/user-stories/<STORY-ID>_*.md` into scenarios named with `<STORY>_<AC>`, apply the interpretation rules (quoted text against visible text, not role names), and never copy `CONFORMANCE.md` or the conformance tests. (7) A pointer to `legacy/`. Verify: `grep -n "^## " docs/user-stories/README.md` lists all seven sections; the story table has one row per file from 2.1, and each row's count equals `grep -c "^### AC-"` for that file, 95 in total; the withdrawn table has its header and no rows. The guard checks on the index run in task 3.3.
- [x] 2.4 Create `docs/user-stories/CONFORMANCE.md` (new). The intro states the status semantics and precedence (`planted-bug` > `story-corrected` > `app-fixed` > `conforms`), that `Flag` is filled only for `planted-bug`, that `Note` (one sentence plus a link) is required for `app-fixed`, `story-corrected` and `planted-bug` and optional for `conforms` (where it records an incidental planted-bug break) and `pending`, that `pending` is allowed only under `Unreleased`, and the release lifecycle from D2: one section per `workshop-<id>` tag and none for `X.Y.Z`, `edge` or `sha-` images; released section tables are frozen; the `sha-` tag and digest header lines are added in one follow-up commit after tagging; the first record-affecting change after a release opens a new `## Unreleased` section at the top as a full copy of the latest released table. Then comes `## Unreleased` with header lines for the image tag, the image digest (the authoritative identifier), the `sha-` tag (for orientation only, a moving per-commit pointer) and the story commit (image tag and story commit filled at release; digest and `sha-` tag after tagging), the table `| Criterion | Status | Flag | Note |` with all 95 criteria sorted by story and criterion number and status `pending`, and a `### Planted bugs without criterion` subsection with the empty table `| Flag | Reason |`. Verify: `grep -c "| pending |" docs/user-stories/CONFORMANCE.md` is 95; `grep -c "^## Unreleased"` and `grep -c "^### Planted bugs without criterion"` each print 1; the `<STORY>_AC-<n>` IDs built from the story headings with grep and sed, put in the D2 order with `sort -V`, equal the Criterion column in the order it stands in the file, so the table order is checked and not only its membership (`diff` of the two lists is empty); `sort -V` is what the D2 order needs, because a plain lexicographic `sort` puts `AC-10` before `AC-2` and would reject a correct table for every story with more than nine criteria; the intro names all four statuses (`conforms`, `app-fixed`, `story-corrected`, `planted-bug`) with the precedence `planted-bug` > `story-corrected` > `app-fixed` > `conforms`, the `Flag` rule (filled only for `planted-bug`, several keys comma-separated), the `Note` rule (one sentence plus a link, required for `app-fixed`, `story-corrected` and `planted-bug`, optional for `conforms` and `pending`), the `pending` rule (allowed only under `## Unreleased`) and the D2 release lifecycle (one section per `workshop-<id>` tag and none for `X.Y.Z`, `edge` or `sha-` images, released tables frozen, `sha-` tag and digest added in one follow-up commit after tagging, a new `## Unreleased` copy as a full copy of the latest released table at the first record-affecting change after a release); and between `## Unreleased` and its `| Criterion |` header row there is exactly one labelled header line each for the image tag, the image digest, the `sha-` tag and the story commit, each still a placeholder (`sed -n '/^## Unreleased/,/^| Criterion |/p' docs/user-stories/CONFORMANCE.md` shows four such lines), which tasks 18.2 and 18.3 later fill. The guard checks on the record run in task 3.5.

## 3. Pytest configuration, story parser and offline guard (D3, D4)

- [x] 3.1 In `pyproject.toml` `[tool.pytest.ini_options]` (introduced by `drift-coverage`), register the markers `ac(id)`, `planted_bug(flag)` and `conformance` next to `browser`, and combine the default selection into exactly `addopts = "-m 'not browser and not conformance'"`. Do not set `base_url` in the ini file. Add `conformance-report/` and `test-results/` to `.gitignore` and to `.dockerignore` if they are missing (`reproducible-image` D9: the build context carries no directory the image does not copy; git-ignored directories are still part of the Docker build context, and `test-results/` holds the Playwright traces and screenshots of task 4.5, which can be large). No new package is needed: `httpx` is a runtime dependency and `pytest-playwright` is in the dev group. Verify: `uv run pytest --markers` lists all four markers; `grep -n "addopts" pyproject.toml` shows exactly `addopts = "-m 'not browser and not conformance'"`; `uv run pytest backend/tests --collect-only -q` exits 0 and collects no test under `backend/tests/browser` (no conformance-marked test exists yet, so the `-m conformance` override is demonstrated in task 4.1, once `test_harness.py` exists); `uv tree | grep -c rerunfailures` prints 0; `git check-ignore -v conformance-report test-results` matches both and `grep -n 'conformance-report\|test-results' .dockerignore` shows both entries; with probe files created (`mkdir -p conformance-report test-results && touch conformance-report/probe.json test-results/probe.zip`, since neither directory exists yet at this point and an empty probe would pass for the wrong reason), the D9 context probe `printf 'FROM busybox\nCOPY . /ctx\nRUN find /ctx -maxdepth 3 | sort\n' | docker build --progress=plain --no-cache -f - .` lists neither `/ctx/conformance-report` nor `/ctx/test-results`; remove the probe files afterwards.
- [x] 3.2 Add `backend/tests/conformance/__init__.py` (new) and `backend/tests/conformance/stories.py` (new). The parser finds top-level story files in `docs/user-stories/` whose names match `^(WEB|API|AI)-\d{3}_.+\.md$`. It parses `### AC-<n>: <title>` headings and `## Revisions` entries, and reads the index's story table and withdrawn table. It splits `CONFORMANCE.md` into version sections (`## Unreleased` or a tag name, with other `##` sections treated as prose), their header lines, their rows (a comma-separated `Flag` cell becomes a list) and the `### Planted bugs without criterion` table (flag and reason). It exposes `lookup(record, version, criterion_id)`. The guard rules of tasks 3.3–3.5 and 5.3 are added to this module as functions `check_*(docs_root, tests_root) -> list[str]`, so that they can also run on temporary copies. The module imports nothing from `backend.app` at module level. Verify: `uv run pytest backend/tests/conformance/test_stories_parser.py` (new, offline) passes on fixture strings. The fixtures cover a rejected malformed heading, an ignored nested file, a multi-flag cell that yields two keys, a skipped prose section, a no-criterion row with its reason, and `lookup(record, "workshop-2026-10", "WEB-006_AC-7")`, which returns exactly one row with its flags and raises on a duplicate or missing row (scenario "Conformance record lookup").
- [x] 3.3 Implement guard checks 1, 2, 6 and 8 and run them from `backend/tests/conformance/test_story_coverage.py` (new, offline, not marked `conformance`). They check that AC headings are well-formed and unique per story, that no withdrawn ID reappears as a heading, that the index story table matches the story files present, including active-criteria counts, and that no criterion was removed silently: every ID in a row of a released version section is an active heading or withdrawn, and per story the active and withdrawn numbers form 1..max without gaps. Verify: `uv run pytest backend/tests/conformance/test_story_coverage.py` passes on the imported docs, including the index story table from task 2.3, and its collected ID list contains 95 entries of the form `<STORY>_<AC>` (scenario "Downstream conversion"). Negative tests on `tmp_path` copies each fail and name the offending ID or file: a duplicate `AC-3`; the withdrawn `WEB-006_AC-7` used again as a heading; a story file missing from the index; `WEB-006_AC-5` removed (heading, row and index count) without a withdrawn entry, reported as a gap; a fixture `CONFORMANCE.md` with `## Unreleased` above `## workshop-test`, both listing `WEB-006_AC-11`, where the AC-11 heading, its `Unreleased` row and one from the index count are removed without a withdrawn entry. A positive counterpart, the same removal with a withdrawn-table entry and its replacement heading, passes (scenario "Criterion withdrawn", both halves).
- [x] 3.4 Implement guard check 3 (marker coverage). The rule scans the story modules `backend/tests/conformance/test_(web|api|ai)_*.py` with `ast` for `pytest.mark.ac(...)` and `pytest.mark.planted_bug(...)`. It accepts only string literals, requires exactly one `ac` marker per check function and a module-level `pytestmark` that contains `pytest.mark.conformance`, reports referenced IDs that do not exist, and requires every active ID to be referenced. IDs of a story without a module are exempt only while all of that story's rows are `pending` under `Unreleased` and release mode is off. This keeps the foundation PR green as the Migration Plan expects, without weakening the release gate. The rule also fails if a `*.robot` or `*.resource` file exists under `backend/tests/`. Verify with negative tests on temporary modules, each of which reports its problem: a typo `WEB-006_AC-77`, `pytest.mark.ac(ID_VAR)`, a check with two `ac` markers, a missing `pytestmark`, a story module that omits one of its IDs, and a `.robot` file. With no story modules, the guard passes normally and fails under `CONFORMANCE_RELEASE=workshop-test` (scenario "Coverage check").
- [x] 3.5 Implement guard checks 4, 5 and 7 and wire them into `test_story_coverage.py`. Check 4: at most one `## Unreleased` section exists and it is the top version section; no released section contains `pending`; the top version section has exactly one row per active criterion and no row for an unknown or withdrawn ID; every status is from the allowed set; `pending` appears only under `Unreleased`; `Flag` is filled exactly for `planted-bug`; `Note` is non-empty on every `app-fixed`, `story-corrected` and `planted-bug` row and unconstrained on `conforms` and `pending` rows; a `Flag` cell may hold several comma-separated flag keys (design D2, C3), each checked on its own. Check 5: the flags of each criterion equal the `planted_bug` markers on its checks and exist in `PLANTED_BUGS`, imported lazily from `backend/app/core/workshop.py`; every row under "Planted bugs without criterion" names a registry flag that appears in no row and has a non-empty reason with a link; once the top section has no `pending` rows, and always in release mode, every registry flag appears in at least one `planted-bug` row or in the no-criterion table (never both, never neither), and every `BUG_*` flag that `build_presets()["drift_and_bug"]` enables appears in at least one `planted-bug` row and not in the no-criterion table. Check 7: with `CONFORMANCE_RELEASE=<tag>` set, the top section is named exactly `<tag>` and contains no `pending`. Verify: negative tests each fail and name the criterion, flag or section. They cover a duplicate row, a missing row, a row for a withdrawn ID, `planted-bug` without a flag, a flag on `conforms`, `story-corrected` without a note, `app-fixed` without a note, `planted-bug` without a note, the unknown flag `BUG_NOPE`, a mismatch between marker and record flag, `pending` under `## workshop-test`, a release tag that differs from the section name, `## Unreleased` below a released section, two `## Unreleased` sections, a registry flag in neither place (no `pending` rows), a no-criterion row without a reason, a flag both in a row and in the no-criterion table, and `BUG_WRONG_PRICE` only in the no-criterion table with `CONFORMANCE_RELEASE=workshop-test`. A positive test with a row whose `Flag` cell is `BUG_MISSING_BUTTON, BUG_WRONG_PRICE` passes when its checks carry both `planted_bug` markers, and the same fixture with one marker removed fails and names the missing flag. Two further positive tests pin the asymmetry between `Flag` and `Note`: a `conforms` row with an empty `Flag` cell whose `Note` records an incidental `BUG_BROKEN_LINKS` break passes, and a fixture of `pending` rows with empty notes passes. Checks 4, 5 and 7 pass on the real `docs/user-stories/CONFORMANCE.md` from task 2.4 (95 `pending` rows under `## Unreleased`, release mode off), and `uv run pytest backend/tests` passes on the foundation state.

## 4. Live check harness (D3)

- [x] 4.1 Create `backend/tests/conformance/conftest.py` (new) with target handling and no module-level import from `backend.app`. It follows the shared harness rules (design D3, `reproducible-image` D11): suite fixtures only, no fixture with a canonical harness name, no environment variables set for the pytest process, and no change to `backend/tests/harness.py` or the root `backend/tests/conftest.py`. A session-scoped `base_url` fixture overrides pytest-base-url's inside this directory, using `drift-coverage`'s base-URL mechanism (its Decision 8): it returns the `--base-url` option when given and `None` otherwise; no project-specific environment variable selects the target (pytest-base-url itself fills that option's default from its own `PYTEST_BASE_URL`). Before returning a value, the same fixture resolves its host and raises `pytest.UsageError` naming `CONFORMANCE_ALLOW_REMOTE` for any host other than `localhost`, `127.0.0.1` and `::1` unless `CONFORMANCE_ALLOW_REMOTE=1` is set, so the refusal happens before any live fixture or browser context exists and no request is ever sent (resolving a host is not a request to the target). With `None`, the first live fixture raises a `pytest.UsageError` naming `--base-url`, so every check that needs a target ends as an error in setup and a bare run never falls back to whatever listens on port 9090; that fixture also reads `/health` once per session for the report. Every later invocation in these tasks therefore passes `--base-url` explicitly. This task also creates `backend/tests/conformance/helpers.py` (new) with `class ConformanceSetupError(Exception)` and nothing else, because this is the task that states the no-`AssertionError` rule (task 4.3 adds `phrase()`, `control()` and `section()` to the same module); every fixture in this suite raises it (or `pytest.UsageError` for a misconfigured invocation) for setup, precondition and teardown failures and uses no `assert` and no `expect(...)`, because `xfail(strict=True, raises=AssertionError)` (task 4.4) also matches exceptions from the setup and teardown phases and would turn such a failure into XFAIL. Offline runs never contact a target. Verify: `uv run pytest backend/tests/conformance -m conformance --base-url https://example.com` ends with every collected check erroring in setup on a usage error naming `CONFORMANCE_ALLOW_REMOTE`, and contacts no target (`test_base_url` is the only check collected at this point, and it needs no running target); `backend/tests/conformance/test_harness.py` (new, module-level `pytestmark = pytest.mark.conformance`, no `ac` marker; later tasks add to it) has a check `test_base_url` that needs no running target, and `uv run pytest backend/tests/conformance/test_harness.py -m conformance -k test_base_url` passes once with `--base-url http://127.0.0.1:9090` (asserting that value) and once without the option (asserting `None`); `uv run pytest backend/tests --collect-only -q -m conformance` now overrides the `addopts` default and lists exactly the checks of `test_harness.py` and no other test, while `uv run pytest backend/tests --collect-only -q` lists none of them and no test under `backend/tests/browser` (the override demo moved here from task 3.1); `grep -rnE "CONFORMANCE_BASE_URL|PYTEST_BASE_URL" backend/tests pyproject.toml`, `grep -n "base_url" pyproject.toml` and `grep -n "9090" backend/tests/conformance/conftest.py` find nothing; `grep -nE "(^|[^a-z_])assert |expect\(" backend/tests/conformance/conftest.py` finds nothing; `grep -nE "def (temp_database|app_client|seeded_app_client|pdf_unavailable|fake_weasyprint)\(" backend/tests/conformance/*.py` finds nothing; `git diff --stat main -- backend/tests/conftest.py backend/tests/harness.py` is empty; `uv run pytest backend/tests` passes with no server running; `grep -nE "^(from|import) backend" backend/tests/conformance/conftest.py` finds nothing; `grep -nE "^class ConformanceSetupError\(Exception\):" backend/tests/conformance/helpers.py` matches and `grep -nE "^(from|import) backend" backend/tests/conformance/helpers.py` finds nothing, and `conftest.py` imports the class from that module without a `backend.` prefix (`helpers.py` is not a test module, so it contributes no check to the collection listings above).
- [x] 4.2 Add the function-scoped `space` fixture with the two space modes from D3 (`CONFORMANCE_SPACE_MODE=per-check|default`, default `per-check`; any other value is a usage error). In `per-check` mode the id is `cf-<6 hex characters of a per-run id>-<4-digit counter>`, validated against the `workshop-spaces` handle pattern, and every request carries `X-Workshop-Space`. In `default` mode no space header is sent; before the first reset of the run the fixture reads status once and raises `ConformanceSetupError` if a planted bug, a stage other than `v1`, or `NEW_CART_UI`, `MOBILE_UI_V1` or `SEARCH_V2` is enabled; a 401 from reset raises `ConformanceSetupError` naming `WORKSHOP_SHARED_MODE`; the fixture stops with a `pytest.UsageError` under parallel workers (`PYTEST_XDIST_WORKER` set). Setup in both modes sends `POST /api/workshop/reset`, then `POST /api/workshop/preset` with `{"preset": "clean"}` (raising `ConformanceSetupError` unless the body reports `"status": "success"`), then the variant's step (task 4.4). The precondition check then reads `GET /api/workshop/status`: `space` equals the id or `default`, `locator_stage` and `active_bugs` equal the variant's expectation (`v1` and empty for `clean`), and `NEW_CART_UI`, `MOBILE_UI_V1` and `SEARCH_V2` are false in `all_flags`. A violation raises `ConformanceSetupError` naming the flag or stage. Teardown resets again and raises `ConformanceSetupError` if that fails. No fixture in `conftest.py` uses `assert`, `expect(...)` or any other `AssertionError`, because pytest turns an `AssertionError` from setup or teardown into XFAIL in the `xfail(raises=AssertionError)` variants of task 4.4. Verify with `backend/tests/conformance/test_harness.py` (from task 4.1) against the local target, started with `--base-url http://localhost:9090`. With no `--base-url` option, every check that uses `space` ends as ERROR in setup, before any request, with a usage error naming `--base-url`, which is the first point at which the missing-target guard of task 4.1 is observable. In `per-check` mode: status echoes the space id; the fixture's reset helper empties a cart filled in that space and leaves the same session id's cart in `default` untouched. In `CONFORMANCE_SPACE_MODE=default`: status reports `default`; the reset empties a legacy `workshop-demo` cart and leaves a `cf-…` cart untouched; with `NEW_CART_UI` enabled through `PUT /api/admin/flags/NEW_CART_UI` before the run, the harness test ends as ERROR naming `NEW_CART_UI`; `PYTEST_XDIST_WORKER=gw0` stops with a usage error. In both modes, a second target started on another port with `-e WORKSHOP_FLAG_NEW_CART_UI=true` makes the harness test end as ERROR naming `NEW_CART_UI`, not as FAILED. In both modes, a third target started with `-e WORKSHOP_FLAG_BUG_WRONG_PRICE=true` makes the harness test end as ERROR naming `BUG_WRONG_PRICE`, not as FAILED: in `per-check` mode through the `active_bugs` precondition, and in `default` mode through the pre-run status check, before any reset (scenario "Target not in clean state", both examples).
- [x] 4.3 Add the `api` fixture: an `httpx.Client` with `base_url`, `X-Workshop-Space` in `per-check` mode only, a 10-second timeout, and `X-Session-ID` only when a check passes one. Add the UI fixtures: `space_page` sets `X-Workshop-Space` with `context.set_extra_http_headers` on pytest-playwright's function-scoped `context` (headless Chromium) in `per-check` mode, and `prefill_cart(context, items)` generates a session id, sets the `session_id` cookie for the base URL and posts each item to `/api/cart/items` with the same `X-Session-ID`. Extend `backend/tests/conformance/helpers.py` (created in task 4.1) with `phrase(text)`, which returns a compiled case-insensitive pattern for `has_text` and `get_by_text`: the quoted text is trimmed, each whitespace run matches one or more whitespace characters, whitespace is never optional, and the phrase may be contained in a longer text; `control(scope, role, text)`, which returns `scope.get_by_role(role).filter(has_text=phrase(text))`; `section(page, heading)`, which returns the section holding the heading with that visible text, located from the heading itself (`page.get_by_role("heading", name=heading, exact=True).locator("xpath=ancestor::section[1]")`, design D3) and using no id, class or `data-test`; `class ConformanceSetupError(Exception)` already lives there from task 4.1 and remains the only way the suite's fixtures and helpers signal a setup, precondition, usage or teardown failure. Verify: `backend/tests/conformance/test_helpers.py` (new, offline) shows that `phrase("Add to Cart")` matches "Add to cart", `phrase("Tax")` matches "Estimated tax", `phrase("Log out")` matches "Log\n                out", `phrase("Logout")` does not match "Log\n                out", and `phrase("Add to Cart")` does not match the accessible name "Add Aurora Neural Headphones to cart"; the same module checks offline that `section()` is built from role and name locators plus the `ancestor::section[1]` step only, with no id, class or `data-test` selector. In `test_harness.py`, run against the local target with `--base-url http://localhost:9090` (mandatory, task 4.1): after `prefill_cart` with product 1, `/cart` in `space_page` shows "Aurora Neural Headphones"; in `per-check` mode, navigating `space_page` to `/api/workshop/status` shows the space id and the same session id has no items in `default`; on `/products`, `page.get_by_role("article")` finds 18 while `grid = section(page, "All products")` gives `grid.get_by_role("article")` 12 and `control(grid, "button", "Add to Cart")` 12, one per card in that grid, because the six mini preview cards under "Collections to explore" carry no add-to-cart button (design Context); the card scoped by the heading "Aurora Neural Headphones" inside `grid` matches exactly one article, while the same filter on the whole page matches two; `page.get_by_role("button", name=phrase("Add to Cart"))` finds none, which documents that role names are not the pattern; on `/products/1`, `control` scoped to the container holding the `h1` finds exactly one button, while unscoped it finds more than one.
- [x] 4.4 Implement variants and sweep mode in `conftest.py`. `pytest_generate_tests` gives every check the variant `clean`; every check that uses the `space_page` fixture (the UI checks) also `stage2`, `stage3`, `stage4` and `drift_and_bug`; and every check with `planted_bug` markers one variant per flag. A stage variant applies that preset after `clean` and expects `v2`–`v4` with no active bugs. The `drift_and_bug` variant applies that preset after `clean`, expects `v4` (`drift-coverage` Decision 12) and the bug set of `build_presets()["drift_and_bug"]`, and is marked `xfail(strict=True, raises=AssertionError)` when the check's `planted_bug` markers intersect that set. A flag variant sends `POST /api/workshop/flags` with `{"flags": {"<FLAG>": true}}` after `clean`, confirms in status that exactly that bug is active, and is marked `xfail(strict=True, raises=AssertionError)`. The variant step and the precondition raise `ConformanceSetupError`, never an `AssertionError`, so an xfail-marked variant cannot mask a broken precondition (task 4.1). With `CONFORMANCE_SWEEP_FLAG=<FLAG>` (must name a registry flag) or `CONFORMANCE_SWEEP_PRESET=drift_and_bug` (must name a preset), every check runs once with that flag or preset applied after `clean` and without xfail; an invalid value stops the run with a usage error. Verify with temporary checks in `test_harness.py` (only the first stays): a UI check using `space_page` that asserts `active_bugs == []`, marked `planted_bug("BUG_CHECKOUT_TOTAL")`, reports `[clean]`, `[stage2]`, `[stage3]` and `[stage4]` PASSED and `[drift_and_bug]` and `[BUG_CHECKOUT_TOTAL]` XFAIL (because the precondition raises `ConformanceSetupError`, an XFAIL can only come from the check body, so it shows that the precondition accepted `v4` for `drift_and_bug`); a temporary fixture override that breaks the precondition (expecting `v1` where the variant applies `v4`, or a bug the variant does not enable, and skipping the `POST /api/workshop/flags` step) makes both the `[BUG_CHECKOUT_TOTAL]` variant and the marked `[drift_and_bug]` variant end as ERROR naming the flag or stage, not XFAIL, and the run exit non-zero — this, not the XFAIL above, is what proves the precondition is enforced inside the xfail-marked variants; the same override written with a bare `assert` instead of `ConformanceSetupError` ends as XFAIL, which is the regression this rule guards against; a temporary teardown failure in the `space` fixture likewise makes the `[BUG_CHECKOUT_TOTAL]` variant end as ERROR rather than disappearing; a target started with `-e WORKSHOP_FLAG_BUG_WRONG_PRICE=true` makes the marked check end as ERROR naming `BUG_WRONG_PRICE` in its `[clean]`, `[stage2]`, `[stage3]`, `[stage4]` and `[BUG_CHECKOUT_TOTAL]` variants, and the run exit non-zero, while its `[drift_and_bug]` variant, whose expected bug set already contains `BUG_WRONG_PRICE`, still ends as XFAIL in `per-check` mode, because the override adds no bug that variant does not enable itself; with `CONFORMANCE_SPACE_MODE=default` against that same target every variant of the check, `[drift_and_bug]` included, ends as ERROR naming `BUG_WRONG_PRICE` through the pre-run status check (task 4.2); an inverted copy reports a strict XPASS as FAILED; a variant raising `httpx.ConnectError` reports FAILED, not XFAIL; a temporary fixture override that applies `stage2` where the `[stage3]` variant expects `v3` ends as ERROR naming the stage; `CONFORMANCE_SWEEP_FLAG=BUG_CHECKOUT_TOTAL` and `CONFORMANCE_SWEEP_PRESET=drift_and_bug` each run the first check once, and it FAILS; `CONFORMANCE_SWEEP_FLAG=BUG_NOPE` and `CONFORMANCE_SWEEP_PRESET=stage9` exit with a usage error.
- [x] 4.5 Add the report plugin to `conftest.py`. It aggregates results per `ac` marker: a criterion passes when its `clean` and stage variants pass and its flag variants and marked `drift_and_bug` variants xfail; a setup, precondition or teardown error in any variant, the xfail-marked ones included, gives `error`, and a run with at least one `error` exits non-zero. It writes `conformance-report/<space mode>/report.json` and `report.md`. The header holds the base URL, the space mode, the `/health` version and the run time. The rows hold criterion, result, checks and verified variants, and checks without `ac` are listed with their result under "Harness". The plugin prints a terminal summary of failing or erroring criterion IDs and of failing or erroring harness checks, and adds each ID as the JUnit property `criterion`. Document the invocation, including both space modes and `--tracing retain-on-failure --screenshot only-on-failure`, in the `CONFORMANCE.md` intro; the documented invocation always passes `--base-url` explicitly (it is mandatory, there is no default, task 4.1) and its value must be a locally started image, not a source run. Verify with temporary checks outside the story-module pattern against the local target, always with `--base-url http://localhost:9090` (one passing and one failing UI check with distinct IDs): `conformance-report/per-check/report.md` lists both IDs as `pass` and `fail`, and its version equals the one from `curl -s localhost:9090/health`; against the targets started with `-e WORKSHOP_FLAG_NEW_CART_UI=true` and with `-e WORKSHOP_FLAG_BUG_WRONG_PRICE=true` (task 4.2), the passing check's ID is listed as `error` in both space modes; with `CONFORMANCE_SPACE_MODE=default` the report lands in `conformance-report/default/` and its header names the mode; `--junitxml=conformance-report/per-check/junit.xml` contains `<property name="criterion" value="…">`; the failing check leaves a trace under `test-results/` (scenario "Run against a container"); with the broken-precondition override from task 4.4 active on a `[BUG_CHECKOUT_TOTAL]` variant, that criterion is listed as `error` and not as `pass` in both space modes, and the run exits non-zero although no check FAILED, so the `conformance` step of task 6.1 fails and `publish` is blocked (spec requirement "Workshop tag gate": an `error` MUST block the tag); the `CONFORMANCE.md` intro shows the documented invocation for both space modes (the default `per-check` run and the `CONFORMANCE_SPACE_MODE=default` run), each with an explicit `--base-url` (mandatory, no default, task 4.1) whose value is a locally started image and not a source run, and with `--tracing retain-on-failure --screenshot only-on-failure`, and it names the per-mode report location `conformance-report/<space mode>/`.

## 5. Triage procedure and decision records (D5)

- [x] 5.1 Add the prose section `## How deviations are resolved` to `docs/user-stories/CONFORMANCE.md`, before the version sections. For how to run the checks it points at the documented invocation in the intro (task 4.5), which already covers the local target, both space modes and the report location, and adds only what the intro does not cover: the two sweep-mode variables `CONFORMANCE_SWEEP_FLAG` and `CONFORMANCE_SWEEP_PRESET`. It holds D5 procedure steps 2–7: checks are written from the story text and committed before the first run; each failing criterion gets exactly one outcome after a check defect has been ruled out; then the bug sweep and the close-out; the maintainer decides in PR review and agents only propose. It also holds the triage table with the half-day timebox and a recording checklist per outcome.
  - A check defect: fix the check, no status. This includes a check that fails only because a control's accessible name differs from its visible text, or because it relies on a hook that drift changes.
  - A failure only in a stage or `drift_and_bug` variant: a drift defect, fixed in the drift layer and recorded as `app-fixed` with a note naming the stage; `story-corrected` only if the criterion names a hook that drift changes; never `planted-bug`.
  - `app-fixed`: the fix plus a regression test in the suite of the code it covers (`backend/tests/unit/`, `backend/tests/integration/`, `backend/tests/contract/` for render states and the drift contract, `backend/tests/spaces/` for space, cart or order scoping), a note naming the change and version, and visible-text changes collected for the handoff. **Shared harness** (`reproducible-image` D11): the regression test uses the root fixtures by exact name (`app_client`, `seeded_app_client`, `temp_database`, `pdf_unavailable`, and `fake_weasyprint` whenever it creates an order) or `isolated_app`; it defines no fixture with those names, adds fixtures only in its own suite directory's conftest, creates no other root conftest, and sets no environment variable of the pytest process. **Drift contract**, whenever a fix touches `backend/app/templates/**`, `backend/app/static/app.js` or `backend/app/assets/styles.css`: (a) new or changed elements in a covered flow (e.g. per-field checkout messages, a product count, the header sign-in button) get ids, classes and `data-test` only through `drift.id`, `drift.cls` and `drift.test`, including `for` and `aria-describedby` targets — except the shared uncovered blocks `form-field` (with `form-field__icon`) and `button` with its modifiers `button--primary`, `button--ghost`, `button--text` and `button--lg`, which stay written literally beside the covered hook and are never added to `COVERED_CLASSES`, because `drift.cls` raises on an unknown key (`drift-coverage` task 2.4) and covering them would break its byte-for-byte stylesheet assertion (its task 6.1 (a) and design Decision 3); assertion targets get `data-test`; there are no literal ids outside `STABLE_IDS`, `STABLE_IDS` is never widened for covered-flow elements, and no behavior-only `data-*` marker is added; and no new element anywhere in `base.html` or a page template carries the `role="status"` attribute — the add-to-cart confirmation stays the only element with that attribute on every page, because `drift-coverage` binds `showFlash` with `document.querySelector('[role="status"]')` (its task 8.2) and a contract test asserts exactly one per page in every stage (its task 8.1); a live result count, such as the WEB-002 product count, gets `aria-live="polite"` on an element without `role="status"`, exactly as the cart badge does (a count that only re-renders on a full filter submit needs no live region at all), and per-field validation messages are plain text targeted through `aria-describedby` via `drift.id`, or `role="alert"`, which carries no uniqueness constraint, and in the checkout form they sit inside the field wrappers, never as a second direct-child `<p>` of `form[action="/checkout"]`, whose only direct-child `<p>` is the signed-in note that `drift-coverage`'s contract test counts (its task 8.1); (b) new keys go into `COVERED_IDS`, `COVERED_CLASSES` or `DATA_TEST_VALUES` and the stage 2–4 `StageSpec` tables under the uniqueness rule, with matching rows in the `## Drift mapping` tables of `docs/WORKSHOP-FEATURES.md`; (c) every new render state (e.g. the `POST /checkout` validation-error render, the product not-found page) is added to `COVERED_PAGES` in `backend/tests/contract/test_stage_contract.py`, with its new selectors in `backend/tests/contract/coverage_oracle.py`; (d) `app.js` changes bind only to stable hooks (`form[action="/checkout"]`, `form.elements.<name>`, roles, ARIA attributes and relationships, `href`, and the content data attributes `data-product`, `data-product-name`, `data-category` and `data-chat-prompt`), add no behavior-only `data-*` marker (no `data-price-*` or other removed marker returns), write no `data-*` attribute or id at runtime, change classes only through `classList`, and any markup JS inserts is cloned from a `<template>` rendered with `drift` and filled with `textContent`; (e) HTML pages are rendered only while `workshop_view` is active: validation happens inside a route that declares it, and no global exception handler renders HTML; (f) visible text stays identical in every stage, and a visible-text change also updates `drift-coverage`'s smoke-test locators (`backend/tests/browser/test_drift_smoke.py`) and `docs/WORKSHOP-FEATURES.md` in the same PR; accessible names kept stable by `drift-coverage` are never changed to fit a quoted phrase; (g) the fix is verified with `uv run pytest backend/tests` (template scan, script scan, contract tests, docs sync, including `drift-coverage`'s exactly-one-`role="status"` contract test extended to each added render state) and `grep -rn 'role="status"' backend/app/templates` returns only the add-to-cart confirmation region in `base.html`, and `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` for `stage1`–`stage4`, and a regression test for an HTML error or validation page renders it in stages 1 and 2.
  - `story-corrected`: keep the ID (verified behavior unchanged, or a D1 clarification), or withdraw and replace it following the README ID rules, with a `## Revisions` entry and updates to the index tables. The Revisions reason is neutral (shopper or API consumer point of view, no defect, flag, drift stage, locator or detection purpose); any bug motivation goes only into the row's note.
  - `planted-bug` with a new flag: see task 5.2.
  - Spin-out: a new OpenSpec change, the row stays `pending` and the tag waits; an interim story correction needs explicit maintainer sign-off and a note naming the change.
  Verify: the guard still passes (prose sections are ignored); the section names all four statuses, the drift-only failure rule, the shared-harness item, the drift-contract item including the single-`role="status"` rule and the uncovered-block exception, the neutral-reason rule, the spin-out rule and the approval rule; and it points at the intro for the invocation instead of repeating it, so the `--base-url` command lines appear only there, while the two sweep-mode variables appear only in this section.
- [x] 5.2 Add the checklist for a new planted bug to that section: `drift-coverage` is archived, so `openspec/specs/planted-bugs/spec.md` exists (`test -f`); the clean state behaves correctly; an entry in `PLANTED_BUGS` (`flag`, `flow`, `trigger`, `defect`, `scope`) in `backend/app/core/workshop.py`, which also yields the seed row and the preset membership (off in `clean`, on in `buggy`); a row in the `## Planted bugs` table of `docs/WORKSHOP-FEATURES.md` (columns `Flag | Flow | Trigger | Defect`, in registry order; never a separate table), so that `drift-coverage`'s `backend/tests/contract/test_docs_sync.py` passes; updated `drift-coverage` contract tests that enumerate the registry bugs (`backend/tests/contract/test_presets_api.py`, `test_planted_bugs.py`); the `planted_bug` marker and a record row with the flag; the delta `openspec/changes/acceptance-conformance/specs/planted-bugs/spec.md` (new), written only after `drift-coverage` was archived: a MODIFIED "Bug registry" block that copies the whole current requirement from `openspec/specs/planted-bugs/spec.md`, with its header text unchanged and every scenario including "Clean preset", and adds the new key or keys to every scenario of that requirement that enumerates the active registry bugs (today "Buggy preset" and "Stage preset keeps active bugs", which must end up listing the same set of flags), matching the updated `test_presets_api.py` cases of the same names, while "Clean preset" keeps listing no active bugs, plus an ADDED requirement with the bug's flow, trigger and defect; the conditional `planted-bugs` entry under Modified Capabilities in `proposal.md` stays; a facilitator note for the handoff. Verify: the checklist, including the archive prerequisite, is present in `CONFORMANCE.md`; `openspec validate acceptance-conformance --strict --json` reports `"valid": true` and no issue at any level whose message contains "Archive would refuse".
- [x] 5.3 Extend the guard with decision-record consistency (D4 check 9): every `story-corrected` row has a `## Revisions` entry naming that criterion in its story file; every Revisions entry names a criterion that is withdrawn or has status `story-corrected` or `planted-bug`; every withdrawn ID's replacement exists as a heading; story files contain none of `BUG_`, `LOCATOR_`, `planted`, `drift` (case-insensitive), `CONFORMANCE.md` or `backend/tests` (neutral story files; task 2.1 confirmed that the imported text contains no `drift`). Verify: negative tests each fail and name the ID or file. They cover a row without a Revisions entry, a Revisions entry for a `conforms` criterion, a missing replacement heading, a story file that mentions `BUG_WRONG_PRICE`, and a Revisions reason that mentions a drift stage. `uv run pytest backend/tests` passes on the real docs.

## 6. CI gate (D6)

- [ ] 6.1 **Moved to `workshop-rollout` task 1.4** (not done here; the maintainer archived this change before rollout). Add the job `conformance` to `.github/workflows/image.yml` (design D6): `needs: [build, smoke]`, runs for `refs/tags/workshop-*`, pushes to `main` and `workflow_dispatch` (never for pushed `vX.Y.Z` tags or pull requests), `runs-on: ubuntu-24.04`, `permissions` `contents: read` and `packages: read`, a matrix `space_mode: [per-check, default]` with `fail-fast: false`, `timeout-minutes: 45` per leg, with actions pinned by full SHA. Steps per leg: check out and log in to GHCR read-only; `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}` (the untagged candidate that `build` pushed) and wait for `healthy`; pinned `setup-uv`, then `uv sync --locked`; restore the Playwright browser cache, keyed by the locked Playwright version, then `uv run playwright install --with-deps chromium`; on workshop tags only, `CONFORMANCE_RELEASE=${{ github.ref_name }} uv run pytest backend/tests/conformance/test_story_coverage.py`; `CONFORMANCE_SPACE_MODE=${{ matrix.space_mode }} uv run pytest backend/tests/conformance -m conformance --base-url http://localhost:9090 --junitxml=conformance-report/${{ matrix.space_mode }}/junit.xml --tracing retain-on-failure --screenshot only-on-failure`; `if: always()`: append the failing or erroring criterion IDs from `report.md`, and any failing or erroring checks from its "Harness" section under a "Harness" heading, with the space mode and the tested digest, to `$GITHUB_STEP_SUMMARY` and upload `conformance-report/` and `test-results/` as an artifact named per leg; on failure, print `docker logs`. Verify: `docker run --rm -v "$PWD":/repo -w /repo rhysd/actionlint:latest` reports no errors. After merging, the run on `main` shows both `conformance` legs started from the digest in the `build` job's `digest` output, each with its report artifact and a step summary naming its mode and that digest.
- [ ] 6.2 **Moved to `workshop-rollout` task 1.5** (not done here; the maintainer archived this change before rollout). Extend `publish` on `reproducible-image`'s job graph (its D7: `build` pushes the candidate by digest with no tag, and only `publish` creates `sha-<short>`, `edge`, `X.Y.Z` and `workshop-<id>`). Append `conformance` to the `needs: [build, smoke, test]` that `drift-coverage` task 16.2 left, giving `needs: [build, smoke, test, conformance]`, and append `&& (needs.conformance.result == 'success' || !startsWith(github.ref, 'refs/tags/workshop-'))` to its condition, giving `if: ${{ !cancelled() && github.event_name != 'pull_request' && needs.build.result == 'success' && needs.smoke.result == 'success' && needs.test.result == 'success' && (needs.conformance.result == 'success' || !startsWith(github.ref, 'refs/tags/workshop-')) }}`. A failed gate on a workshop tag therefore leaves the candidate with no tag at all, `sha-<short>` included. Verify: actionlint passes; the workflow file's `publish` job shows exactly that `needs` list and condition; on a pull request run, `conformance` and `publish` are skipped; the gate PR description contains the behavior per ref (workshop tag gated by `smoke`, `test` and both conformance legs; `main` and `workflow_dispatch` on other refs non-blocking; pushed `vX.Y.Z` tags and pull requests without conformance). The non-blocking `main` path is verified in task 6.3; the blocked-tag path in task 18.1, which alone cannot tell a correct expression from one missing `!cancelled()`.
- [ ] 6.3 **Moved to `workshop-rollout` task 2.5** (not done here; the maintainer archived this change before rollout). Rehearse the non-blocking `main` path right after the gate PR merges and before any story PR. If an earlier `main` run already had a failing `conformance` job with a succeeding `publish`, record that run's URL instead. Otherwise merge a one-commit PR that adds to `backend/tests/conformance/test_harness.py` a check marked `conformance`, without an `ac` marker, that fails with `assert False, "non-blocking main rehearsal"`. Verify on that `main` run: `conformance` concluded `failure`, the step summary and the uploaded report each list the failing check under "Harness", neither names a criterion ID, and the step summary also names the space mode and the tested digest; `publish` ran and succeeded; an anonymous `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:edge` shows the digest from that run's `build` output. Then merge the revert and verify that the next `main` run has both `conformance` legs green and the guard passing. Record both run URLs in the gate PR description.

## 7. Audit WEB-002 Browse Product Catalogue (13 criteria)

- [x] 7.1 Write `backend/tests/conformance/test_web_002_browse_product_catalogue.py` (new) from the story text and the README interpretation rules only, with UI checks on `/products` for AC-1 to AC-13. Counts and bounds come from the story's test data (12 products, 9 categories, $39.50, $899.00). Per-card assertions (AC-1: name, image, price and an "Add to Cart" button) are scoped to each card, and quoted text goes through `phrase()`/`control()`. Every criterion that iterates, counts or enumerates catalogue cards (AC-1 and the 12-product count among them) takes its cards from `section(page, "All products")`; the six mini preview cards under "Collections to explore" repeat six products without an add-to-cart button, description or rating and are outside every WEB-002 card assertion, as is the search-results region (design Context and D3). No check calls `get_by_role("article")` at page level. The expected product sets for AC-7, AC-8, AC-9 and AC-12 come from `GET /api/products/` during arrange. AC-8 finds the two sliders by role `slider` and their accessible names "Minimum price" and "Maximum price" inside the group named "Price range" (they have no visible text; `drift-coverage` task 8.1 keeps the names identical in every stage), fills the minimum with 100 and then the maximum with 300 so the page's handler updates the submitted values, asserts that the visible values show $100 and $300, clicks "Apply filters", and treats both bounds as inclusive; it never writes the hidden `price_min`/`price_max` inputs directly. Verify: the checks are committed before the first run, in a commit without app or story changes; the guard passes with all 13 WEB-002 IDs referenced; `grep -nE "get_by_test_id|data-test|locator\(\s*[\"'][#.]"` on the module finds nothing, and `grep -nE "name=phrase\("` finds nothing; `grep -nE "data-price|price-(min|max)-range"` on the module finds nothing; `grep -nE "page\.get_by_role\(\s*[\"']article" ` on the module finds nothing, and the criterion check that counts catalogue cards asserts that `section(page, "All products")` holds 12 cards; the page-wide count of 18 articles stays pinned by the harness check of task 4.3, the only place that resolves articles at page level; the reviewer compares each check with its criterion and confirms that quoted text uses the visible-text form.
- [x] 7.2 Run the module against a target built from the current branch (`docker compose up -d --build`, then `uv run pytest backend/tests/conformance/test_web_002_browse_product_catalogue.py -m conformance --base-url http://localhost:9090 --tracing retain-on-failure`, once in `per-check` mode and once with `CONFORMANCE_SPACE_MODE=default` on a restarted container). Triage every failing criterion following "How deviations are resolved". Candidates to confirm or reject: AC-3, slider bounds rounded to `$40` (`products.html`); AC-6 and AC-9, the hidden `price_min` of 40 excludes Insight Smart Notebook ($39.50) and there is no product count; AC-1, whether "price" means the product's price (a clarification candidate for `BUG_WRONG_PRICE`, proposed in task 16.1). Verify: the story PR description has exactly one triage row (ID, evidence, proposed outcome, reason) for each criterion that either `report.md` lists as `fail` or `error`, and the maintainer approves each row.
- [x] 7.3 Apply the approved WEB-002 outcomes with their checklists (app fixes with regression tests and the drift contract, story corrections with neutral Revisions entries, new planted bugs following task 5.2, spin-outs as separate changes). Set the rows of the triaged criteria; rows that pass stay `pending` until task 17.1. Verify: against a rebuilt target, the reports of both space modes show `pass` for every WEB-002 criterion, including its stage variants, except approved spin-outs; `uv run pytest backend/tests` passes (guard, unit and contract tests); if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 8. Audit WEB-003 View Product Detail (9 criteria)

- [x] 8.1 Write `backend/tests/conformance/test_web_003_view_product_detail.py` (new) from the story text only, with UI checks on `/products/1` (Aurora Neural Headphones, $249.99, Audio, 4.8 stars, 214 reviews) and `/products/12`: AC-3 clicks "Add to Cart" inside the product's action area (the container holding the `h1` with the product name, not the cards under "You might also like"), waits for the POST to `/api/cart/items` with `page.expect_request`, and confirms through `GET /api/cart/` with the page's `session_id` that product 1 was added; AC-4 follows "Buy Now" to `/checkout`; AC-5 runs on both `/products/1` and `/products/12` and expects at most 4 related cards inside `section(page, "You might also like")` — not the three static "Why you'll love it" feature `<article>` elements and not the "Trending in the studio" mini cards, which are articles too (design Context) — with same-category products first, each linking to a detail page that returns 200; AC-9 opens `/products/9999` and `/products/abc` and expects an error message and no product detail content. Verify: the checks are committed before the first run; the guard passes with all 9 WEB-003 IDs referenced; the locator greps from task 7.1 find nothing in the module; the reviewer compares each check with its criterion.
- [x] 8.2 Run the WEB-003 module in both space modes against a target built from the current branch and triage every failing criterion following "How deviations are resolved". Candidates: AC-3, `data-product-id` names a locator hook (story-corrected to "identifies the product", verified by the added product); AC-9, `/products/9999` returns JSON `{"detail": "Product not found"}` and `/products/abc` a JSON 422 (app-fixed with an HTML not-found page rendered by the `/products/{product_id}` route itself, per drift-contract item (e) in task 5.1); bug candidate for task 16.1: AC-5 (`BUG_BROKEN_LINKS`, via `/products/1`, whose related cards include ids 8 and 12). AC-6 cannot detect `BUG_WRONG_PRICE` (no trending card has an id divisible by 3), and AC-5 cannot detect `BUG_MISSING_BUTTON` (no button requirement). Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report.
- [x] 8.3 Apply the approved WEB-003 outcomes with their checklists and set the rows of the triaged criteria. Verify: against a rebuilt target, the reports of both space modes show `pass` for every WEB-003 criterion, including its stage variants, except approved spin-outs; `uv run pytest backend/tests` passes; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 9. Audit WEB-004 Search Products (8 criteria)

- [x] 9.1 Write `backend/tests/conformance/test_web_004_search_products.py` (new) from the story text only: AC-1 checks the hero search on `/`; AC-2, AC-3 and AC-6 to AC-8 run on `/products` with the queries `headphones`, `aurora` and `xyz123` from the test data; AC-3 and AC-8 scope their per-card assertions to each result card inside the results region `get_by_role("region", name="Search results")` (design D3), never to the "All products" grid or the "Collections to explore" previews, which stay rendered on `/products` while results are shown, and AC-8 finds each card's "Add to Cart" button with `control()` (`drift-coverage` A3, its task 9.1, renders result cards with the card macro and drops the icon-only button, so each card has exactly one); AC-4 asserts with `page.expect_request` that submitting calls `/api/search` carrying the query value, and that the response lists Aurora Neural Headphones, with a request predicate that matches the path `/api/search` or `/api/search/` exactly and so excludes `/api/search/suggest`; AC-5 types into the search input and asserts a request to `/api/search/suggest` and a visible suggestion list below the input. Per the interpretation rules, the test data's parameter name `q` and its response shapes are not normative unless a criterion refers to them. Verify: the checks are committed before the first run; the guard passes with all 8 WEB-004 IDs referenced; the locator greps find nothing; the reviewer compares each check with its criterion.
- [x] 9.2 Run the WEB-004 module in both space modes against a target built from the current branch and triage every failing criterion. Candidates: AC-4, with `drift-coverage` A3 submitting no longer calls `/api/search` because results come from the server-rendered fragment of `GET /search/results` (story-corrected by withdrawal and replacement with AC-9: either the API wording, `GET /api/search` with `query` returns matching products, or endpoint-neutral wording; the maintainer decides; app fixes that bring back client-side rendering from `/api/search` are excluded); AC-5, suggestions are objects rather than strings, and requests start at 2 characters (`app.js`) (story-corrected). Bug candidates for task 16.1: AC-3 and AC-8 for `BUG_WRONG_PRICE`, `BUG_MISSING_BUTTON` and `BUG_BROKEN_LINKS`, depending on whether a query returns a product whose id triggers the bug. Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report.
- [x] 9.3 Apply the approved WEB-004 outcomes with their checklists and set the rows of the triaged criteria. If the AC-4 withdrawal is approved: remove the AC-4 criterion from `WEB-004_search_products.md`, add the `### AC-9` heading and a `## Revisions` entry with a neutral reason, add the README withdrawn-table row (WEB-004_AC-4 → WEB-004_AC-9) and keep the WEB-004 count at 8, replace the AC-4 check with an `ac("WEB-004_AC-9")` check, drop the AC-4 row from `## Unreleased` and add an AC-9 row with status `story-corrected`. Verify: against a rebuilt target, the reports of both space modes show `pass` for every WEB-004 criterion except approved spin-outs; the guard passes with 8 active WEB-004 IDs, and if the withdrawal was applied `WEB-004_AC-4` is no longer a heading and `WEB-004_AC-9` shows `pass`; `uv run pytest backend/tests` and `openspec validate acceptance-conformance --strict` pass; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 10. Audit WEB-005 Manage Cart (9 criteria)

- [x] 10.1 Write `backend/tests/conformance/test_web_005_manage_cart.py` (new) from the story text only: AC-1, AC-2 and AC-9 click "Add to Cart" in the UI, once in the card of a named product on `/products` and once in the product's action area on `/products/1` (not in the related cards), and wait for the POST to `/api/cart/items` with `page.expect_request`; AC-2 runs at 1280×800 and at 390×844; AC-3 to AC-6 arrange the cart through the API (product 1 ×1 and another product ×2) and assert line totals and the summary texts "Complimentary" and "Calculated at checkout", with the total equal to the subtotal; AC-7 uses a fresh session; AC-8 is checked through the API with the header, the `session_id` cookie, neither (expecting `"session": "workshop-demo"` in both space modes) and both (header wins). Verify: the checks are committed before the first run; the guard passes with all 9 WEB-005 IDs referenced; the locator greps find nothing; the reviewer compares each check with its criterion.
- [x] 10.2 Run the WEB-005 module in both space modes against a target built from the current branch and triage every failing criterion. Candidates: AC-2, the badge stays hidden on mobile until the menu opens (story-corrected); AC-8, the cart and checkout APIs ignore the `session_id` cookie (`api/cart.py`, `api/checkout.py`), as `workshop-spaces` D3 decides (APIs read the `X-Session-ID` header only; `workshop-spaces` does not cover a cookie-only API call, so AC-8's cookie-only case is the guard) (story-corrected: the cart API takes the session from `X-Session-ID` and otherwise uses `workshop-demo`, and the cookie identifies the cart for pages; ID kept or withdrawn per the README ID rules; an app fix that makes the APIs read the cookie is excluded because it would undo `workshop-spaces` D3). Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report.
- [x] 10.3 Apply the approved WEB-005 outcomes with their checklists and set the rows of the triaged criteria. For the AC-8 story correction: update the criterion in `WEB-005_manage_cart.md` so the API session comes from the `X-Session-ID` header with the `workshop-demo` fallback, add a `## Revisions` entry with a neutral reason (for example "clarified that API clients identify their cart with the `X-Session-ID` header"), withdraw and replace the ID with README withdrawn-table and index updates if review judges the change semantic, and change the check so its cookie-only case expects the fallback cart (`"session": "workshop-demo"`, without the cookie's items) while header, neither and both cases stay. No application code changes for AC-8. Verify: against a rebuilt target, the reports of both space modes show `pass` for every WEB-005 criterion (or its replacement), including its stage variants, except approved spin-outs; `uv run pytest backend/tests` passes, including `workshop-spaces`' `backend/tests/spaces/test_space_carts.py` and `test_space_resolution.py` unchanged; `git diff main -- backend/app/api/cart.py backend/app/api/checkout.py backend/app/core/spaces.py` is empty unless another approved WEB-005 outcome needed it; the guard passes, including the Revisions and withdrawn-table rules for AC-8; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 11. Audit WEB-006 Complete Checkout (11 criteria)

- [x] 11.1 Write `backend/tests/conformance/test_web_006_complete_checkout.py` (new) from the story text only. Carts are arranged through the API; acting and asserting happen in the UI on `/checkout`: AC-1 checks summary items, subtotal, shipping, tax and total; AC-2 and AC-3 check fields by label and input type; AC-4 checks `notanemail` and `a@b` are invalid and block submission (no success message, and the cart still has its items), while `test@example.com` shows no error; AC-5 checks `A` against `Test User`; AC-6 checks `123` and `1234` against `123 Test Street, City`; AC-7 checks the success message with an order number in the criterion's format; AC-8 checks both PDF links return 200 with `application/pdf` (requested with the space header in `per-check` mode); AC-9 checks `/cart` is empty and the badge matches the criterion; AC-10 starts with an empty cart, opens `/checkout`, asserts that no `get_by_role("alert")` is visible yet, fills every field with valid test-data values (`test@example.com`, `Test User`, `123 Test Street, City`) so browser validation cannot block the submit, clicks "Place order" inside `page.expect_request` for a POST to `/checkout` to prove a submission happened, then asserts that `get_by_role("alert")` contains "Your cart is empty" and that no success message and no `ORD-` order number are shown; the empty-state text in the summary does not count as evidence, and no specific HTTP status is asserted because the story names none; AC-11 checks an invalid submission stays on `/checkout` with a message next to each invalid field. Verify: the checks are committed before the first run; the guard passes with all 11 WEB-006 IDs referenced; the locator greps find nothing; the reviewer compares each check with its criterion.
- [x] 11.2 Run the WEB-006 module in both space modes against a target built from the current branch and triage every failing criterion. Candidates: AC-1, summary from `drift-coverage`; total rule to clarify ("Order total" is subtotal plus shipping plus tax, with "Complimentary" shipping counted as 0); planted bug `BUG_CHECKOUT_TOTAL` in task 16.1; AC-4, AC-5, AC-6 and AC-11, no per-field messages, and the server's JSON 422 replaces the page (`main.py`, `checkout.html`) (app-fixed by validating inside the `POST /checkout` handler and re-rendering `checkout.html` with per-field messages through drift hooks, per drift-contract items (a), (c) and (e) in task 5.1; if the estimate exceeds half a day, spin out); AC-7, `^ORD-\d{4,}$` against `ORD-` plus 8 hex characters (story-corrected, consistent with API-006 AC-2); AC-9, badge hidden at 0 (story-corrected). Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report, including the time estimate for any validation fix.
- [x] 11.3 Apply the approved WEB-006 outcomes with their checklists and set the rows of the triaged criteria. Verify: against a rebuilt target, the reports of both space modes show `pass` for every WEB-006 criterion, including its stage variants, except approved spin-outs; any spin-out change exists (`openspec list`) and is named in its row's note; `uv run pytest backend/tests` passes; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 12. Audit WEB-007 User Authentication (10 criteria)

- [x] 12.1 Write `backend/tests/conformance/test_web_007_user_authentication.py` (new) from the story text only: AC-1 checks the signed-out header on `/`, `/products` and `/cart`; AC-2 and AC-3 find the dialog by role and accessible name, its fields by label, and its close button, which has no visible text, by accessible name; AC-4, AC-5 and AC-10 sign in as `jamie@flowlinesupply.com` / `demo123` and assert the name, the account sections and persistence across `/products` and `/cart`; AC-6 expects each order's invoice and summary links to return 200 with `application/pdf`; AC-7 finds the "Logout" button with `control()` (so under the D1 whitespace rule it does not match "Log out") and logs out; AC-8 uses a wrong password and an unknown user and expects an alert in the dialog, which stays open; AC-9 closes the dialog and records no request to `/api/auth/login` (`page.on("request")`). Verify: the checks are committed before the first run; the guard passes with all 10 WEB-007 IDs referenced; the locator greps find nothing; the reviewer compares each check with its criterion.
- [x] 12.2 Run the WEB-007 module in both space modes against a target built from the current branch and triage every failing criterion. Candidates: AC-1, the header says "Log in" while the dialog says "Sign in" (`base.html`) (app-fixed because the app contradicts itself, or story-corrected); AC-7, "Log out" against "Logout" (story-corrected: not whitespace-equivalent under D1). Record the empty-email "[object Object]" message as a finding without a status. Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report.
- [x] 12.3 Apply the approved WEB-007 outcomes with their checklists and set the rows of the triaged criteria. List every visible-text change for the handoff. Verify: against a rebuilt target, the reports of both space modes show `pass` for every WEB-007 criterion, including its stage variants, except approved spin-outs; `uv run pytest backend/tests` passes; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 13. Audit API-005 Cart Operations (11 criteria)

- [x] 13.1 Write `backend/tests/conformance/test_api_005_cart_operations.py` (new) from the story text only, with checks through the `api` fixture: AC-1, AC-8 and AC-10 send no `X-Session-ID` and expect `"session": "workshop-demo"` in both space modes; AC-2, AC-3 and AC-11 use product 1 at $249.99 with exact totals 499.98 and 749.97, and check field types; AC-4, AC-5 and AC-6 expect 422 with the offending field in `detail[].loc` and the limit in the message; AC-7 expects 404 with `{"detail": "Product not found"}`; AC-9 uses `X-Session-ID: test-session-123`. Verify: the checks are committed before the first run; the guard passes with all 11 API-005 IDs referenced; the reviewer compares each check with its criterion.
- [x] 13.2 Run the API-005 module in both space modes against a target built from the current branch and triage every failing criterion. Candidate: AC-1, AC-8 and AC-10 rely on `workshop-spaces` D3, in which the fallback cart is `<space>:workshop-demo` outside `default` and `GET /api/cart/`, `POST /api/cart/items` and `DELETE /api/cart/` report `"session": "workshop-demo"` without the prefix in every space (`session_label`; before that change `DELETE /api/cart/` returned `cart_service.session_key`); expected to conform, and a mismatch is a defect in `workshop-spaces`' implementation, fixed there and recorded as `app-fixed`. Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report.
- [x] 13.3 Apply the approved API-005 outcomes with their checklists and set the rows of the triaged criteria. Verify: against a rebuilt target, the reports of both space modes show `pass` for every API-005 criterion except approved spin-outs; `uv run pytest backend/tests` passes; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 14. Audit API-006 Checkout and Order Creation (13 criteria)

- [x] 14.1 Write `backend/tests/conformance/test_api_006_checkout_order.py` (new) from the story text only, with carts arranged under an `X-Session-ID`: AC-1 checks `status`, `order` and `documents`; AC-2 checks `ORD-[A-F0-9]{8}`; AC-3 checks product 1 ×2 gives 499.98, 35.0 and 534.98; AC-4 checks the rounding rules; AC-5 checks item fields and types; AC-6 checks exactly two paths, containing `invoice_ORD-` and `summary_ORD-` and ending in `.pdf`; AC-7 checks the cart is empty afterwards; AC-8 checks 400 with `{"detail": "Cart is empty"}`; AC-9 to AC-11 check 422; AC-12 checks the order is built from the `checkout-test` session's cart, with items compared; AC-13 checks customer fields and `status` `processing`. Verify: the checks are committed before the first run; the guard passes with all 13 API-006 IDs referenced; the reviewer compares each check with its criterion.
- [x] 14.2 Run the API-006 module in both space modes against a target built from the current branch and triage every failing criterion. Candidate: AC-6 document paths. Inside the image `documents` lists `/data/pdfs/invoice_<order_number>.pdf` and `/data/pdfs/summary_<order_number>.pdf` (`reproducible-image` D3, decided), and `/static/pdfs/` no longer serves order documents; the file names are unchanged, so the criterion conforms if it concerns file names and is story-corrected (neutral reason) if it names a `static/pdfs` location. Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report.
- [x] 14.3 Apply the approved API-006 outcomes with their checklists and set the rows of the triaged criteria. Verify: against a rebuilt target, the reports of both space modes show `pass` for every API-006 criterion except approved spin-outs; `uv run pytest backend/tests` passes; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 15. Audit API-007 User Login (11 criteria)

- [x] 15.1 Write `backend/tests/conformance/test_api_007_user_login.py` (new) from the story text only: AC-1 checks the response keys; AC-2 checks `expires_at` within ±5 minutes of now plus 4 hours; a value without an offset is read as UTC only if the README interpretation rules say so, otherwise it is a finding; AC-3 to AC-7 for Jamie: the addresses Home and Studio in that order, Visa 4242 and Amex 3782 with `display`, and 2 orders with the required fields, the URL patterns `/api/docs/orders/{id}/invoice.pdf` and `/summary.pdf`, and descending `created_at`; AC-8 and AC-9 check 401 with `{"detail": "Invalid credentials"}`; AC-10 checks Alex with 1 address, 1 payment method and 1 order; AC-11 checks 422. Verify: the checks are committed before the first run; the guard passes with all 11 API-007 IDs referenced; the reviewer compares each check with its criterion.
- [x] 15.2 Run the API-007 module in both space modes against a fresh target built from the current branch and triage every failing criterion. Candidates: AC-2, a naive UTC datetime (`api/auth.py`) (conforms by interpretation, or app-fixed with an explicit offset); AC-4, addresses without `order_by` (`models/user.py`) (app-fixed if unstable); AC-7, equal seeded timestamps after single-transaction seeding (app-fixed with explicit seeded timestamps). Verify: the PR description has one approved triage row for each `fail` or `error` criterion in either report, and AC-4 and AC-7 were run on at least two freshly started containers.
- [x] 15.3 Apply the approved API-007 outcomes with their checklists and set the rows of the triaged criteria. Verify: against a rebuilt target, the reports of both space modes show `pass` for every API-007 criterion except approved spin-outs; `uv run pytest backend/tests` passes; if a fix touched templates, `app.js` or the stylesheet, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.

## 16. Bug sweep (D5 step 5)

- [x] 16.1 Run `CONFORMANCE_SWEEP_FLAG=<FLAG> uv run pytest backend/tests/conformance -m conformance --base-url http://localhost:9090` once for every flag in `PLANTED_BUGS` (`BUG_MISSING_BUTTON`, `BUG_WRONG_PRICE`, `BUG_BROKEN_LINKS`, `BUG_SLOW_RESPONSE`, `BUG_CHECKOUT_TOTAL`, plus bugs added in groups 7–15), and once with `CONFORMANCE_SWEEP_PRESET=drift_and_bug`. For each broken criterion, decide whether it is meant to detect the bug (`planted-bug`) or breaks incidentally (note only). For the flags that `drift_and_bug` enables, an incidental break is resolved instead: it becomes a registered `planted-bug`, or, where the criterion does not concern the affected product, the check arranges data so the trigger does not apply. Mappings that need a clarification are proposed even when the sweep shows no break. Candidate mappings: `BUG_CHECKOUT_TOTAL` → WEB-006_AC-1 after "Order total" is clarified; `BUG_MISSING_BUTTON` → WEB-002_AC-1, which relies on the card scoping of task 7.1, because the affected products 5 and 10 are also rendered as preview mini cards that never carry a button (design Context); `BUG_WRONG_PRICE` → WEB-002_AC-1 after "price" is clarified; `BUG_BROKEN_LINKS` → WEB-003_AC-5 (via `/products/1`); WEB-004_AC-3 and AC-8 for the three card bugs if the sweep breaks them; `BUG_SLOW_RESPONSE` → none. `BUG_WRONG_PRICE` and `BUG_CHECKOUT_TOTAL` must end with at least one criterion. Verify: the sweep PR description lists, per flag and for `drift_and_bug`, the failing criteria from its `report.md` with a proposed decision each, shows that the `drift_and_bug` failing set equals the union of the sweeps for its flags or explains every difference, and the maintainer approves the decisions.
- [x] 16.2 Apply the sweep decisions in this order. First the approved clarifications: story corrections that keep the ID (design D1, C1; no new criteria, C2), or, where review judges a change semantic, a withdrawal with a replacement ID under the README ID rules, each with a Revisions entry whose reason is neutral (e.g. "clarified that the displayed price is the product's price") while the bug motivation goes only into the row's note, and the matching check changes (WEB-006_AC-1: the order total equals subtotal plus shipping plus tax, with "Complimentary" shipping counted as 0; WEB-002_AC-1: "price" is the product's price). Rerun the sweep for the affected flags and confirm that the clarified checks now fail with an AssertionError. Then add `@pytest.mark.planted_bug("<FLAG>")` to the detecting checks (one marker per flag), set their rows to `planted-bug` with the flag or flags, comma-separated in the `Flag` cell (C3), and a note that mentions any other applicable status, add incidental breaks of other flags as notes, and list bugs that no criterion detects under `### Planted bugs without criterion` with a reason and link (only bugs that `drift_and_bug` does not enable). Verify: `uv run pytest backend/tests` passes, including the guard's marker, record and registry consistency, and on a temporary copy of the docs with `pending` replaced by `conforms` the completeness and heal-vs-hide rules of check 5 pass. In a live run in both space modes, every `[<FLAG>]` variant is XFAIL, every `[clean]` and stage variant PASSED, and every `[drift_and_bug]` variant is XFAIL exactly for the checks marked with `BUG_WRONG_PRICE` or `BUG_CHECKOUT_TOTAL` and PASSED otherwise (scenario "Heal-vs-hide preset"). For the criterion recorded with `BUG_CHECKOUT_TOTAL` (expected WEB-006_AC-1), selected by node id, `[clean]` passes and `[BUG_CHECKOUT_TOTAL]` is XFAIL, and a sweep run with `BUG_CHECKOUT_TOTAL` fails with an AssertionError on the same container, where the next normal run passes again (scenario "Registered deviation"). For a check recorded with `BUG_WRONG_PRICE` (expected WEB-002_AC-1), `[BUG_WRONG_PRICE]` is XFAIL; if that criterion is also recorded with `BUG_MISSING_BUTTON`, its `[BUG_MISSING_BUTTON]` variant is XFAIL as well. If no criterion ends up recorded with `BUG_CHECKOUT_TOTAL` or `BUG_WRONG_PRICE`, stop: the spec's "Drift-stage conformance" requirement cannot be met, so the clarification or the spec is revised first.

## 17. Close-out and decision-record review

- [x] 17.1 Close out the audit. Confirm that every spin-out change is merged, or that its criterion has a maintainer-signed interim story correction. Rebuild the local target from `main`, run the full suite in both space modes (restarting the container between the passes, as CI uses one container per leg), and set every remaining `pending` row to `conforms`. Verify: `uv run pytest backend/tests/conformance -m conformance --base-url http://localhost:9090` and `CONFORMANCE_SPACE_MODE=default uv run pytest backend/tests/conformance -m conformance --base-url http://localhost:9090` both pass, and both `report.md` files show `pass` for every active criterion, with all stage variants passing and every planted-bug and marked `drift_and_bug` variant XFAIL (scenarios "Clean run", "Default and named spaces", "Stage presets"); `grep -c "| pending |" docs/user-stories/CONFORMANCE.md` prints 0; `uv run pytest backend/tests` passes, now with the completeness and heal-vs-hide rules enforced; `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090` passes for `stage1`–`stage4`.
- [ ] 17.2 **Moved to `workshop-rollout` task 4.1** (not done here; the maintainer archived this change before rollout). Review the whole decision record with the maintainer: every non-`conforms` row has a note with a working link; every `story-corrected` criterion has a Revisions entry with the original wording and the reason; every Revisions reason and withdrawn-table reason reads as a shopper- or consumer-facing clarification and reveals no defect, flag, drift stage, locator or detection purpose (the token check cannot catch paraphrases); withdrawn IDs and their replacements are in the README withdrawn table, and the story table counts are current; every row under "Planted bugs without criterion" has an approved reason; `openspec list` shows no open spin-out that the tag waits for. Verify: the guard passes; `git diff <import commit> -- 'docs/user-stories/WEB-*' 'docs/user-stories/API-*'` contains only hunks that map to a Revisions entry or to a withdrawal, and no criterion was removed without a withdrawn entry; the maintainer's approval, including the neutral-reason review, is recorded on the close-out PR.
- [x] 17.3 Reconcile the `planted-bugs` registration with what the audit did. If the audit added planted bugs, keep the delta `specs/planted-bugs/spec.md` written under task 5.2 (after `drift-coverage` was archived) and the conditional `planted-bugs` entry under Modified Capabilities in `proposal.md`. If it added none, make sure this change has no `specs/planted-bugs/` directory, remove the conditional `planted-bugs` entry from Modified Capabilities in `proposal.md`, and say so in the close-out PR description. Verify: if bugs were added, `test -f openspec/specs/planted-bugs/spec.md` succeeds; `openspec validate acceptance-conformance --strict --json` reports `"valid": true` and no issue whose message contains "Archive would refuse"; the `### Modified Capabilities` section of `proposal.md` lists `planted-bugs`; in a test space (`X-Workshop-Space: cf-probe`), preset `buggy` makes `/api/workshop/status` list the new flags and preset `clean` lists none; `uv run pytest backend/tests/contract` passes. If none were added, `test ! -e openspec/changes/acceptance-conformance/specs/planted-bugs` succeeds; the `### Modified Capabilities` section of `proposal.md` no longer lists `planted-bugs`; `openspec validate acceptance-conformance --strict` passes; the close-out PR description states that no planted bug was added; and `git diff <drift-coverage merge commit> -- backend/app/core/workshop.py` shows no change to `PLANTED_BUGS`. Outcome: the audit added no planted bug (groups 7 to 16 recorded only the registered flags `BUG_MISSING_BUTTON`, `BUG_WRONG_PRICE`, `BUG_BROKEN_LINKS` and `BUG_CHECKOUT_TOTAL`, and listed `BUG_SLOW_RESPONSE` without criterion), so the "none added" branch applies: this change has no `specs/planted-bugs/` directory and the conditional entry was removed from Modified Capabilities in `proposal.md`.

## 18. Release and handoff

- [ ] 18.1 **Moved to `workshop-rollout` task 4.2** (not done here; the maintainer archived this change before rollout). Rehearse the blocked tag (scenario "Unregistered deviation blocks the tag"). On a throwaway branch from `main`, rename `## Unreleased` to `## workshop-gatetest-1`, make the WEB-006_AC-7 check fail on purpose (wrong expected pattern), make one UI check fail in its drift variants but not in `[clean]`: pick a UI check that carries no `planted_bug` marker and point one of its locators at the stage-1 CSS class of a covered element, so that `[clean]` and `[stage3]` pass while `[stage2]`, `[stage4]` and `[drift_and_bug]` fail (`drift-coverage`'s stage contract renames the classes of covered elements in stages 2 and 4 and keeps them identical in stage 3, so no hook kind fails in stage 3 alone; a check without `planted_bug` markers keeps its `[drift_and_bug]` variant a real failure instead of the strict xfail that task 4.4 applies to marked checks). Push the git tag `workshop-gatetest-1`. Verify (gate behavior only): the run's `smoke` and `test` jobs passed, both `conformance` legs ran against the `build` job's `digest` output and failed with `WEB-006_AC-7` and the drift-only criterion — the one whose `[clean]` and `[stage3]` variants passed while its `[stage2]`, `[stage4]` and `[drift_and_bug]` variants failed — both named in the step summary, and `publish` was skipped; an anonymous `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:workshop-gatetest-1` fails because the tag does not exist. Record the run URL and its `build` `digest` output in the gate PR and hand them to `reproducible-image` task 10.5 (a), which checks the image level (the candidate digest carries no tag, `sha-<short>` included). Afterwards, delete the git tag and the branch (`git push --delete origin workshop-gatetest-1`).
- [ ] 18.2 **Moved to `workshop-rollout` task 4.4** (not done here; the maintainer archived this change before rollout). Once both `conformance` legs on `main` are green for the release candidate, commit on `main` the rename of `## Unreleased` to `## workshop-<id>`. The header names the image tag `workshop-<id>` and states that the `sha-` tag and the story commit are those of the tagged commit. Verify: `CONFORMANCE_RELEASE=workshop-<id> uv run pytest backend/tests/conformance/test_story_coverage.py` passes locally, and the `main` run for this commit is green.
- [ ] 18.3 **Moved to `workshop-rollout` task 4.5** (not done here; the maintainer archived this change before rollout). Push the git tag `workshop-<id>` on that commit. Afterwards, in one follow-up commit, record the literal `sha-<short>` and the digest (the tag run's `build` `digest` output, the authoritative identifier) in the section header without changing the table; a commit cannot contain its own hash. Verify (gate behavior only): in the tag run, both `conformance` legs ran against the `build` job's `digest` output and pass, and their `report.md` headers show version `workshop-<id>` and their space mode, with every criterion `pass` and every stage, planted-bug and `drift_and_bug` variant behaving as expected; `publish` ran only after both legs, `smoke` and `test` succeeded, and it succeeded; section `## workshop-<id>` contains exactly one `WEB-006_AC-7` row (scenario "Conformance record lookup"); the digest recorded in the header equals the `build` `digest` output of the tag run; the follow-up commit's diff touches only header lines, and the guard passes. Hand the run URL and digest to `reproducible-image` task 10.5 (b), which checks the image level of the published tag (platforms, promoted digest, `sha-<short>`, revision label, anonymous arm64 pull and the `/health` version).
- [ ] 18.4 **Moved to `workshop-rollout` task 5.3** (not done here; the maintainer archived this change before rollout). Hand off to the maintainers of `manykarim/ai-engineering-robotframework` in an issue. It names the tag `workshop-<id>` and the story path `docs/user-stories/<STORY-ID>_*.md` at that tag, links the README sections "For downstream consumers" and the interpretation rules, and lists the story corrections and withdrawn IDs, the visible-text changes from app fixes, and new flag names as facilitator notes. It also states that `CONFORMANCE.md` and `backend/tests/conformance/` must not be copied into the participant repository. Verify: the issue exists and contains all six items.
- [x] 18.5 Archive this change after the workshop tag is published: `openspec archive acceptance-conformance -y`. `drift-coverage` is already archived; `reproducible-image` may still be active and is archived in its own task 10.6. Verify: `openspec/specs/acceptance-stories/spec.md` exists; the audit added no planted bug (task 17.3), so the archive applies no `planted-bugs` delta and leaves `openspec/specs/planted-bugs/spec.md` unchanged; `openspec list` no longer shows `acceptance-conformance`.
