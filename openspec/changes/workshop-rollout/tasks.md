## 1. Pull request and merge

- [x] 1.1 **(was `reproducible-image` 9.2)** Create `.github/workflows/image.yml` (new) with its triggers (push to `main`; tags `v[0-9]+.[0-9]+.[0-9]+` and `workshop-*`; `pull_request`; `workflow_dispatch`), permissions `contents: read` and `packages: write`, concurrency grouped per ref with `cancel-in-progress` only for pull requests, and every action pinned by a full commit SHA with a version comment. Add the `check` job: checkout, `astral-sh/setup-uv` with the pinned uv release, `uv lock --check`, `uv sync --locked` and `uv run pytest backend/tests`. Verify that actionlint reports no findings, that `grep -n "uses:" .github/workflows/image.yml | grep -vE "@[0-9a-f]{40} # v"` returns nothing, and that the `check` job is green on the pull request.
- [ ] 1.2 **(was `reproducible-image` 9.3)** Add the `build` job, which needs `check` (design D7). It runs `tools/resolve_image_version.py` and `docker/metadata-action` with `type=sha,prefix=sha-,format=short`, `type=semver,pattern={{version}}`, `type=match,pattern=^workshop-.*$,group=0`, `type=edge,branch=main` and `flavor: latest=false`. It declares exactly these job outputs, with these fixed names: `digest` (set below), which `smoke`, `publish` and the later `test` (`drift-coverage`) and `conformance` (`acceptance-conformance`) jobs read to address the candidate; `app_version` (the resolved `APP_VERSION`), which `smoke` passes to the smoke script; and `tags` (the computed tag list, `sha-<short>` plus the human-readable tag if any, one full image reference per line), which only `publish` reads, as the tags it promotes (9.5). Outside pull requests, it runs `setup-qemu`, `setup-buildx`, a GHCR login with `GITHUB_TOKEN`, and `build-push-action` for `linux/amd64,linux/arm64` with `cache-from`/`cache-to: type=gha,mode=max`, the `APP_VERSION` build argument, the metadata labels (including `org.opencontainers.image.revision`), `org.opencontainers.image.version=<APP_VERSION>`, no `tags` input, and `outputs: type=image,name=ghcr.io/manykarim/demo-webshop,push-by-digest=true,name-canonical=true,push=true`. It pushes the candidate by digest without any tag, sets the job output `digest` to the pushed index digest (`sha256:…`), and writes the digest, ref, `APP_VERSION` and the computed tag list (one full image reference per line) to `$GITHUB_STEP_SUMMARY`; the step summary is the only durable record of the tag list, because a finished run's job outputs cannot be read back through the API and a blocked candidate never reaches `publish`. On pull requests, it builds amd64 only with `load: true` and no login or push, starts the image with only `-p 9090:9090`, waits for `healthy`, and runs `tools/smoke_image.py` phases `fresh`, `restarted` (after `docker restart`) and `recreated` (after `docker rm -f` and a new `docker run`); later changes add their pull-request checks as further steps after the `recreated` phase, against the same loaded container. Also on pull requests, a dry-run step proves the `workshop-*` mapping without publishing: it creates a local, never-pushed git tag `workshop-dryrun-${{ github.run_id }}` on HEAD, runs `tools/resolve_image_version.py` with `GITHUB_REF=refs/tags/workshop-dryrun-<run_id>`, and runs `docker/metadata-action` with `context: git` and the same tag rules. It asserts that the computed tags contain `workshop-dryrun-<run_id>` and `sha-<short>` and no `latest`, and that `app_version` is `workshop-dryrun-<run_id>`. If `context: git` does not resolve the local tag reliably, keep only the `resolve_image_version.py` assertion and note in the pull request that the `type=match` rule is then first checked live by task 10.5 (a), which reads the computed tag list and `APP_VERSION` from the `build` step summary of the `acceptance-conformance` task 18.1 run, on the blocked rehearsal tag `workshop-gatetest-1` and before any real workshop tag is cut. Verify that the multi-arch `build-push-action` step in the workflow file has no `tags` input, so an unverified candidate never carries a tag, and that the `build` job's `outputs:` map declares exactly `digest`, `app_version` and `tags`. Verify on the pull request run that `build` is green including all three smoke phases and the dry-run step, that the login and push steps are skipped, and that GHCR gained no package version. Prerequisite for this and every later packages-API check: the GitHub packages REST API needs a gh token with the `read:packages` scope, even for a public package, and the maintainer's token does not have it today (its scopes are `gist`, `read:org`, `repo`, `workflow`), so the endpoint would answer 403 instead of listing tags once the package exists. Before the first such check, run `gh auth refresh -h github.com -s read:packages` (this adds the scope and keeps the existing ones, and affects only the local token, not the workflow's `GITHUB_TOKEN`) and verify that `gh auth status` lists `read:packages` among the token scopes and that `gh api 'users/manykarim/packages?package_type=container'` returns 200 with a JSON list, possibly empty, and not 403 `You need at least read:packages scope`. With the scope in place, verify that `gh api user/packages/container/demo-webshop/versions` returns HTTP 404 `Package not found.` (not 403), or, if the package already exists, lists the same versions as before the run. This check and the one in 10.1 use the authenticated-user route `user/packages/...`, because the package is private until 10.2 and `users/manykarim/packages/...` serves public packages only, so the by-username route would answer 404 for an existing private package as well; `users/manykarim/...` is used only in 9.5 and 10.5 (a), which run after 10.2. The scope probe above stays on `users/manykarim/packages?package_type=container`: "List packages for a user" returns the packages the requesting user has access to, so it works while the package is private and still distinguishes 200 from a 403 missing-scope answer. Record the amd64-only pull request build duration in the pull request as a baseline.
- [ ] 1.3 **(was `drift-coverage` 16.2)** Add drift-coverage's verification to `.github/workflows/image.yml` on `reproducible-image`'s job graph (design Decision 16; `reproducible-image` D7: `build` pushes the candidate by digest with no tag, and only `publish` creates `sha-<short>`, `edge`, `X.Y.Z` and `workshop-<id>`).
  - (a) Pull requests: at the end of `build`, after the `tools/smoke_image.py` `recreated` phase, add steps guarded by `if: github.event_name == 'pull_request'` that run against that same running container of the locally loaded amd64 image (`-p 9090:9090`): pinned `astral-sh/setup-uv`, `uv sync --locked`, `uv run playwright install --with-deps chromium`, `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090`, and `docker logs` on failure.
  - (b) Other events: add a `test` job with `needs: build`, `if: github.event_name != 'pull_request'`, `runs-on: ubuntu-24.04`, `permissions` `contents: read` and `packages: read`, and actions pinned by full SHA. It checks out the commit, runs pinned `setup-uv` and `uv sync --locked`, and runs `uv run pytest backend/tests/unit backend/tests/contract`. It then logs in to GHCR read-only (needed while the package is private, until `reproducible-image` task 10.2), runs `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}`, and waits for `healthy`, printing `docker logs` and failing after 90 seconds. Finally it runs `uv run playwright install --with-deps chromium` and `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090`.
  - (c) Change `publish` from `needs: [build, smoke]` to `needs: [build, smoke, test]` and append `&& needs.test.result == 'success'` to its condition, giving `if: ${{ !cancelled() && github.event_name != 'pull_request' && needs.build.result == 'success' && needs.smoke.result == 'success' && needs.test.result == 'success' }}`. `acceptance-conformance` task 6.2 later adds `conformance`. A failing `test` therefore leaves the candidate with no tag at all, `sha-<short>` included.
  Verify that actionlint reports no errors. Verify that on a pull request run the `build` job's browser steps show the `stage1`–`stage4` parametrizations green, `test` and `publish` are skipped, and GHCR gained no package version. Verify that on the first `main` run after the merge, `test` runs the image at the `build` job's `digest` output and is green, `publish` starts only after `test` finished, and `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short>` shows that digest. Verify that a failing `test` blocks every tag: on a scratch branch, push a commit that makes one browser smoke assertion fail on purpose and dispatch the workflow (`gh workflow run image.yml --ref <branch>`). `check`, `build` and `smoke` pass, `test` fails, `publish` is skipped, `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short of that commit>` fails because the tag does not exist, and `gh api users/manykarim/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest output of that run>") | .metadata.container.tags'` prints `[]`. That packages-API call needs the `read:packages` scope from `reproducible-image` task 9.3 (`gh auth refresh -h github.com -s read:packages`, which affects only the local token); the by-username route is the right one here, because this change runs after `reproducible-image` task 10.2 has made the package public. Delete the scratch branch afterwards.
- [x] 1.4 **(was `acceptance-conformance` 6.1)** Add the job `conformance` to `.github/workflows/image.yml` (design D6): `needs: [build, smoke]`, runs for `refs/tags/workshop-*`, pushes to `main` and `workflow_dispatch` (never for pushed `vX.Y.Z` tags or pull requests), `runs-on: ubuntu-24.04`, `permissions` `contents: read` and `packages: read`, a matrix `space_mode: [per-check, default]` with `fail-fast: false`, `timeout-minutes: 45` per leg, with actions pinned by full SHA. Steps per leg: check out and log in to GHCR read-only; `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}` (the untagged candidate that `build` pushed) and wait for `healthy`; pinned `setup-uv`, then `uv sync --locked`; restore the Playwright browser cache, keyed by the locked Playwright version, then `uv run playwright install --with-deps chromium`; on workshop tags only, `CONFORMANCE_RELEASE=${{ github.ref_name }} uv run pytest backend/tests/conformance/test_story_coverage.py`; `CONFORMANCE_SPACE_MODE=${{ matrix.space_mode }} uv run pytest backend/tests/conformance -m conformance --base-url http://localhost:9090 --junitxml=conformance-report/${{ matrix.space_mode }}/junit.xml --tracing retain-on-failure --screenshot only-on-failure`; `if: always()`: append the failing or erroring criterion IDs from `report.md`, and any failing or erroring checks from its "Harness" section under a "Harness" heading, with the space mode and the tested digest, to `$GITHUB_STEP_SUMMARY` and upload `conformance-report/` and `test-results/` as an artifact named per leg; on failure, print `docker logs`. Verify: `docker run --rm -v "$PWD":/repo -w /repo rhysd/actionlint:latest` reports no errors. After merging, the run on `main` shows both `conformance` legs started from the digest in the `build` job's `digest` output, each with its report artifact and a step summary naming its mode and that digest.
- [ ] 1.5 **(was `acceptance-conformance` 6.2)** Extend `publish` on `reproducible-image`'s job graph (its D7: `build` pushes the candidate by digest with no tag, and only `publish` creates `sha-<short>`, `edge`, `X.Y.Z` and `workshop-<id>`). Append `conformance` to the `needs: [build, smoke, test]` that `drift-coverage` task 16.2 left, giving `needs: [build, smoke, test, conformance]`, and append `&& (needs.conformance.result == 'success' || !startsWith(github.ref, 'refs/tags/workshop-'))` to its condition, giving `if: ${{ !cancelled() && github.event_name != 'pull_request' && needs.build.result == 'success' && needs.smoke.result == 'success' && needs.test.result == 'success' && (needs.conformance.result == 'success' || !startsWith(github.ref, 'refs/tags/workshop-')) }}`. A failed gate on a workshop tag therefore leaves the candidate with no tag at all, `sha-<short>` included. Verify: actionlint passes; the workflow file's `publish` job shows exactly that `needs` list and condition; on a pull request run, `conformance` and `publish` are skipped; the gate PR description contains the behavior per ref (workshop tag gated by `smoke`, `test` and both conformance legs; `main` and `workflow_dispatch` on other refs non-blocking; pushed `vX.Y.Z` tags and pull requests without conformance). The non-blocking `main` path is verified in task 6.3; the blocked-tag path in task 18.1, which alone cannot tell a correct expression from one missing `!cancelled()`.
- [ ] 1.6 **(was `reproducible-image` 10.1)** Merge the change into `main` after review. Verify on the first workflow run that `check`, `build` and both `smoke` legs are green, and that `publish` created `sha-<short>` and `edge` on this run's candidate, which `gh api user/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest output of that run>") | .metadata.container.tags'` shows by listing both tags. Use the authenticated-user route `user/packages/...` here, not `users/manykarim/packages/...`: the by-username route serves public packages only and answers 404 until 10.2 has made the package public, while the authenticated-user route also serves the owner's private package; both need the `read:packages` scope from 9.3. As a second, registry-level check, run `docker login ghcr.io` (the package is still private, so an anonymous inspect cannot work here) and verify that `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:edge` and `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short>` both resolve to the `build` job's `digest` output. `publish` itself fails only at its anonymous inspect step (expected while the package is private). Record the duration of the multi-arch `build` job (cold cache) in the pull request next to the amd64 baseline from 9.3.

## 2. First main run and public package

- [x] 2.1 **(was `reproducible-image` 9.4)** Add the `smoke` job (needs `build`, not on pull requests) as a matrix on `ubuntu-24.04` and `ubuntu-24.04-arm`. It logs in to GHCR read-only, because the package stays private until task 10.2 and the untagged candidate can only be pulled by digest. It runs `ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}` with only `-p 9090:9090` and waits for `healthy`, printing `docker logs` and failing after 90 seconds. It prints `docker exec <container> uname -m`. Then it runs `tools/smoke_image.py` phases `fresh`, `restarted` and `recreated` with `--expect-version ${{ needs.build.outputs.app_version }}`. Verify with actionlint now, and on the first `main` run (10.1): both legs are green, the arm leg prints `aarch64`, the amd64 leg prints `x86_64`, the `fresh` phase expects and gets `dev`, and on both architectures the runtime order's `invoice.pdf` and `summary.pdf` and every seeded order's `invoice.pdf` and `summary.pdf` render as `application/pdf` while `/static/pdfs/invoice_<order_number>.pdf` returns 404.
- [ ] 2.2 **(was `reproducible-image` 9.5)** Add the `publish` job with `needs: [build, smoke]` (`build` because it reads that job's outputs) and `if: ${{ !cancelled() && github.event_name != 'pull_request' && needs.build.result == 'success' && needs.smoke.result == 'success' }}`. The later changes extend it to the final form of design D7: `drift-coverage` task 16.2 adds `test` to `needs` and `needs.test.result == 'success'` to the condition, and `acceptance-conformance` task 6.2 adds `conformance` and `(needs.conformance.result == 'success' || !startsWith(github.ref, 'refs/tags/workshop-'))`, giving `needs: [build, smoke, test, conformance]`. The job logs in, takes the tag list from `needs.build.outputs.tags` (one full image reference per line, computed once in `build`: `sha-<short>` always, plus `edge`, `X.Y.Z` or `workshop-<id>` when present), so the tag names are derived in exactly one place, and runs one `docker buildx imagetools create` with a `-t` per line on `ghcr.io/manykarim/demo-webshop@${{ needs.build.outputs.digest }}`, the verified digest, without rebuilding. Because the list always contains `sha-<short>`, an empty human-readable part (a `workflow_dispatch` on another branch) needs no extra condition. It then runs `docker logout ghcr.io` and, for each created tag, an anonymous `docker buildx imagetools inspect --raw` that asserts that `linux/amd64` and `linux/arm64` are present (ignoring `unknown/unknown` attestation entries) and that the digest equals the tested one. It writes the promoted tags and the digest to `$GITHUB_STEP_SUMMARY`. Verify with actionlint now, that the job's `imagetools create` step reads `needs.build.outputs.tags` and derives no tag name of its own, and on the first `main` run (10.1–10.2): the anonymous inspect fails while the package is private and passes after the rerun, and right after the run the digests of `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:edge` and of `:sha-<short>` both equal the `build` job's `digest` output. After 10.2, dispatch the workflow on a scratch branch (`gh workflow run image.yml --ref <branch>`): the run is green, both `smoke` legs expect and get version `dev` (scenario *Image without a version tag* for a `sha-<short>`-only image), `publish` promotes only that commit's `sha-<short>`, and the `edge` digest is unchanged. Also verify that a run whose `smoke` fails creates no tag: on the same scratch branch, push a commit that makes `tools/smoke_image.py` fail its `fresh` phase on purpose and dispatch again; `smoke` fails, `publish` is skipped, `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short of that commit>` fails because the tag does not exist, and `gh api users/manykarim/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest output of that run>") | .metadata.container.tags'` prints `[]` (this check needs the `read:packages` scope from 9.3; the anonymous `docker buildx imagetools inspect` above stays as the second check and cannot replace it, because inspect can only show that one name does not resolve, never that a digest carries no tag at all). Delete the scratch branch afterwards.
- [ ] 2.3 **(was `reproducible-image` 10.2)** One-time step: set `ghcr.io/manykarim/demo-webshop` to public in the GitHub package settings, confirm the package is linked to the repository, and re-run the failed `publish` job until it is green. Verify the scenario *Anonymous pull on Apple Silicon* for `edge`. Run `docker logout ghcr.io` and `docker pull ghcr.io/manykarim/demo-webshop:edge` on an amd64 machine and on an Apple Silicon machine. `docker image inspect --format '{{.Architecture}}'` prints `amd64` and `arm64` respectively, and `docker run -p 9090:9090 ghcr.io/manykarim/demo-webshop:edge` followed by `curl localhost:9090/health` returns `{"status":"ok","version":"dev"}` on both, which is the scenario *Image without a version tag* for `edge`. This also completes the README image-section check from 8.2: run those commands exactly as written with `<tag>` = `edge`.
- [x] 2.4 **(was `drift-coverage` 17.1)** After merging to `main`, check the published `edge` image, which `publish` tagged only after `test` passed for that candidate. Run `docker run -d -p 9090:9090 ghcr.io/manykarim/demo-webshop:edge` and wait for `healthy`, then `uv run pytest -m browser backend/tests/browser --base-url http://localhost:9090`, and `curl -s localhost:9090/openapi.json`. Verify that all four stages pass and that no `/api/workshop/` or `/api/admin/` path is listed. Version and `workshop-<id>` tags are cut after `acceptance-conformance`, not in this change.
- [x] 2.5 **(was `acceptance-conformance` 6.3)** Rehearse the non-blocking `main` path right after the gate PR merges and before any story PR. If an earlier `main` run already had a failing `conformance` job with a succeeding `publish`, record that run's URL instead. Otherwise merge a one-commit PR that adds to `backend/tests/conformance/test_harness.py` a check marked `conformance`, without an `ac` marker, that fails with `assert False, "non-blocking main rehearsal"`. Verify on that `main` run: `conformance` concluded `failure`, the step summary and the uploaded report each list the failing check under "Harness", neither names a criterion ID, and the step summary also names the space mode and the tested digest; `publish` ran and succeeded; an anonymous `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:edge` shows the digest from that run's `build` output. Then merge the revert and verify that the next `main` run has both `conformance` legs green and the guard passing. Record both run URLs in the gate PR description.
- [x] 2.6 **(was `acceptance-conformance` 1.1)** Confirm the Migration Plan prerequisites: `reproducible-image`, `workshop-spaces` and `drift-coverage` are merged into `main`, `drift-coverage` was archived right after merging (its task 17.3), and `edge` is published. `reproducible-image` may still be an active change (it is archived in its task 10.6, after its task 10.5 has checked this change's workshop tags), and `workshop-spaces`' archive state does not matter. Verify: `uv lock --check && uv sync --locked` succeeds; `compose.yaml` and `.github/workflows/image.yml` exist; the workflow has the jobs `check`, `build`, `smoke`, `test` and `publish`, the `build` job's `outputs:` map declares `digest`, `app_version` and `tags`, and `publish` has `needs: [build, smoke, test]`; `backend/tests/harness.py` defines `isolated_app`, and `grep -cE "def (temp_database|app_client|seeded_app_client|pdf_unavailable|fake_weasyprint)\(" backend/tests/conftest.py` prints 5; `workshop-spaces` is merged as well, which none of the later groups can work without: `backend/app/core/spaces.py` exists, `grep -q "def get_effective_flags" backend/app/core/feature_flags.py` and `grep -rq "X-Workshop-Space" backend/app` succeed, `backend/tests/spaces/conftest.py` defines the `shared_mode` fixture, `grep -qE "shared_mode|admin_token" backend/app/core/config.py` succeeds, and `backend/tests/harness.py` lists `WORKSHOP_ADMIN_TOKEN` in `SCRUBBED_ENV_VARS` and `"WORKSHOP_SHARED_MODE": "false"` in `PINNED_ENV_VARS` (none of these names exists on a branch without that change, so the check cannot pass vacuously); `pyproject.toml` has `pytest-playwright` in `[dependency-groups] dev` and a `[tool.pytest.ini_options]` that registers the `browser` marker with `addopts = "-m 'not browser'"`; `uv run python -c "from backend.app.core.workshop import PLANTED_BUGS, build_presets; assert 'BUG_CHECKOUT_TOTAL' in {b.flag for b in PLANTED_BUGS}; assert 'drift_and_bug' in build_presets()"` exits 0; `test -f openspec/specs/planted-bugs/spec.md && grep -q '### Requirement: Bug registry' openspec/specs/planted-bugs/spec.md` succeeds and `openspec list` no longer shows `drift-coverage`; an anonymous `docker pull ghcr.io/manykarim/demo-webshop:edge` succeeds.

## 3. Coolify deployment and version tag

- [ ] 3.1 **(was `reproducible-image` 10.3)** Switch the Coolify application from building the repository to the Docker image `ghcr.io/manykarim/demo-webshop:edge`. Use port 9090 and health check path `/health`. Remove any persistent storage, so the first start seeds fresh data, and do not set `WORKSHOP_APP_VERSION`. Verify that `curl https://<coolify-host>/health` returns `{"status":"ok","version":"dev"}`, that the deployed image digest equals the `edge` digest, that the home page loads, that the demo login `jamie@flowlinesupply.com` returns its order history, and that one seeded order's `summary_url` opens as a PDF.
- [ ] 3.2 **(was `reproducible-image` 10.4)** Push the git tag `v0.2.0`. Verify these points. The workflow is green. `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:0.2.0` lists `linux/amd64` and `linux/arm64`, and its digest equals the `build` job's digest output of the `v0.2.0` run (from the step summary), not merely the current `sha-<short>` tag. The image's `org.opencontainers.image.revision` label (`docker buildx imagetools inspect --format '{{json .Image}}'`) equals the tagged commit, and right after the run `sha-<short>` resolves to the same digest. After `docker logout ghcr.io`, `docker pull ghcr.io/manykarim/demo-webshop:0.2.0` on an Apple Silicon machine gives `docker image inspect --format '{{.Architecture}}'` = `arm64`, and a container from `0.2.0` reports `"version": "0.2.0"`. `docker manifest inspect ghcr.io/manykarim/demo-webshop:latest` fails. Record the `build` job duration of this run (multi-arch, warm cache from `main`); if it exceeds 15 minutes, open a follow-up to switch `build` to the native per-architecture matrix of design D7. Then switch Coolify to `0.2.0`, verify that `/health` reports `0.2.0`, and rehearse rollback: redeploy `edge` (`/health` reports `dev` and the deployed digest equals the `edge` digest), then `0.2.0` again.
- [ ] 3.3 **(was `workshop-spaces` 14.1)** After this change merges and the merge commit's `main` run of `image.yml` has passed `publish`, deploy that candidate on Coolify by digest (`ghcr.io/manykarim/demo-webshop@<digest>`, the `build` job's `digest` output from the run's step summary; not `edge` and not `sha-<short>`) following `docs/COOLIFY-RUNBOOK.md`. Verify:
  - `curl https://<coolify-host>/health` returns `{"status":"ok","version":"dev"}` (`reproducible-image` scenario *Image without a version tag*), the running image digest equals the recorded `build` digest, and the `org.opencontainers.image.revision` label (`docker buildx imagetools inspect --format '{{json .Image}}' ghcr.io/manykarim/demo-webshop@<digest>`) equals the merge commit;
  - the Coolify environment sets neither `WORKSHOP_DATABASE_URL` nor `WORKSHOP_PDF_OUTPUT_DIR`;
  - a preset POST without a space returns 401 with the space hint, and with `X-Workshop-Space: smoke-a` returns 200;
  - `https://<coolify-host>/?space=smoke-a` shows `data-workshop-space="smoke-a"`, and `https://<coolify-host>/` without a space shows `data-workshop-space="default"` with the hint;
  - `GET /api/workshop/status` without a space reports `v1` and no active bugs;
  - after an API add-to-cart and `POST /api/checkout/` under `smoke-a`, that order's `/api/docs/orders/{id}/invoice.pdf` returns 200 under `smoke-a`;
  - temporarily removing `WORKSHOP_ADMIN_TOKEN` makes the deployment fail with a log naming the variable, and the token is restored afterwards.
- [ ] 3.4 **(was `workshop-spaces` 14.2)** Run the early load test from outside the Coolify host network against the 14.1 deployment: headless, 40 users (or registered participants × 1.25 once known), at least 10 minutes, with `--csv` and `--html` reports kept outside git and attached to the PR. This run tunes the pool and is not capacity evidence. Verify: the run is recorded in the runbook's capacity record as kind `early` with image reference, commit, digest, users, duration, p95, 5xx count and leak count; tuned `pool_size` and `max_overflow` values, if changed, are committed to `backend/app/core/db.py`, the new commit's candidate is deployed by digest once its `main` run has published and checked as in 14.1, and the run is repeated; any `leak:` failure or 5xx is fixed as a defect before 14.4. A failed p95 here is a warning for 14.4, not a scale-out decision.
- [ ] 3.5 **(was `workshop-spaces` 14.3)** Rehearse the runbook procedures on the candidate deployment and record each outcome in the rehearsal record. Verify:
  - resetting one space with the documented curl returns its status to `v1` with an empty cart;
  - after a redeploy, a runtime order created before is 404, and status for `load-001` shows `v1` with an empty cart;
  - rolling back to `0.2.0` (the tag deployed by `reproducible-image` task 10.4) makes `/health` report `0.2.0`, and a preset POST without a space no longer returns 401;
  - redeploying the candidate by its recorded digest makes `/health` report `dev` with the candidate digest again, and a preset POST without a space returns 401 with the space hint.

## 4. Audit review, gating and workshop tag

- [ ] 4.1 **(was `acceptance-conformance` 17.2)** Review the whole decision record with the maintainer: every non-`conforms` row has a note with a working link; every `story-corrected` criterion has a Revisions entry with the original wording and the reason; every Revisions reason and withdrawn-table reason reads as a shopper- or consumer-facing clarification and reveals no defect, flag, drift stage, locator or detection purpose (the token check cannot catch paraphrases); withdrawn IDs and their replacements are in the README withdrawn table, and the story table counts are current; every row under "Planted bugs without criterion" has an approved reason; `openspec list` shows no open spin-out that the tag waits for. Verify: the guard passes; `git diff <import commit> -- 'docs/user-stories/WEB-*' 'docs/user-stories/API-*'` contains only hunks that map to a Revisions entry or to a withdrawal, and no criterion was removed without a withdrawn entry; the maintainer's approval, including the neutral-reason review, is recorded on the close-out PR.
- [ ] 4.2 **(was `acceptance-conformance` 18.1)** Rehearse the blocked tag (scenario "Unregistered deviation blocks the tag"). On a throwaway branch from `main`, rename `## Unreleased` to `## workshop-gatetest-1`, make the WEB-006_AC-7 check fail on purpose (wrong expected pattern), make one UI check fail in its drift variants but not in `[clean]`: pick a UI check that carries no `planted_bug` marker and point one of its locators at the stage-1 CSS class of a covered element, so that `[clean]` and `[stage3]` pass while `[stage2]`, `[stage4]` and `[drift_and_bug]` fail (`drift-coverage`'s stage contract renames the classes of covered elements in stages 2 and 4 and keeps them identical in stage 3, so no hook kind fails in stage 3 alone; a check without `planted_bug` markers keeps its `[drift_and_bug]` variant a real failure instead of the strict xfail that task 4.4 applies to marked checks). Push the git tag `workshop-gatetest-1`. Verify (gate behavior only): the run's `smoke` and `test` jobs passed, both `conformance` legs ran against the `build` job's `digest` output and failed with `WEB-006_AC-7` and the drift-only criterion — the one whose `[clean]` and `[stage3]` variants passed while its `[stage2]`, `[stage4]` and `[drift_and_bug]` variants failed — both named in the step summary, and `publish` was skipped; an anonymous `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:workshop-gatetest-1` fails because the tag does not exist. Record the run URL and its `build` `digest` output in the gate PR and hand them to `reproducible-image` task 10.5 (a), which checks the image level (the candidate digest carries no tag, `sha-<short>` included). Afterwards, delete the git tag and the branch (`git push --delete origin workshop-gatetest-1`).
- [ ] 4.3 **(was `workshop-spaces` 14.4)** Gating capacity test (T-1 week). Prerequisites: `drift-coverage` and `acceptance-conformance` are merged, and `main` is green. Deploy the candidate of the release-candidate commit (the commit intended for `workshop-<id>`) by digest, taken from its `main` run after `publish` passed, following the runbook, and check `dev`, the digest and the revision label as in 14.1. Verify that `GET /api/workshop/presets` lists `drift_and_bug`, which shows that `drift-coverage` is in the build. Run the load test from outside the host network: headless, 40 users (or registered participants × 1.25), at least 10 minutes, with preset switches including `buggy` and `drift_and_bug`, reports kept. Verify:
  - Locust exits 0: p95 below 1 s excluding `[slow-bug]`, zero 5xx, zero `leak:` failures and zero failed `asset:` requests;
  - in the CSV, the `[slow-bug]` entries for `/products` and `GET /api/products/` have a minimum response time of at least 1000 ms, which shows the planted delay is active and excluded;
  - the capacity record has a `gating` row with the image reference, commit (revision label), digest, `drift_and_bug` present, users, duration, p95, 5xx count and leak count;
  - the reset and redeploy rehearsal from 14.3 passes on this deployment.
  If the single-instance run fails: keep its row; record in the runbook the instance count N, the handle-character split with registered counts per instance, and the URL table; deploy the N instances; re-run Locust against every instance with that instance's registered handles × 1.25 users (while registrations are incomplete, at least the target divided by N, rounded up), all at the same time when the instances share a Coolify host; verify that the recorded split assigns each of `a`-`z` and `0`-`9` to exactly one instance (a short script over the runbook's split table prints each of the 36 characters once, with its instance, and exits non-zero on a missing or duplicate character), that every run exits 0, and record one row per instance. A `leak:` failure or 5xx at any share is a defect to fix, not a reason to scale out further. Any application change merged after this run and before tagging, including pool tuning, requires repeating 14.4 on the new commit.
- [ ] 4.4 **(was `acceptance-conformance` 18.2)** Once both `conformance` legs on `main` are green for the release candidate, commit on `main` the rename of `## Unreleased` to `## workshop-<id>`. The header names the image tag `workshop-<id>` and states that the `sha-` tag and the story commit are those of the tagged commit. Verify: `CONFORMANCE_RELEASE=workshop-<id> uv run pytest backend/tests/conformance/test_story_coverage.py` passes locally, and the `main` run for this commit is green.
- [ ] 4.5 **(was `acceptance-conformance` 18.3)** Push the git tag `workshop-<id>` on that commit. Afterwards, in one follow-up commit, record the literal `sha-<short>` and the digest (the tag run's `build` `digest` output, the authoritative identifier) in the section header without changing the table; a commit cannot contain its own hash. Verify (gate behavior only): in the tag run, both `conformance` legs ran against the `build` job's `digest` output and pass, and their `report.md` headers show version `workshop-<id>` and their space mode, with every criterion `pass` and every stage, planted-bug and `drift_and_bug` variant behaving as expected; `publish` ran only after both legs, `smoke` and `test` succeeded, and it succeeded; section `## workshop-<id>` contains exactly one `WEB-006_AC-7` row (scenario "Conformance record lookup"); the digest recorded in the header equals the `build` `digest` output of the tag run; the follow-up commit's diff touches only header lines, and the guard passes. Hand the run URL and digest to `reproducible-image` task 10.5 (b), which checks the image level of the published tag (platforms, promoted digest, `sha-<short>`, revision label, anonymous arm64 pull and the `/health` version).
- [ ] 4.6 **(was `reproducible-image` 10.5)** Check the image level of the first workshop tags, which `acceptance-conformance` pushes once its gate exists; this change itself pushes no `workshop-*` git tag and never publishes a rehearsal image under a `workshop-*` name (design D7, Migration Plan steps 5 and 6). Before merge, the `APP_VERSION` mapping is proven by the 9.1 unit tests and, when the 9.3 dry run resolves the local tag, the `type=match` rule too; otherwise that rule is first checked live in (a) below. The promotion mechanism is proven live on `v0.2.0` in 10.4. `acceptance-conformance` checks only the gate behavior in its tasks 18.1 and 18.3 and refers to this task for the image-level checks. (a) Blocked rehearsal tag, right after `acceptance-conformance` task 18.1 pushed `workshop-gatetest-1` and its `publish` job was skipped (scenario *Failed verification publishes no workshop tag*): take the `digest` output of that run's `build` job from its step summary; `gh api users/manykarim/packages/container/demo-webshop/versions --jq '.[] | select(.name == "<digest>") | .metadata.container.tags'` prints `[]` (needs the `read:packages` scope from 9.3; only the packages API can show that a digest carries no tag at all), and `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:sha-<short of the tagged commit>` fails or resolves to a different digest. From the same step summary, also verify the tag mapping on a real `workshop-*` ref, unconditionally and whether or not the 9.3 dry run fell back: the computed tag list contains `ghcr.io/manykarim/demo-webshop:workshop-gatetest-1` and `ghcr.io/manykarim/demo-webshop:sha-<short of the tagged commit>` and no `latest` and no `edge`, and the recorded `APP_VERSION` is `workshop-gatetest-1`. This is the live confirmation of the `type=match` rule and the `flavor: latest=false` flavor on a tag that is never published; a blocked candidate still proves the mapping, because `build` computes the tags before `publish` is skipped. (b) First real workshop tag, right after the `publish` job of the `acceptance-conformance` task 18.3 run succeeded (scenarios *Workshop tag publishing*, *Anonymous pull on Apple Silicon* and *Setup check reads the version* for a published workshop image): after `docker logout ghcr.io`, `docker buildx imagetools inspect ghcr.io/manykarim/demo-webshop:workshop-<id>` lists `linux/amd64` and `linux/arm64`, and its digest equals that run's `build` `digest` output; right after the run, `:sha-<short>` of the tagged commit resolves to the same digest; the `org.opencontainers.image.revision` label (`docker buildx imagetools inspect --format '{{json .Image}}'`) equals the full SHA of the tagged commit; `docker pull ghcr.io/manykarim/demo-webshop:workshop-<id>` on an Apple Silicon machine without credentials gives `docker image inspect --format '{{.Architecture}}'` = `arm64`, and a container started from it with only `-p 9090:9090` answers `/health` with status 200, `"status": "ok"` and `"version": "workshop-<id>"`. Verify that the results of (a) and (b), with both run URLs and digests, are recorded in a comment on this change's pull request.

## 5. Workshop day and handoff

- [ ] 5.1 **(was `workshop-spaces` 15.1)** After `acceptance-conformance` is merged and `workshop-<id>` is tagged, run the runbook's T-1 day checklist: deploy the `workshop-<id>` tag on Coolify, fill in the `<coolify-host>` placeholders in `docs/COOLIFY-RUNBOOK.md`, and compare the build with the gating run. The image's `org.opencontainers.image.revision` must equal the commit recorded in 14.4, or `git diff --stat <tested commit> workshop-<id> -- backend tools Dockerfile pyproject.toml uv.lock` must be empty (digests are not compared, because the version layer differs per tag). Otherwise, re-run the 14.4 load test against the `workshop-<id>` deployment before publishing the URL, record it, and apply the scale-out contingency if it fails. Then smoke-test with two spaces (preset isolation, the indicator, `Space: default` with the hint without a space, and a 401 without a space), redeploy to wipe the state, and hand the URL and the `X-Workshop-Space` convention to the maintainers of the workshop repository's `coolify` profile, including the note that agent configurations open `?space=<handle>` or send the header. Verify that `/health` reports `workshop-<id>`, that the rehearsal record contains the revision comparison or the re-run row, and that every checklist item is ticked.
- [ ] 5.2 **(was `workshop-spaces` 15.2)** On workshop day, run the day-of checklist: a fresh redeploy, a `/health` version check, a status check without a space (`v1`, no active bugs), watching the logs for 5xx, and keeping the token with facilitators only. Verify that the health check returns the `workshop-<id>` version and status without a space reports `v1` before participants start, and that the log review for the day shows no 5xx responses.
- [ ] 5.3 **(was `acceptance-conformance` 18.4)** Hand off to the maintainers of `manykarim/ai-engineering-robotframework` in an issue. It names the tag `workshop-<id>` and the story path `docs/user-stories/<STORY-ID>_*.md` at that tag, links the README sections "For downstream consumers" and the interpretation rules, and lists the story corrections and withdrawn IDs, the visible-text changes from app fixes, and new flag names as facilitator notes. It also states that `CONFORMANCE.md` and `backend/tests/conformance/` must not be copied into the participant repository. Verify: the issue exists and contains all six items.

## 6. Close-out

- [ ] 6.1 After every task above is done, archive this change with `openspec archive workshop-rollout -y`. It has no spec delta (`skip_specs: true`), so archiving changes no file under `openspec/specs/`. Verify that `openspec list` no longer shows `workshop-rollout` and that `git diff --stat -- openspec/specs` is empty for the archive commit.
